---
phase: 10-push-alerts
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - backend/services/alert_watcher.py
  - backend/schemas/alerts.py
  - backend/api/routes.py
  - backend/.env.example
  - backend/tests/test_alert_watcher.py
  - backend/tests/test_fcm_secrets.py
  - backend/tests/test_live_fcm.py
  - .planning/phases/10-push-alerts/10-COVERAGE.md
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The watcher evaluates all 18 mapped cities through the cached tool seam and dispatches only Orange and Red alerts
    - Repeat alerts for the same location plus level stay silent for 6 hours while an Orange-to-Red upgrade re-fires immediately
    - Subscribe, direct send, manual check, and status endpoints work with throttle plus sanitize plus 422/502 mapping and key-free bodies
  artifacts:
    - backend/services/alert_watcher.py
    - backend/schemas/alerts.py
    - backend/api/routes.py
    - backend/tests/test_alert_watcher.py
    - backend/tests/test_fcm_secrets.py
    - backend/tests/test_live_fcm.py
    - .planning/phases/10-push-alerts/10-COVERAGE.md
  key_links:
    - Watcher alert_level comes from the tool payload that derive_alert_level produced, never a second heuristic
    - Routes call watcher plus fcm_client behind the per-IP throttle and sanitize_detail screen
---

<objective>
Complete NOTF-01 plus NOTF-02 per D-01, D-02, D-03: in-memory registry with subscribe endpoints, hourly watcher over all 18 cities with 6h dedup, direct send endpoint for token plus topic, coverage matrix with the SMS/WhatsApp OPT-OUT, and the full secret-safety plus pass-plus-report gates.

Purpose: Turns the proven Plan 01 send path into a scheduled, deduped, operable push system with registration endpoints and a decided coverage matrix.
Output: alert_watcher.py, schemas/alerts.py, route additions, env doc line, three test files, 10-COVERAGE.md, green full suite.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-push-alerts/10-CONTEXT.md
@.planning/phases/09-multilingual-voice/09-COVERAGE.md
@backend/services/fcm_client.py
@backend/api/routes.py
@backend/api/throttle.py
@backend/schemas/chat.py
@backend/tools/weather.py
</context>

<tasks>

<task type="auto">
  <name>Watcher: registry plus cached-seam evaluate plus 6h dedup</name>
  <files>backend/services/alert_watcher.py</files>
  <read_first>backend/services/fcm_client.py full file for send_to_token plus send_to_topic plus load_service_account plus token-provider signatures; backend/tools/weather.py CITY_COORDS plus CACHE_TTL_S plus get_current_weather invoke convention plus _CACHE_LOCK discipline; backend/tools/imd_client.py derive_alert_level plus _ALERT_SEVERITY ordering</read_first>
  <acceptance_criteria>File backend/services/alert_watcher.py exports subscribe, unsubscribe, registry_counts, evaluate_city, should_dispatch, run_watch_cycle, reset_for_tests. WATCH_CITIES derives from CITY_COORDS and holds exactly 18 entries. DISPATCH_LEVELS equals Orange plus Red only. COOLDOWN_S equals 21600. should_dispatch returns False for Green and Yellow always, False for a same location-plus-level repeat inside the cooldown, True for an Orange-to-Red upgrade inside the cooldown. evaluate_city returns the tool payload alert_level without calling derive_alert_level itself. run_watch_cycle returns a dict with checked plus dispatched plus skipped keys and never raises on a single-city failure.</acceptance_criteria>
  <action>Create backend/services/alert_watcher.py per D-01 all 18 mapped cities with hourly evaluation and Orange-plus-Red dispatch only, per D-02 in-memory registry with no DB, and per D-03 one dispatch per location-plus-level per 6 hours with severity upgrades re-firing immediately. Build the registry as lock-guarded _tokens set plus _topic_subs dict with subscribe accepting a non-empty token capped at 2048 chars plus topic list validated against the pattern capital-A to Z, small-a to z, digits, dot, underscore, dash, tilde, percent up to 128 chars, raising ValueError otherwise. Implement evaluate_city by invoking the existing get_current_weather tool seam with the city name and parsing its JSON string, taking alert_level plus advisory plus location straight from the payload so derive_alert_level stays the sole threshold authority with no second heuristic, and return None on any exception after logger.exception so one bad city never aborts the cycle. Implement should_dispatch on monotonic time with a per location-plus-level timestamp plus a per-location last-level record, applying the 21600 second cooldown on repeats and bypassing it only when the new severity strictly exceeds the last dispatched severity for that location. Implement run_watch_cycle accepting an optional send function defaulting to the fcm_client token-plus-topic senders and an optional now override for tests, iterating the 18 cities, dispatching to topic alerts-slug plus directly to tokens subscribed to that topic with title WeatherGPT plus level plus alert plus location and body from the advisory truncated to 200 chars, and collecting per-city dispatched-or-skipped entries. Add reset_for_tests clearing registry plus dedup state. Recorded planner decision on watcher trigger from the agent discretion list: callable run_watch_cycle plus a POST slash api slash alerts slash check route from the next task as the cron target with hourly cadence documented, instead of an in-process scheduler, because a scheduler would need a new dependency and the cron-callable keeps stdlib-only posture.</action>
  <verify>
    <automated>python -m pytest tests/test_alert_watcher.py -q</automated>
    <fails_when>pytest exits non-zero, or any dedup-matrix case returns the wrong dispatch boolean</fails_when>
  </verify>
  <done>Registry, cached-seam evaluation, and the full dedup matrix behave per D-01 through D-03</done>
</task>

<task type="auto">
  <name>Routes: subscribe plus send plus check plus status with throttle and sanitize</name>
  <files>backend/schemas/alerts.py, backend/api/routes.py, backend/.env.example</files>
  <read_first>backend/api/routes.py chat plus voice_transcribe plus _check_voice_throttle for the throttle plus sanitize_detail plus ValueError-to-422 plus RuntimeError-to-502 mapping; backend/schemas/chat.py SpeakRequest plus TranscribeResponse for schema style; backend/services/alert_watcher.py subscribe plus run_watch_cycle plus registry_counts signatures from the previous task</read_first>
  <acceptance_criteria>POST slash api slash alerts slash subscribe with a token plus topics returns 200 without echoing the full token; an invalid topic returns 422. POST slash api slash alerts slash send with alert_level Green returns 422 and with Orange plus a token or topic returns 200 carrying the FCM message name. POST slash api slash alerts slash check returns 200 with checked equal to 18. GET slash api slash alerts slash status returns 200 with watch_cities 18 plus cooldown_hours 6 plus fcm_configured boolean and no token material. backend slash .env.example documents FCM_SERVICE_ACCOUNT_FILE. Command python -m pytest tests/test_alert_watcher.py -q -k route exits 0.</acceptance_criteria>
  <action>Create backend/schemas/alerts.py with SubscribeRequest holding token max length 2048 plus topics list of max length 32, SendRequest holding optional token plus optional topic plus title max length 200 plus body max length 1000 plus alert_level plus location max length 120, and StatusResponse plus CheckResponse shapes; if any target file is R-flagged run attrib -R on it before editing. Extend backend/api/routes.py with four endpoints reusing check_rate_limit plus sanitize_detail plus the established 422 for ValueError, 502 for RuntimeError, 500 catch-all mapping per the route error-mapping carryover: POST subscribe registering via watcher subscribe and echoing only the last 6 chars of the token; POST send requiring exactly one of token or topic, rejecting Green and Yellow with 422 per D-01 never-push, loading the service account and returning a clear 502 naming the missing file when unconfigured, otherwise dispatching through fcm_client and returning the message name; POST check running run_watch_cycle server-side and returning its report as the documented hourly cron target; GET status returning watch city count plus cooldown hours plus subscription counts plus fcm_configured boolean with zero token bytes. Append to backend/.env.example an FCM_SERVICE_ACCOUNT_FILE line defaulting to fcm-service-account.json with a comment that a missing file disables push cleanly and the file stays gitignored.</action>
  <verify>
    <automated>python -m pytest tests/test_alert_watcher.py -q -k route</automated>
    <fails_when>Any route test returns an unexpected status code or a Green payload dispatches instead of 422</fails_when>
  </verify>
  <done>All four alert endpoints enforce throttle, sanitize, and the Orange-Red-only rule</done>
</task>

<task type="auto">
  <name>Seal: watcher tests plus sentinel suite plus coverage matrix plus live opt-in</name>
  <files>backend/tests/test_alert_watcher.py, backend/tests/test_fcm_secrets.py, backend/tests/test_live_fcm.py, .planning/phases/10-push-alerts/10-COVERAGE.md</files>
  <read_first>.planning/phases/09-multilingual-voice/09-COVERAGE.md for the INTEGRATE versus OPT-OUT table style plus notes section; backend/tests/test_live_sarvam.py for the skipped-by-default opt-in pattern; backend/tests/test_multilingual_secrets.py for the three-sentinel plus caplog plus route-body safety pattern</read_first>
  <acceptance_criteria>backend slash tests slash test_alert_watcher.py covers the dedup matrix, the Orange-via-cached-seam integration with a single upstream hit on repeat, and all four routes. backend slash tests slash test_fcm_secrets.py proves three sentinels absent from errors plus bodies plus logs. backend slash tests slash test_live_fcm.py is skipped by default and runs only under RUN_LIVE_FCM equal to 1. File .planning/phases/10-push-alerts/10-COVERAGE.md carries INTEGRATE rows for token send plus topic send plus threshold evaluation and exactly the SMS slash WhatsApp OPT-OUT row with the no-credentials-plus-DLT reason. Commands python -m pytest tests/test_alert_watcher.py -q, python -m pytest tests/test_fcm_secrets.py -q, and python -m pytest tests/ -q all exit 0 with the live test skipped.</acceptance_criteria>
  <action>Write backend/tests/test_alert_watcher.py with a monotonic-time monkeypatch driving should_dispatch through first-Orange fires, repeat inside 6h suppressed, Orange-to-Red upgrade fires immediately, Green and Yellow never fire, Red-to-Orange downgrade inside cooldown suppressed; an imd_client MockTransport test returning weather_code 95 current JSON proving evaluate_city surfaces Orange through the cached get_current_weather seam with one upstream hit across two calls; and ASGITransport route tests for subscribe 200 plus invalid-topic 422 plus send-Green 422 plus send-Orange 200 with fcm transport mocked plus check returning checked 18 plus status counts. Write backend/tests/test_fcm_secrets.py mirroring the multilingual sentinel suite with three unique sentinels for private key plus access token plus device token, forcing a 500 through send plus the send route plus the check route, asserting all sentinels absent from raised errors plus response bodies plus caplog text, plus a sanitize_detail unit check. Write backend/tests/test_live_fcm.py following the live_sarvam opt-in shape skipped unless RUN_LIVE_FCM equals 1, sending one validate_only benign Orange payload so nothing delivers. Write .planning/phases/10-push-alerts/10-COVERAGE.md with INTEGRATE rows for direct-token send via FCM v1 messages:send behind the token-provider seam tagging NOTF-01, topic send to alerts-slug tagging NOTF-01, threshold evaluation over all 18 cities through the cached seam with derive_alert_level as sole authority tagging NOTF-02, each row naming endpoint plus MockTransport-only CI posture; an OPT-OUT section holding exactly the SMS slash WhatsApp row with reason provider key plus DLT registration absent and deferred per CONTEXT; a notes section recording the 6h cooldown plus upgrade re-fire, the google-auth opt-in for live sends with zero new runtime deps, validate_only live posture, and key-never-logged posture. Finish with the proven Phase 09 verify command verbatim python -m pytest tests/ -q as the pass-plus-report gate.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Full suite exits non-zero, or the live test runs without RUN_LIVE_FCM equal to 1, or any COVERAGE row lacks its requirement tag</fails_when>
  </verify>
  <done>Watcher, routes, secrets, coverage matrix, and full suite all green with live tests safely skipped</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to alert routes | Untrusted token, topic, title, body strings cross here |
| watcher to provider seam | Provider JSON crosses here on every cycle |
| registry memory to status route | Token material must never cross back out |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Information disclosure | status plus subscribe responses | high | mitigate | Echo at most token last-6 plus counts; sentinel suite covers bodies plus logs |
| T-10-05 | Denial of service | watcher cycle plus check route | medium | mitigate | Per-IP throttle reused; single-city failure skips instead of aborting; 18-city bound |
| T-10-06 | Elevation of privilege | send endpoint | medium | mitigate | Orange-Red-only enforcement at send; topic shape validated; provider errors map to sanitized 502 |
| T-10-07 | Spoofing | forged alert_level in send body | low | accept | Send callers assert human-operated use; watcher path always uses tool-derived levels |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan; requirements file stays byte-identical |
</threat_model>

<verification>
Watcher proof: dedup matrix plus cached-seam Orange plus route battery green. Safety proof: three-sentinel suite clean on errors, bodies, and logs. Docs proof: 10-COVERAGE.md carries INTEGRATE rows for send token plus send topic plus threshold evaluation and exactly the SMS/WhatsApp OPT-OUT with reason. Gate: python -m pytest tests/ -q fully green with the live test skipped by default.
</verification>

<success_criteria>
Hourly watcher evaluates 18 cities through the cached seam, Orange and Red dispatch with 6h dedup and instant upgrade re-fire, endpoints enforce throttle plus sanitize, secrets stay clean, coverage matrix decided, full suite green.
</success_criteria>

<output>
Create `.planning/phases/10-push-alerts/10-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Plan |
|----------|------|------|
| backend/services/fcm_client.py (auth seam plus token/topic send) | file | 10-01 |
| backend/tests/test_fcm_dispatch.py (tracer plus sentinel proof) | file | 10-01 |
| backend/services/alert_watcher.py (registry plus evaluate plus dedup) | file | 10-02 |
| backend/schemas/alerts.py plus api/routes.py endpoints | file | 10-02 |
| backend/tests/test_alert_watcher.py plus test_fcm_secrets.py plus test_live_fcm.py | file | 10-02 |
| .planning/phases/10-push-alerts/10-COVERAGE.md (FCM matrix) | file | 10-02 |
