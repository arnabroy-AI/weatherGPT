---
phase: 10-push-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/fcm_client.py
  - backend/tests/test_fcm_dispatch.py
autonomous: true
requirements:
  - NOTF-01
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - An Orange alert payload dispatches through FCM v1 (token path and topic path) with Bearer auth and the reply carries the FCM message name
    - A missing service-account file yields a clear disabled signal and no import-time or call-time crash
    - No private-key, token, or key material ever appears in logs, errors, or responses
  artifacts:
    - backend/services/fcm_client.py
    - backend/tests/test_fcm_dispatch.py
  key_links:
    - Token-provider seam output lands on the outbound Authorization Bearer header
    - Service-account project_id builds the FCM v1 send URL
---

<objective>
Prove the FCM dispatch slice end-to-end per D-02 and NOTF-01: service-account load plus token-provider seam plus raw httpx send to FCM v1 for token and topic targets, verified with a fully mocked transport and an Orange fixture pinned through derive_alert_level.

Purpose: This is the tracer. Every later watcher and route task depends on this send path being proven, so it lands first with production-quality error handling and key-free logging.
Output: backend/services/fcm_client.py plus backend/tests/test_fcm_dispatch.py, pytest green.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-push-alerts/10-CONTEXT.md
@backend/services/sarvam_client.py
@backend/tools/imd_client.py
@backend/core/config.py
</context>

<tasks>

<task type="tracer">
  <name>Tracer: FCM client with auth seam plus token and topic send</name>
  <files>backend/services/fcm_client.py</files>
  <read_first>backend/services/sarvam_client.py full file for the transport-override plus _require_api_key plus sanitized RuntimeError plus key-never-logged pattern; backend/core/config.py for the existing FCM_SERVICE_ACCOUNT_FILE setting plus get_settings caching; backend/tools/imd_client.py set_transport plus REQUEST_TIMEOUT_SECONDS plus sanitized fetch pattern</read_first>
  <acceptance_criteria>File backend/services/fcm_client.py exports load_service_account, get_access_token, send_to_token, send_to_topic, set_transport, reset_transport, set_token_provider, reset_token_provider. load_service_account returns None for a missing file and raises a sanitized RuntimeError for malformed JSON or missing keys type plus project_id plus private_key plus client_email plus token_uri. send_to_token and send_to_topic POST to https colon slash slash fcm.googleapis.com slash v1 slash projects slash PROJECT_ID slash messages:send with an Authorization Bearer header and return the response name field. Empty token or topic raises ValueError. Non-200 raises a sanitized RuntimeError carrying only the status code. Every log line carries at most the message name plus level plus location, never token or key bytes.</acceptance_criteria>
  <action>Create backend/services/fcm_client.py per D-02 token-plus-topic dispatch and the no-new-runtime-deps carryover, using stdlib plus httpx only with zero additions to backend/requirements.txt. Recorded planner decision: auth uses seam option b, not google-auth, because RS256 signing needs a crypto library that is not installed, CONTEXT prefers no new runtime deps with raw httpx to FCM v1, and CI is mocked anyway; the live google-auth path stays a documented opt-in inside get_access_token. Implement load_service_account with an optional path argument defaulting to get_settings().FCM_SERVICE_ACCOUNT_FILE, returning None when the file is absent per the missing-file clear-skip carryover and never crashing at import. Implement module-level _TRANSPORT_OVERRIDE with set_transport and reset_transport mirroring sarvam_client, plus _TOKEN_PROVIDER_OVERRIDE with set_token_provider and reset_token_provider so tests inject a fake bearer without any crypto. Implement get_access_token to call the injected provider first when set, otherwise lazily import google.oauth2.service_account and raise a clear RuntimeError naming the pip install google-auth opt-in when the import is absent. Implement send_to_token building message dict with token plus notification title plus body plus data alert_level plus location, and send_to_topic building the same shape with topic instead of token. Use a 10 second timeout, log success with logger.info carrying only message name plus target kind plus alert level, log failures with logger.exception server-side while surfacing sanitized messages. Never place the private key, access token, or service-account JSON in any message, log, or return value.</action>
  <verify>
    <automated>python -m pytest tests/test_fcm_dispatch.py -q</automated>
    <fails_when>pytest exits non-zero or reports no tests collected, meaning the tracer slice is not wired</fails_when>
  </verify>
  <done>Orange fixture dispatches through the mocked FCM endpoint on both token and topic paths and the message name returns</done>
</task>

<task type="auto">
  <name>Dispatch proof: Orange fixture plus negative paths plus sentinel test</name>
  <files>backend/tests/test_fcm_dispatch.py</files>
  <read_first>backend/services/sarvam_client.py translate_text plus _post_translate for MockTransport assertion style; backend/tests/test_multilingual_secrets.py full file for the sentinel plus caplog plus ASGITransport safety pattern; backend/tools/imd_client.py derive_alert_level for the Orange-pinning call signature</read_first>
  <acceptance_criteria>File backend/tests/test_fcm_dispatch.py holds at least six tests: Orange authority pin, token-path dispatch, topic-path dispatch, missing-file skip, forced-500 sentinel cleanliness, empty-target ValueError. The MockTransport handler asserts request path v1 slash projects slash test-project-123 slash messages:send, Authorization header Bearer fake-access-token-9z, and JSON body message dot token or message dot topic. Command python -m pytest tests/test_fcm_dispatch.py -q exits 0. No test reads the real backend slash fcm-service-account.json and no test performs live network I/O.</acceptance_criteria>
  <action>Create backend/tests/test_fcm_dispatch.py with a tmp_path fake service-account JSON using project_id test-project-123 plus client_email fake at test-project-123.iam.gserviceaccount.com plus token_uri https colon slash slash oauth2.googleapis.com slash token plus private_key holding the literal fake-private-key-material-UNIQUE-7f3a, injected via monkeypatched FCM_SERVICE_ACCOUNT_FILE plus get_settings cache_clear, and a token provider injected via set_token_provider returning fake-access-token-9z with reset in a finally block. First pin the sole-threshold-authority carryover by asserting derive_alert_level with code 95 returns Orange, then send an Orange Mumbai payload through the MockTransport on both paths. Assert the token path body carries message dot token device-token-abc plus notification title plus body plus data dot alert_level Orange, the topic path body carries message dot topic alerts-mumbai, and both return the message name projects slash test-project-123 slash messages slash msg-1. Add a missing-file test pointing the setting at a nonexistent path asserting load_service_account returns None and send raises RuntimeError containing the words not configured. Add a forced-500 test with caplog at INFO asserting the raised error plus captured logs contain neither the fake private key nor the fake access token. Add empty token and empty topic tests asserting ValueError. Mirror the conftest isolation discipline by clearing get_settings cache and resetting both transport and token provider overrides after each test without editing conftest.</action>
  <verify>
    <automated>python -m pytest tests/test_fcm_dispatch.py -q</automated>
    <fails_when>Any test fails or errors, or the Orange pin assertion returns a level other than Orange</fails_when>
  </verify>
  <done>Mocked FCM dispatch proven on both paths with missing-file skip and zero key leakage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller to fcm_client | Untrusted token, topic, title, body strings cross here |
| fcm_client to Google FCM | Bearer credential leaves the server here |
| disk service-account file to loader | File bytes are secret material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information disclosure | fcm_client logs and errors | high | mitigate | Log only message name plus level plus location; sanitized RuntimeError carries status code only; sentinel test in this plan |
| T-10-02 | Spoofing | service-account file on disk | medium | mitigate | File already gitignored; loader validates shape and never echoes contents; missing file disables instead of crashing |
| T-10-03 | Tampering | FCM request body | medium | mitigate | Token and topic length plus shape validated before send; empty values raise ValueError |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan; google-auth stays an opt-in import, never installed here |
</threat_model>

<verification>
Tracer proof: python -m pytest tests/test_fcm_dispatch.py -q passes with token plus topic dispatch through MockTransport and the FCM v1 URL shape asserted. Safety proof: forced-500 plus caplog shows no private key or bearer bytes. Scope proof: backend/requirements.txt byte-identical to before this plan.
</verification>

<success_criteria>
Orange fixture reaches the mocked FCM v1 send URL on both paths with Bearer auth, missing credentials disable cleanly, no key material leaks, and the new tests pass.
</success_criteria>

<output>
Create `.planning/phases/10-push-alerts/10-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Artifact | Type | Plan |
|----------|------|------|
| backend/services/fcm_client.py (auth seam plus token/topic send) | file | 10-01 |
| backend/tests/test_fcm_dispatch.py (tracer plus sentinel proof) | file | 10-01 |
| backend/services/alert_watcher.py (registry plus evaluate plus dedup) | file | 10-02 |
| backend/schemas/alerts.py plus api/routes.py endpoints | file | 10-02 |
| backend/tests/test_alert_watcher.py plus test_fcm_secrets.py plus test_live_fcm.py | file | 10-02 |
| .planning/phases/10-push-alerts/10-COVERAGE.md (FCM matrix) | file | 10-02 |
