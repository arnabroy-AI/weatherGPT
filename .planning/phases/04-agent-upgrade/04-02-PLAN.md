---
phase: 04-agent-upgrade
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - services/agri_advisories.py
  - services/agent.py
  - tests/test_agent_agri.py
autonomous: true
requirements:
  - AGNT-03
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - Paddy sowing advice for Nashik returns curated crop guidance combined with live tool weather values, not generic text (per D-05)
    - Any open-ended agri/climate question is answered best-effort from curated base plus live data, never refused for missing crop (per D-06)
    - Tool values quoted in agri replies match the tool JSON (grounding carries over from Plan 01)
  artifacts:
    - services/agri_advisories.py (curated static advisories, test-assertable)
    - services/agent.py (agri/climate prompt rules referencing curated base + live tools)
    - tests/test_agent_agri.py (mocked-LLM agri grounding tests)
  key_links:
    - Curated module imported/available to the agent prompt path without changing _TOOLS
    - Agri replies combine curated text with live get_current_weather / get_weather_forecast JSON
---

<objective>
Curated agri advisories plus live tool grounding for open-ended agri/climate questions.

Purpose: Delivers AGNT-03 — grounded agri answers from a test-assertable curated base combined with live weather data, with no new tools and no route/schema changes.
Output: New services/agri_advisories.py module, agri/climate SYSTEM_PROMPT rules, and mocked-LLM agri tests.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-upgrade/04-CONTEXT.md
@services/agent.py
@tools/weather.py
</context>

<tasks>

<task type="auto">
  <name>Curated agri advisory module</name>
  <files>services/agri_advisories.py</files>
  <read_first>tools/imd_client.py (_build_advisory wording authority and non-IMD qualifiers to mirror); tools/weather.py (CITY_COORDS city coverage including nashik and pune)</read_first>
  <action>Create services/agri_advisories.py as a dependency-free module exposing a lookup function keyed by crop and season cue (normalize case/whitespace; accept at least paddy, wheat, cotton, sugarcane, maize, soybean, plus kharif/rabi/summer/monsoon cues) returning short advisory strings that always carry the non-IMD qualifier, plus a generic best-effort fallback advisory for unknown crops per D-06 open-ended scope. Keep content compact (a few dozen entries maximum), plain data plus one lookup function, no secrets, no network, no tool registration. Per D-05 the module is prompt-adjacent data the agent combines with live tool output, not a replacement for tool calls.</action>
  <verify>
    <automated>python -m pytest tests/test_agent_agri.py -q -k curated</automated>
    <fails_when>pytest exits non-zero or reports no tests ran for the curated lookup selection</fails_when>
  </verify>
  <acceptance_criteria>services/agri_advisories.py exposes a crop-plus-season lookup returning non-empty strings containing non-IMD for paddy/kharif and for an unknown crop; module imports with no third-party imports; curated lookup tests pass.</acceptance_criteria>
  <done>Curated advisory base exists, is test-assertable, and covers known plus unknown crops best-effort.</done>
</task>

<task type="auto">
  <name>Agri/climate prompt rules plus mocked-LLM agri tests</name>
  <files>services/agent.py, tests/test_agent_agri.py</files>
  <read_first>services/agent.py (SYSTEM_PROMPT rules from Plan 01, _TOOLS list that must stay at two entries); services/agri_advisories.py (lookup function name and qualifier wording); tests/test_agent_grounding.py (FakeExecutor harness pattern to reuse)</read_first>
  <action>Append numbered agri/climate rules to SYSTEM_PROMPT in the same imperative style per D-05/D-06: for agri/climate questions call get_current_weather and, for sowing/harvest timing, get_weather_forecast, then combine live values with the curated advisory base; answer any crop best-effort (never refuse for a missing crop; use the generic fallback); keep the disclosure rule and Alert line rule applicable to agri replies. Do not add tools, do not change _TOOLS, retry, threadpool, logging, or routes. Reuse the Plan 01 FakeExecutor harness (patch services.agent._get_executor, real tool JSON via MockTransport) in tests/test_agent_agri.py with at least three tests: paddy sowing for Nashik returns curated paddy guidance plus the live temperature_c value; an unknown-crop question returns a best-effort answer instead of a refusal; a climate summary question grounds in forecast JSON day values.</action>
  <verify>
    <automated>python -m pytest tests/test_agent_agri.py -q</automated>
    <fails_when>pytest exits non-zero or any agri grounding assertion fails while curated lookup tests pass</fails_when>
  </verify>
  <acceptance_criteria>SYSTEM_PROMPT contains agri rules naming both live tools and the curated base; tests/test_agent_agri.py proves Nashik paddy reply contains curated text and the tool temperature value; unknown-crop test proves a best-effort answer; python -m pytest tests/test_agent_agri.py -q passes.</acceptance_criteria>
  <done>Open-ended agri/climate answers proven grounded in curated base plus live tool data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated data→reply | Static advisory text flows into user-visible replies |
| tool JSON→agri reply | Live values combined with curated text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure | services/agri_advisories.py | low | mitigate | Static agronomy text only; no secrets, no user data, no network |
| T-04-04 | Tampering | Agri prompt rules | low | mitigate | Rules reference curated base plus live tools; tests pin quoted values to tool JSON |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new runtime deps; no install step |
</threat_model>

<verification>
Agri slice green: python -m pytest tests/test_agent_agri.py -q passes; no regression: python -m pytest tests/test_agent_grounding.py -q passes; both tools still registered in services/agent.py _TOOLS.
</verification>

<success_criteria>
Paddy sowing for Nashik returns curated plus live-grounded advice per D-05; unknown crops answered best-effort per D-06; no new tools, no schema/route changes.
</success_criteria>

## Artifacts this phase produces

- services/agri_advisories.py — curated static advisories keyed by crop and season cue with best-effort fallback (NEW from this plan)
- services/agent.py — agri/climate SYSTEM_PROMPT rules appended (extends Plan 01)
- tests/test_agent_agri.py — mocked-LLM agri grounding tests reusing the Plan 01 harness (NEW from this plan)

<output>
Create `.planning/phases/04-agent-upgrade/04-02-SUMMARY.md` when done
</output>
