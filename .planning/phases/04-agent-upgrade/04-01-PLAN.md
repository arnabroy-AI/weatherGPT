---
phase: 04-agent-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/agent.py
  - tests/test_agent_grounding.py
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Current-weather replies quote values from tool JSON instead of inventing observations (per D-01)
    - Replies disclose non-IMD model data and surface fallback/stale notes (per D-02)
    - Unknown location triggers exactly one clarifying question, never a silent default city (per D-03, D-04)
    - Alert line format still matches the Phase 1 reply regex (carryover)
  artifacts:
    - services/agent.py (upgraded SYSTEM_PROMPT rules 1-7)
    - tests/test_agent_grounding.py (mocked-LLM harness + grounding tests)
  key_links:
    - SYSTEM_PROMPT grounding rules drive mocked-LLM replies in tests
    - intermediate_steps tool JSON is the assertion oracle for grounding
---

<objective>
Tracer: prove the end-to-end grounded-reply slice through the upgraded prompt with a mocked LLM — tool values quoted, disclosure present, Alert line intact.

Purpose: Establishes the single seam (services/agent.py SYSTEM_PROMPT) plus the mocked-LLM test harness every later plan builds on, without touching routes, schemas, or tool behavior.
Output: Upgraded SYSTEM_PROMPT grounding/location/disclosure rules and a passing mocked-LLM grounding test module.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-upgrade/04-CONTEXT.md
@.planning/phases/03-forecast-alerts/03-CONTEXT.md
@services/agent.py
@tools/weather.py
@tests/test_error_contract.py
</context>

<tasks>

<task type="tracer">
  <name>End-to-end grounded reply — one path through upgraded prompt with mocked LLM</name>
  <files>services/agent.py, tests/test_agent_grounding.py</files>
  <read_first>services/agent.py (SYSTEM_PROMPT rules 1-7, _TOOLS list, _derive_alert_level regex, process_chat intermediate_steps recovery); tools/weather.py (_FALLBACK_NOTE, disclosure fields source/advisory/stale); tests/test_error_contract.py (LLM fail-loudly 502 pattern that must stay untouched)</read_first>
  <action>Rewrite services/agent.py SYSTEM_PROMPT keeping numbered-imperative style and backticked tool names, per locked decisions: strengthen grounding rules per D-01 (call get_current_weather / get_weather_forecast, quote temperature_c / condition / alert_level values from tool JSON, never invent observations); rewrite rule 6 per D-02 to mandate disclosure wording (state non-IMD model data, repeat tool source note, surface fallback/stale notes in the reply); encode D-03 (exactly ONE clarifying follow-up when location unknown/unmappable, then best-effort answer, no refusal) and D-04 (never assume a default city such as Delhi). Keep Alert line rule verbatim-compatible with the regex Alert colon level in _derive_alert_level so worst-day lines like Alert: Orange (Sat) still match; keep _TOOLS list, 30s retry/threadpool/logging, and process_chat signature untouched. Build the mocked-LLM harness in tests/test_agent_grounding.py by patching services.agent._get_executor with a FakeExecutor whose invoke returns canned output plus intermediate_steps carrying REAL tool JSON (obtained by calling get_current_weather through imd_client MockTransport, never live network and never OpenRouter). Write at least three tests: current-weather reply quotes the tool temperature_c value and condition string; reply contains the disclosure phrase non-IMD model data; reply ends with an Alert line matching the regex. No live OpenRouter calls anywhere in tests.</action>
  <verify>
    <automated>python -m pytest tests/test_agent_grounding.py -q</automated>
    <fails_when>pytest exits non-zero or reports any failed, skipped-grounding, or no-tests-ran outcome</fails_when>
  </verify>
  <acceptance_criteria>SYSTEM_PROMPT contains the disclosure phrase non-IMD model data and a one-follow-up rule with no default-city language naming Delhi; tests/test_agent_grounding.py patches only services.agent._get_executor and asserts the reply contains the tool temperature value; python -m pytest tests/test_agent_grounding.py -q passes; grep for Alert: in services/agent.py still matches at least one rule line.</acceptance_criteria>
  <done>Single happy path works end-to-end with mocked LLM: grounded values quoted, disclosure present, Alert line intact, suite green.</done>
</task>

<task type="auto">
  <name>Location one-follow-up behavior tests</name>
  <files>tests/test_agent_grounding.py</files>
  <read_first>services/agent.py (SYSTEM_PROMPT location rules written by the tracer task); schemas/chat.py (location field contract, unchanged)</read_first>
  <action>Add mocked-LLM tests in tests/test_agent_grounding.py proving D-03/D-04 through process_chat with the FakeExecutor harness: unknown location with no context yields a reply asking exactly one clarifying question and containing no city temperature values; a follow-up call carrying a location yields a grounded answer; no test ever asserts a Delhi or Mumbai default. Keep the harness seam identical (patch services.agent._get_executor only) and keep api/routes.py, schemas, and tools untouched.</action>
  <verify>
    <automated>python -m pytest tests/test_agent_grounding.py -q</automated>
    <fails_when>pytest exits non-zero or the location tests fail while the tracer tests pass</fails_when>
  </verify>
  <acceptance_criteria>New tests assert a clarifying-question reply for unknown location and a grounded reply once location is supplied; no occurrence of a default-city assertion exists in the file; python -m pytest tests/test_agent_grounding.py -q passes with all tests green.</acceptance_criteria>
  <done>One-follow-up flow proven by mocked-LLM tests; no default city anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt→LLM output | Untrusted model text crosses here; grounding tests pin it to tool JSON |
| tool JSON→reply | Provider values flow into user-visible replies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | SYSTEM_PROMPT content | medium | mitigate | Prompt and tests contain no secrets; secret-safety asserted in Plan 03 |
| T-04-02 | Tampering | Mocked-LLM harness | low | mitigate | FakeExecutor patches services.agent._get_executor only in tests; no production seam change |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new runtime deps in this phase; no install step |
</threat_model>

<verification>
Tracer slice green: python -m pytest tests/test_agent_grounding.py -q passes; full suite unbroken: python -m pytest tests/ -q passes.
</verification>

<success_criteria>
SYSTEM_PROMPT grounding, disclosure, and one-follow-up rules land per D-01..D-04; mocked-LLM tests prove reply values match tool JSON; Alert regex, tools, retry/threadpool/logging untouched.
</success_criteria>

## Artifacts this phase produces

- services/agent.py — upgraded SYSTEM_PROMPT (grounding D-01, disclosure D-02, location D-03/D-04); sole production seam
- tests/test_agent_grounding.py — mocked-LLM harness (FakeExecutor over services.agent._get_executor) + grounding and one-follow-up tests
- Follow-on plans add: services/agri_advisories.py + agri prompt rules (Plan 02); degraded-mode rules + secret-safety gate (Plan 03)

<output>
Create `.planning/phases/04-agent-upgrade/04-01-SUMMARY.md` when done
</output>
