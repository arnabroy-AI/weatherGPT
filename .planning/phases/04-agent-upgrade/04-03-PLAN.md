---
phase: 04-agent-upgrade
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - services/agent.py
  - tests/test_agent_degraded.py
autonomous: true
requirements:
  - AGNT-04
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - Simulated tool outage mid-chat yields a still-200 graceful degraded message, not a 500 (per D-07, tool-data path only)
    - Degraded messages name the failed piece (current vs forecast) and state what still works (per D-08)
    - LLM-outage fail-loudly 502 behavior from Phase 1 is unchanged
    - Prompt and curated content contain no secrets
  artifacts:
    - services/agent.py (degraded-mode prompt rules)
    - tests/test_agent_degraded.py (named-gap degradation + secret-safety tests)
  key_links:
    - Degraded prompt rules compose with existing fallback/partial tool payloads
    - Phase 1 Alert regex and LLM 502 path still pass unmodified
---

<objective>
Graceful named-gap degradation for tool outages plus secret-safety of all prompt content.

Purpose: Delivers AGNT-04 — the agent degrades honestly when weather data fails while the LLM-failure path keeps failing loudly, and proves no secrets leak into prompts or replies.
Output: Degraded-mode SYSTEM_PROMPT rules and a passing degradation plus secret-safety test module.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-upgrade/04-CONTEXT.md
@services/agent.py
@tools/weather.py
@tests/test_error_contract.py
</context>

<tasks>

<task type="auto">
  <name>Degraded-mode prompt rules plus named-gap tests</name>
  <files>services/agent.py, tests/test_agent_degraded.py</files>
  <read_first>services/agent.py (SYSTEM_PROMPT rules from Plans 01-02, process_chat retry loop that must stay fail-loudly); tools/weather.py (_fallback_payload note text, _partial_forecast_payload forecast-unavailable note, stale markers); tests/test_error_contract.py (test_llm_outage_yields_502_with_retry — the behavior that must keep passing)</read_first>
  <action>Append degraded-mode rules to SYSTEM_PROMPT in numbered-imperative style per D-07/D-08: when tool JSON carries fallback, stale, or forecast-unavailable markers, answer with still-200 graceful text naming the failed piece (current conditions vs forecast) and stating what still works (the other tool path, cached values, curated advice); never convert tool-data gaps into refusals or 500s; never soften the LLM-outage path (retry once then fail loudly stays as implemented in process_chat). Reuse the FakeExecutor harness (patch services.agent._get_executor) in tests/test_agent_degraded.py with at least three tests: fallback-marked current JSON yields a reply naming current conditions as unavailable and quoting what still works; forecast-unavailable partial JSON yields a reply naming forecast as unavailable plus current values; a forced executor exception still raises RuntimeError so the route keeps returning 502 (assert pytest.raises(RuntimeError), not a 200).</action>
  <verify>
    <automated>python -m pytest tests/test_agent_degraded.py tests/test_error_contract.py -q</automated>
    <fails_when>pytest exits non-zero or the LLM-outage 502 test regresses to a 200 or a different status</fails_when>
  </verify>
  <acceptance_criteria>SYSTEM_PROMPT contains named-gap rules distinguishing current vs forecast failure; degraded tests prove the reply names the failed piece and states what still works; executor-exception test proves RuntimeError propagates; python -m pytest tests/test_agent_degraded.py tests/test_error_contract.py -q passes.</acceptance_criteria>
  <done>Tool-outage degradation is graceful and names gaps while LLM failure still fails loudly.</done>
</task>

<task type="auto">
  <name>Secret-safety plus Alert-regex plus full-suite gate</name>
  <files>tests/test_agent_degraded.py</files>
  <read_first>services/agent.py (full SYSTEM_PROMPT text to scan); services/agri_advisories.py (curated content to scan); tests/test_secrets.py (existing secret-safety assertion pattern)</read_first>
  <action>Append gate tests to tests/test_agent_degraded.py: secret-safety test asserting SYSTEM_PROMPT and the curated advisory module contain no sk-or, sk-test, OPENROUTER_API_KEY value, or WEATHER_API key material (read the live settings values via get_settings and assert absence, mirroring tests/test_secrets.py); Alert-regex test asserting every Alert line the upgraded prompt can emit (single-day and worst-day with weekday parens) matches the _derive_alert_level regex by calling services.agent._derive_alert_level on sample replies. Finish with the full proven suite python -m pytest tests/ -q and report the count.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>pytest exits non-zero or any pre-existing suite file regresses versus the pre-phase baseline</fails_when>
  </verify>
  <acceptance_criteria>Secret-safety test reads live settings values and proves their absence from prompt and curated content; Alert-regex test covers both single-day and worst-day formats via _derive_alert_level; python -m pytest tests/ -q passes with zero failures.</acceptance_criteria>
  <done>Prompt content proven secret-free, Alert format proven intact, full suite green with pass-plus-report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| degraded reply→user | Partial/fallback data crosses into user-visible replies |
| prompt content→repo | Static prompt text must never carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Information Disclosure | Degraded replies | medium | mitigate | Rules name the failed piece using tool stale/fallback markers; sanitized payloads only, no tracebacks |
| T-04-06 | Information Disclosure | Prompt/curated content | high | mitigate | Secret-safety test asserts live key values absent from SYSTEM_PROMPT and agri module |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new runtime deps; no install step |
</threat_model>

<verification>
Degradation green plus LLM 502 intact: python -m pytest tests/test_agent_degraded.py tests/test_error_contract.py -q passes; whole repo: python -m pytest tests/ -q passes with reported count.
</verification>

<success_criteria>
Tool outage yields still-200 named-gap messages per D-07/D-08; LLM outage still 502 per carryover; prompt content secret-free; Alert regex intact; full suite green.
</success_criteria>

## Artifacts this phase produces

- services/agent.py — degraded-mode SYSTEM_PROMPT rules for named-gap graceful messages (extends Plan 02)
- tests/test_agent_degraded.py — named-gap degradation tests, LLM fail-loudly guard, secret-safety test, Alert-regex test, full-suite gate (NEW from this plan)

<output>
Create `.planning/phases/04-agent-upgrade/04-03-SUMMARY.md` when done
</output>
