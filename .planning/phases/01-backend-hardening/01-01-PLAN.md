---
phase: 01-backend-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pytest.ini
  - tests/conftest.py
  - tests/test_chat_api.py
  - tests/test_weather_tool.py
  - api/routes.py
autonomous: true
requirements: [BACK-01, BACK-04]

estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - POST /api/chat with the agent mocked returns 200 with reply and alert_level for "Hi in Mumbai"
    - GET /health returns status ok with app and version keys
    - pytest passes on tests/test_chat_api.py and tests/test_weather_tool.py
    - The mock tool returns parseable JSON carrying location, temperature_c, and alert_level
  artifacts:
    - requirements.txt (pytest, pytest-asyncio, httpx appended)
    - pytest.ini (asyncio_mode=auto)
    - tests/conftest.py
    - tests/test_chat_api.py
    - tests/test_weather_tool.py
    - api/routes.py (threadpool offload)
  key_links:
    - route calls process_chat via the api.routes namespace (the mock seam)
    - httpx ASGITransport bound to the app imported from main
---

<objective>
Prove the thin end-to-end slice HTTP to route to agent to tool to response with a green pytest run, per D-01/D-03/D-13/D-16. This is the Phase 1 tracer: one mocked path only, production quality, everything later builds on it.

Purpose: Catch architectural dead-ends (import cycles, event-loop blocking, seam errors) on the first commit while context is fresh.
Output: Passing tracer tests plus the threadpool fix that makes the async route safe.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-hardening/01-CONTEXT.md
@main.py
@api/routes.py
@services/agent.py
@schemas/chat.py
@tools/weather.py
@core/config.py
@requirements.txt
</context>

<tasks>

<task type="tracer">
  <name>Tracer: mocked POST /api/chat end-to-end plus test scaffold</name>
  <files>requirements.txt, pytest.ini, tests/conftest.py, tests/test_chat_api.py, api/routes.py</files>
  <read_first>api/routes.py (current sync process_chat call inside async handler plus 502/500 mapping to preserve); services/agent.py (process_chat signature for the fake's return shape); main.py (app object the ASGI transport binds to); core/config.py (get_settings cache behavior the fixture must reset)</read_first>
  <action>Append three version-floored lines to requirements.txt without touching existing lines: pytest, pytest-asyncio, httpx. Create pytest.ini at repo root selecting asyncio_mode auto and testpaths tests, per D-16. Create tests/conftest.py that inserts the repo root on sys.path and provides an autouse fixture setting env OPENROUTER_API_KEY to the literal dummy value test-dummy-key, then clearing the get_settings cache and the agent executor cache before and after each test. In api/routes.py import run_in_threadpool from starlette.concurrency and await it around the existing process_chat call with the same message and location keyword arguments, leaving every except branch byte-identical, per D-13 and D-05. Create tests/test_chat_api.py using httpx AsyncClient over ASGITransport bound to the main app: patch the process_chat name inside the api.routes namespace (the from-import binding means patching the services.agent attribute alone has no effect on the route), per D-01 intent of keeping the LLM out, with a fake returning a fixed reply carrying an Alert Green line and alert_level Green; POST message Hi in Mumbai with location Mumbai and assert 200 plus both response keys; GET /health and assert status ok plus app and version keys present. Then run pip install against requirements.txt followed by the focused pytest file, since the system env lacks pydantic.</action>
  <verify>
    <automated>pip install -r requirements.txt; if ($?) { python -m pytest tests/test_chat_api.py -q }</automated>
    <fails_when>pip exits non-zero, or pytest exits non-zero, or its output reports any failed or error test</fails_when>
  </verify>
  <done>Focused tracer file passes; mocked POST returns 200 with reply and alert_level; /health returns liveness shape</done>
  <acceptance_criteria>POST /api/chat with message Hi in Mumbai returns HTTP 200 with a reply containing the mocked agent text and alert_level Green; GET /health returns HTTP 200 with status ok; command python -m pytest tests/test_chat_api.py -q exits 0 with 0 failed</acceptance_criteria>
  <reversibility rating="reversible">Threadpool offload changes call mechanics only; reverting is a single-hunk restore with no data migration.</reversibility>
</task>

<task type="auto">
  <name>Mock tool JSON contract test</name>
  <files>tests/test_weather_tool.py</files>
  <read_first>tools/weather.py (mock payload keys and tool decorator); tests/conftest.py (dummy-key fixture this test inherits)</read_first>
  <action>Cover the frozen tool contract without touching tools/weather.py: invoke the get_current_weather tool through its invoke entrypoint with a Mumbai location dict, parse the returned string with json.loads, and assert the location echo plus temperature_c, alert_level, and source keys; assert the tool name is get_current_weather so Phase 2 keeps the signature. Run the full tests directory afterwards.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>pytest exits non-zero or reports any failed, error, or collection error</fails_when>
  </verify>
  <done>Full suite green including the tool contract file</done>
  <acceptance_criteria>tests/test_weather_tool.py exists and asserts parseable JSON with location echo Mumbai plus temperature_c and alert_level keys; command python -m pytest tests/ -q exits 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner to PyPI | pip install pulls external packages into the build env |
| test to app | mocked seam replaces the LLM so no secret or network is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pip installs (pytest, pytest-asyncio, httpx) | medium | mitigate | Install only the three official PyPI names with version floors in requirements.txt; verify each name on pypi.org before install; no other packages added in this plan |
| T-01-01 | Information Disclosure | tests/conftest.py dummy key | low | mitigate | Fixture forces OPENROUTER_API_KEY to a dummy value so no real secret is ever read during tests |
</threat_model>

<verification>
Full command python -m pytest tests/ -q exits 0; mocked POST returns 200 with both keys; /health returns the three liveness keys.
</verification>

<success_criteria>
Tracer slice committed with green tests; route no longer blocks the event loop; ROADMAP Phase 1 success criterion 1 (mocked chat) and 4 (pytest baseline) are demonstrably started.
</success_criteria>

<output>
Create `.planning/phases/01-backend-hardening/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Full phase artifact map (this plan delivers the rows marked 01-01):

| Artifact | Type | Delivered by |
|----------|------|--------------|
| pytest.ini (asyncio_mode auto) | file | 01-01 |
| tests/conftest.py (dummy-key fixture, cache resets) | file | 01-01 |
| tests/test_chat_api.py (mocked route tests) | file | 01-01 |
| tests/test_weather_tool.py (tool contract) | file | 01-01 |
| tests/test_error_contract.py (502/500/422 paths) | file | 01-02 |
| tests/test_observability.py (request-id, health, CORS) | file | 01-03 |
| tests/test_secrets.py (key redaction) | file | 01-03 |
| tests/test_live_openrouter.py (opt-in skipped live test) | file | 01-03 |
| COVERAGE.md (no-new-API declaration) | file | 01-03 |
| api/routes.py request threadpool + sanitize helper | symbol | 01-01 + 01-02 |
| services/agent.py LLM timeout/retry constants | symbol | 01-02 |
| main.py request-id middleware + INFO logging | symbol | 01-03 |
