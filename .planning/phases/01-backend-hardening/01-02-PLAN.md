---
phase: 01-backend-hardening
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - services/agent.py
  - api/routes.py
  - tests/test_error_contract.py
autonomous: true
requirements: [BACK-01, BACK-03]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Missing OPENROUTER_API_KEY yields a clear 502 with a safe detail and no traceback leak
    - An LLM failure retries once after a 30s-bounded call, then surfaces an honest 502
    - Unexpected errors surface a generic 500 that never contains key material or internals
    - Oversize, empty, or overlong-location payloads are rejected with 422; valid limits still pass
  artifacts:
    - services/agent.py (timeout constant, single retry, full-traceback server log)
    - api/routes.py (sanitized details, traceback logging, preserved 502/500 mapping)
    - tests/test_error_contract.py
  key_links:
    - agent RuntimeError maps to route 502; any other exception maps to generic 500
    - every error detail passes through the sanitize helper before reaching the client
---

<objective>
Harden the error contract and the agent call: 30s timeout with 1 retry per D-11, preserved 502/500 mapping per D-05, loud failures per D-06, log-full/send-safe per D-07, key redaction per D-17, and input-limit enforcement per D-15.

Purpose: The tracer proved the happy path; this plan proves every sad path the ROADMAP success criteria demand.
Output: Timeout/retry in the agent layer, sanitized error responses, and contract tests for all error shapes.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-hardening/01-CONTEXT.md
@.planning/phases/01-backend-hardening/01-01-SUMMARY.md
@api/routes.py
@services/agent.py
@schemas/chat.py
@core/config.py
</context>

<tasks>

<task type="auto">
  <name>Agent LLM timeout plus single retry</name>
  <files>services/agent.py</files>
  <read_first>services/agent.py (ChatOpenAI construction in _build_llm and executor.invoke in process_chat); core/config.py (validate_secrets message to preserve verbatim)</read_first>
  <action>Add two module constants for a 30-second LLM bound and a 2-attempt budget (initial call plus exactly one retry), per D-11. Pass the 30-second bound as the request timeout when constructing ChatOpenAI in _build_llm. In process_chat wrap executor.invoke in a two-iteration loop that returns on success, logs the full exception server-side with the module logger on each failure, and after the final attempt raises RuntimeError with the existing WeatherGPT agent invocation failed prefix, per D-06 and D-07. Keep _derive_alert_level, the system prompt, tool list, and cache helpers untouched; do not add any degraded-200 fallback.</action>
  <verify>
    <automated>python -m pytest tests/test_chat_api.py tests/test_weather_tool.py -q</automated>
    <fails_when>pytest exits non-zero or reports any failed or error test, showing the agent edit broke the tracer seam</fails_when>
  </verify>
  <done>Agent layer bounds every LLM call at 30s, retries once, then raises loudly; tracer tests still green</done>
  <acceptance_criteria>services/agent.py defines a 30-second timeout constant and a 2-attempt budget, constructs the LLM with the timeout applied, and process_chat raises RuntimeError after the retry is exhausted; prior tracer tests still exit 0</acceptance_criteria>
  <reversibility rating="reversible">Timeout and retry values are plain constants; restoring prior call behavior is a single edit with no migration.</reversibility>
</task>

<task type="auto">
  <name>Route error responses: sanitize plus server-side tracebacks</name>
  <files>api/routes.py</files>
  <read_first>api/routes.py (state left by plan 01-01: threadpool call plus three except branches); core/config.py (settings keys whose values must never leak)</read_first>
  <action>Add a module-level sanitize helper in api/routes.py that replaces every occurrence of the configured OpenRouter and weather key values with a redaction marker before a detail string leaves the server, per D-17. Call get_settings inside the request handler (not at import) so tests can monkeypatch keys. In each except branch log the full traceback server-side with logger.exception including the request path, then raise the same status codes as today: 422 for ValueError, 502 for RuntimeError, generic 500 with a fixed Unexpected error prefix for anything else, per D-05 and D-07. Keep the threadpool call from plan 01-01 untouched and add no new status codes, per D-05.</action>
  <verify>
    <automated>python -m pytest tests/test_chat_api.py tests/test_weather_tool.py -q</automated>
    <fails_when>pytest exits non-zero or reports any failed or error test, showing the route edit regressed the tracer</fails_when>
  </verify>
  <done>Every error detail is sanitized, every traceback is logged server-side, status mapping is unchanged</done>
  <acceptance_criteria>api/routes.py contains a sanitize helper consulted on all three error branches, logger.exception appears in each branch, and the 422/502/500 status codes are unchanged from the pre-plan mapping</acceptance_criteria>
</task>

<task type="auto">
  <name>Error-contract and validation tests</name>
  <files>tests/test_error_contract.py</files>
  <read_first>tests/conftest.py (dummy-key fixture and cache-reset pattern to reuse); api/routes.py (sanitize helper and branch behavior to trigger); schemas/chat.py (message 1-2000 and location 120 limits to exercise); services/agent.py (retry loop to force-fail deterministically)</read_first>
  <action>Create flat-layout tests/test_error_contract.py, per D-03, covering five behaviors with the LLM kept out: missing key yields 502 by clearing env keys, clearing both caches, and calling the real process_chat path so validate_secrets fires; LLM outage yields 502 by patching the executor invoke entry to raise and asserting the client detail carries the invocation-failed prefix with status 502 and never 200, per D-06; unexpected failure yields 500 by patching the route-namespace process_chat to raise a generic exception and asserting the fixed Unexpected prefix; schema limits yield 422 for an empty message, a 2001-char message, and a 121-char location while a whitespace-padded valid message still returns 200, per D-15; every error body in this file is asserted free of raw exception module paths. Reuse the conftest cache-clear pattern and the route-namespace patch target established in plan 01-01.</action>
  <verify>
    <automated>python -m pytest tests/test_error_contract.py -q</automated>
    <fails_when>pytest exits non-zero or reports any failed, error, or collection error</fails_when>
  </verify>
  <done>502, 500, and 422 paths all proven with no traceback or key leak in any client body</done>
  <acceptance_criteria>tests/test_error_contract.py contains at least five test functions spanning missing-key 502, LLM-failure 502, unexpected-error 500, schema-limit 422s, and padded-message acceptance; command python -m pytest tests/test_error_contract.py -q exits 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent to OpenRouter | 30s-bounded, retried-once upstream call that can fail or hang |
| route to client | error details cross to untrusted clients and must carry no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | api/routes.py error details | medium | mitigate | Sanitize helper strips both configured key values on every branch; tests assert bodies carry only safe prefixes |
| T-02-02 | Denial of Service | services/agent.py LLM call | medium | mitigate | 30s request timeout plus exactly one retry then loud 502, so a hung upstream never holds a worker forever |
| T-02-03 | Spoofing | ChatRequest inputs | low | accept | Per D-15 only length limits apply in Phase 1; prompt-injection defenses are explicitly out of scope |
</threat_model>

<verification>
Command python -m pytest tests/ -q exits 0 across tracer, tool, and error-contract files; missing key and forced LLM failure each return 502; generic failure returns 500; limit violations return 422.
</verification>

<success_criteria>
ROADMAP Phase 1 success criterion 3 (clear 502, no traceback leak) is proven by test; error mapping matches D-05/D-06 exactly.
</success_criteria>

<output>
Create `.planning/phases/01-backend-hardening/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan delivers the error-contract rows of the phase artifact map in 01-01-PLAN.md: services/agent.py timeout/retry constants, the api/routes.py sanitize helper with traceback logging, and tests/test_error_contract.py.
