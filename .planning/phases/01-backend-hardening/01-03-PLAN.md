---
phase: 01-backend-hardening
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - main.py
  - tests/test_observability.py
  - tests/test_secrets.py
  - tests/test_live_openrouter.py
  - .planning/phases/01-backend-hardening/COVERAGE.md
autonomous: true
requirements: [BACK-02, BACK-03, BACK-04]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Every response carries X-Request-ID, echoing a valid incoming value or a generated UUID
    - Server logs are human-readable INFO lines carrying the request id; /health returns only status, app, version
    - CORS still allows the frontend origin permissively for local dev
    - No API key material appears in any log or response; the live test skips unless explicitly enabled
    - Full pytest suite passes with a coverage report and no hard gate
  artifacts:
    - main.py (request-id middleware plus INFO logging)
    - tests/test_observability.py
    - tests/test_secrets.py
    - tests/test_live_openrouter.py
    - .planning/phases/01-backend-hardening/COVERAGE.md
  key_links:
    - request-id middleware wraps outside CORS so error responses are stamped too
    - per-request log line ties method, path, status, and request id together
---

<objective>
Add observability and secret safety plus the final suite gate: stdlib INFO logging per D-08, X-Request-ID middleware per D-09, liveness-only health per D-10, open CORS confirmation per D-12, redaction test per D-17, opt-in live test per D-04, and the pass-plus-report gate per D-02.

Purpose: Make the baseline operable (traceable requests, readable logs) and provably secret-safe without changing any frozen contract.
Output: Middleware plus logging in main.py, three new test files, COVERAGE.md, and a green full-suite report.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-hardening/01-CONTEXT.md
@.planning/phases/01-backend-hardening/01-01-SUMMARY.md
@main.py
@api/routes.py
@core/config.py
</context>

<tasks>

<task type="auto">
  <name>Request-id middleware plus startup logging</name>
  <files>main.py</files>
  <read_first>main.py (FastAPI app, CORSMiddleware registration, health and root routes to leave untouched)</read_first>
  <action>Configure stdlib logging at INFO with a human-readable timestamp, level, logger-name, and message format exactly once at startup, guarded so reimports do not duplicate handlers, per D-08. Add an HTTP middleware registered after the existing CORSMiddleware call so it executes outermost and stamps every response including errors: accept an incoming X-Request-ID only when it is 64 chars or fewer of letters, digits, and hyphens, otherwise generate a fresh UUID4; log one INFO line per request with method, path, status, and id; set the id on the response header, per D-09. Leave allow_origins open, per D-12, and leave the health payload exactly as status, app, version with no dependency checks, per D-10.</action>
  <verify>
    <automated>python -m pytest tests/test_chat_api.py tests/test_weather_tool.py -q</automated>
    <fails_when>pytest exits non-zero or reports any failed or error test, showing the main.py edit regressed the tracer</fails_when>
  </verify>
  <done>Every response carries a valid X-Request-ID; startup logs one INFO line per request; CORS and health shapes unchanged</done>
  <acceptance_criteria>main.py configures INFO logging once, defines request-id middleware registered after CORS, echoes valid incoming ids and generates UUID4 otherwise, and the /health payload still contains exactly status, app, and version</acceptance_criteria>
  <reversibility rating="reversible">Middleware and logging are additive and header-only; removal restores prior responses byte-for-byte.</reversibility>
</task>

<task type="auto">
  <name>Observability, secret-safety, and opt-in live tests</name>
  <files>tests/test_observability.py, tests/test_secrets.py, tests/test_live_openrouter.py</files>
  <read_first>tests/conftest.py (fixture and ASGI-client pattern to mirror); main.py (middleware header name and health shape to assert); api/routes.py (sanitize helper the secret test exercises)</read_first>
  <action>Create three flat-layout files, per D-03. In test_observability.py assert an incoming request id is echoed verbatim on both the chat and health responses, assert a fresh UUID-shaped id appears when the header is absent, assert the health body key set equals exactly the three liveness keys, and assert a request carrying an Origin header receives a wildcard allow-origin response, per D-09, D-10, and D-12. In test_secrets.py set both key env vars to sentinel marker values, clear both caches, force an LLM failure through the real agent path, and assert neither sentinel appears in the response body nor in captured log output, per D-17. In test_live_openrouter.py add a single test gated to skip unless env WEATHERGPT_LIVE equals 1, calling the real agent once with a short benign query, per D-04, so the default suite never touches the network.</action>
  <verify>
    <automated>python -m pytest tests/test_observability.py tests/test_secrets.py tests/test_live_openrouter.py -q</automated>
    <fails_when>pytest exits non-zero, reports any failed or error test, or the live test runs without the env flag instead of skipping</fails_when>
  </verify>
  <done>Request-id, health, CORS, redaction, and skip-by-default live behaviors all proven</done>
  <acceptance_criteria>All three files exist in flat tests layout; the live file reports 1 skipped with the flag unset; sentinel key values appear in neither responses nor logs; command python -m pytest tests/test_observability.py tests/test_secrets.py tests/test_live_openrouter.py -q exits 0</acceptance_criteria>
</task>

<task type="auto">
  <name>Coverage declaration plus full-suite pass-and-report gate</name>
  <files>.planning/phases/01-backend-hardening/COVERAGE.md</files>
  <read_first>.planning/phases/01-backend-hardening/01-CONTEXT.md (phase boundary: mock tool only, IMD work is Phase 2)</read_first>
  <action>Write COVERAGE.md containing the reasoned declaration that Phase 1 hardens the existing FastAPI baseline against the mock tool and touches no new external service, so no API matrix applies; note the pre-existing OpenRouter integration is untouched and IMD arrives in Phase 2. Then run the full suite with a coverage report against the app packages, per the D-02 pass-plus-report gate with no hard threshold, and confirm green.</action>
  <verify>
    <automated>python -m pytest tests/ -q --cov=. --cov-report=term-missing</automated>
    <fails_when>pytest exits non-zero, reports any failed or error test, or the coverage report fails to render</fails_when>
  </verify>
  <done>COVERAGE.md exists with the no-new-API declaration; full suite green with coverage reported</done>
  <acceptance_criteria>File .planning/phases/01-backend-hardening/COVERAGE.md declares no external API integration for Phase 1 with IMD deferred to Phase 2; command python -m pytest tests/ -q --cov=. --cov-report=term-missing exits 0</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to middleware | incoming X-Request-ID is untrusted input reflected into headers and logs |
| suite to OpenRouter | opt-in live test crosses to a real external LLM only when flagged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | X-Request-ID echo in main.py | low | mitigate | Echo only values matching letters, digits, hyphens up to 64 chars; anything else is replaced with a server-generated UUID4 |
| T-03-02 | Repudiation | per-request logs | low | mitigate | Middleware emits one INFO line per request tying method, path, status, and request id |
| T-03-03 | Information Disclosure | opt-in live test | low | mitigate | Test skips unless WEATHERGPT_LIVE equals 1, per D-04, so CI never sends keyed traffic |
| T-03-04 | Information Disclosure | permissive CORS | low | accept | Per D-12 open origins stay for local dev; tightening moves to Phase 7 behind an env var |
</threat_model>

<verification>
Full command python -m pytest tests/ -q exits 0 with the live test skipped by default; every response carries X-Request-ID; no sentinel key material in logs or bodies; coverage report renders.
</verification>

<success_criteria>
ROADMAP Phase 1 success criteria 2 (health plus CORS) and 4 (pytest with coverage reported) are proven; the phase is demoable end-to-end on the mock tool.
</success_criteria>

<output>
Create `.planning/phases/01-backend-hardening/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan delivers the observability rows of the phase artifact map in 01-01-PLAN.md: the main.py request-id middleware with INFO logging, tests/test_observability.py, tests/test_secrets.py, tests/test_live_openrouter.py, and COVERAGE.md.
