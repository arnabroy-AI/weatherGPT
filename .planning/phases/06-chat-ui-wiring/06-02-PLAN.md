---
phase: 06-chat-ui-wiring
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/components/chat/alert-badge.tsx
  - frontend/components/chat/message-bubble.tsx
  - frontend/components/chat/starters.tsx
  - frontend/components/chat/location-bar.tsx
  - frontend/components/chat/chat-states.tsx
  - frontend/app/chat/page.tsx
  - frontend/lib/chat-client.ts
autonomous: true
requirements:
  - FRNT-03
  - FRNT-04
estimate:
  tokens: 50000
  raw_tokens: 33000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - Every assistant reply shows the correct Green/Yellow/Orange/Red badge byte-matching alerts-showcase, and Orange/Red bubbles get the 2px level-color left border
    - First visit shows the empty state with 3 clickable byte-match starters; loading, error, and 50-exchange trim states all render per copy contract
    - Location bar value feeds every request and persists across sends in-session
    - Conversation history is in-memory only with a 50-exchange cap and no persistence
  artifacts:
    - frontend/components/chat/alert-badge.tsx
    - frontend/components/chat/message-bubble.tsx
    - frontend/components/chat/starters.tsx
    - frontend/components/chat/location-bar.tsx
    - frontend/components/chat/chat-states.tsx
  key_links:
    - page state -> location-bar value -> sendChatMessage location on every send
    - assistant message alert_level -> alert-badge -> Badge with showcase classes
    - starter click -> sends exact seeded string as user message
---

<objective>
Expansion: badges, bubbles, starters, location bar, history cap, and empty/loading/error states per D-03, D-04, D-05.

Purpose: Deliver the full conversational UX on top of the proven Plan 01 slice — data-color badges, distinct bubbles, seeded entry points, and state coverage.
Output: Five chat components wired into page.tsx with contract tests extended and tsc plus build green.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-ui-wiring/06-CONTEXT.md
@.planning/phases/06-chat-ui-wiring/06-UI-SPEC.md
@.planning/phases/06-chat-ui-wiring/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alert badges plus message bubbles plus starters</name>
  <files>frontend/components/chat/alert-badge.tsx, frontend/components/chat/message-bubble.tsx, frontend/components/chat/starters.tsx</files>
  <read_first>frontend/components/alerts-showcase.tsx (exact badgeClass strings for all four levels); frontend/components/live-demo-teaser.tsx (exact SEEDED query strings and honesty note); .planning/phases/06-chat-ui-wiring/06-UI-SPEC.md (Badge system table, Message/Bubble System table, Copywriting Contract starters and loading copy)</read_first>
  <action>Build three components per D-04 and D-05. Alert-badge exports ALERT_BADGE_CLASSES mapping Green Yellow Orange Red to the exact class strings from alerts-showcase and an AlertBadge component rendering shadcn Badge variant outline with visible text Alert plus level name and aria-label mirroring visible text, rendering nothing when level is missing or unknown. Message-bubble exports UserBubble rendering right-aligned max 85 percent solid teal-700 white text (dark teal-400 slate-950) rounded-2xl rounded-br-md with Body 16px and muted right-aligned timestamp, and AssistantBubble rendering left-aligned max 100 percent glass-card with reply text via text node plus whitespace-pre-wrap, AlertBadge under text when level is known, 2px left level-color border only for Orange and Red, and verbatim Alert line preserved in reply text. Starters exports STARTER_QUERIES with exactly Current weather in Pune, Mumbai this weekend, Paddy sowing advice for Nashik plus a Starters component rendering three Button outline chips that call onSelect with the exact string on click with 44px minimum targets, plus the source note Sample starters — live answers quote measured values and name their source. Never use badge classes on interactive elements, never dangerouslySetInnerHTML, never truncate long replies.</action>
  <acceptance_criteria>alert-badge.tsx exports ALERT_BADGE_CLASSES with all four levels whose class strings equal the alerts-showcase strings light plus dark; message-bubble.tsx renders user teal and assistant glass-card with conditional left border and whitespace-pre-wrap and zero dangerouslySetInnerHTML; starters.tsx contains the three exact seeded strings and source note; npx tsc --noEmit passes</acceptance_criteria>
  <verify>
    <automated>npx --prefix frontend tsc --noEmit</automated>
    <fails_when>tsc prints any error lines or exits nonzero — component types are broken</fails_when>
  </verify>
  <done>Badges byte-match the showcase, bubbles are visibly distinct, starters send byte-match strings.</done>
</task>

<task type="auto">
  <name>Task 2: Location bar plus history cap plus chat states wired into page</name>
  <files>frontend/components/chat/location-bar.tsx, frontend/components/chat/chat-states.tsx, frontend/app/chat/page.tsx, frontend/lib/chat-client.ts</files>
  <read_first>frontend/app/chat/page.tsx (Plan 01 tracer shell to extend); frontend/lib/chat-client.ts (sendChatMessage signature to reuse); .planning/phases/06-chat-ui-wiring/06-UI-SPEC.md (Chat Layout location bar, History cap 50 exchanges, Copywriting Contract empty/error/loading copy, Fetch Contract error-to-state mapping)</read_first>
  <action>Build location-bar exporting LocationBar with visible label Location htmlFor plus amber MapPin icon plus native input placeholder Location (optional) — e.g. Pune with h-11 rounded 10px glass fill plus hint Used for every question you send, controlled value persisted in page state across sends. Build chat-states exporting EmptyState with heading Start with a weather question plus body Ask in plain words — WeatherGPT grounds every answer and labels it Green to Red. Pick a starter below or type your own. plus Starters, LoadingBubble with role status plus text Getting your answer… plus skeleton bars, ErrorCard with role alert plus copy Couldn't get that answer. Check your connection and try again — nothing you typed was lost. plus per-message Retry, and TrimNotice with text Older messages were trimmed to keep this session light. Rewire page.tsx per D-03 and D-04: keep Header and H1 and role log list, replace inline location input with LocationBar, render EmptyState plus Starters when zero messages, cap history at 50 exchanges (100 bubbles) in in-memory React state only with oldest pair dropped plus one center TrimNotice and no localStorage, keep location value in composer after backend clarification replies, abort previous in-flight request before a new send, disable Send while in-flight, map 422 to inline ErrorCard with That message couldn't be sent. Try shorter wording. and 502/500 to ErrorCard quoting detail verbatim as text with Retry, keep typed message preserved on error, extend the node --test contract file with badge-class equality and starter-string equality and history-cap assertions.</action>
  <acceptance_criteria>LocationBar label, placeholder, and hint match spec and every send passes current bar value with empty string when blank; EmptyState, LoadingBubble, ErrorCard, TrimNotice copy matches contract; history capped at 50 exchanges in-memory with no localStorage reference in chat code; extended node --test passes; temp-dir production build has zero errors</acceptance_criteria>
  <verify>
    <automated>node --test frontend/lib/__tests__/chat-contract.test.mjs</automated>
    <fails_when>node --test reports any failing assertion or 0 passing — state wiring regressed the contract</fails_when>
  </verify>
  <done>Location feeds every request, all states render per copy, history trims at 50 exchanges in-memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Location string and message cross per send; badge level comes from backend and is display-only |
| backend detail→DOM | 422/502/500 detail strings render inside ErrorCard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Information Disclosure | ErrorCard detail quote | medium | mitigate | Render detail via text nodes only; never as HTML; keep prefix copy static from contract |
| T-06-04 | Tampering | location input | low | mitigate | Pass through as opaque string; no client-side geocoding or parsing; max length enforced server-side |
| T-06-SC | Tampering | npm installs | high | mitigate | No new packages in this plan; reuse installed shadcn plus lucide-react only |
</threat_model>

<verification>
Extended contract tests pass via node --test; npx tsc --noEmit passes in place; production build passes via temp-dir robocopy copy with zero errors; grep confirms badge classes equal alerts-showcase and starter strings equal live-demo-teaser.
</verification>

<success_criteria>
Badges match showcase byte-for-byte with level-name text always present; starters byte-match teaser strings; empty/loading/error/trim states render per copy; location feeds every request; history is in-memory capped at 50 exchanges.
</success_criteria>

## Artifacts this phase produces

- frontend/components/chat/alert-badge.tsx — data-color badges Green/Yellow/Orange/Red (D-04)
- frontend/components/chat/message-bubble.tsx — distinct user/assistant bubbles with Orange/Red left border
- frontend/components/chat/starters.tsx — 3 byte-match seeded starter chips (D-05)
- frontend/components/chat/location-bar.tsx — session location input feeding every request (D-03)
- frontend/components/chat/chat-states.tsx — empty, loading, error, trim states per copy contract (D-05)

<output>
Create `.planning/phases/06-chat-ui-wiring/06-02-SUMMARY.md` when done
</output>
