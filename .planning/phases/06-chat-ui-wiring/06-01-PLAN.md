---
phase: 06-chat-ui-wiring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/chat-client.ts
  - frontend/app/chat/page.tsx
  - frontend/lib/__tests__/chat-contract.test.mjs
autonomous: true
requirements:
  - FRNT-03
  - FRNT-04
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - User opens /chat and sees a Chat header, location input, empty conversation area, and a working composer
    - User types a question, hits Send, and sees their bubble plus one assistant reply from a mocked fetch round-trip
    - Request body is exactly {message, location} and response shape {reply, alert_level} matching schemas/chat.py
    - Typecheck and production build pass with zero errors
  artifacts:
    - frontend/lib/chat-client.ts
    - frontend/app/chat/page.tsx
    - frontend/lib/__tests__/chat-contract.test.mjs
  key_links:
    - composer -> chat-client.sendChatMessage -> POST {base}/api/chat with Content-Type application/json
    - NEXT_PUBLIC_WEATHERGPT_API env -> getApiBaseUrl -> fetch base URL
    - contract test asserts frontend shapes byte-match backend schemas/chat.py and api/routes.py
---

<objective>
Tracer: production-quality /chat route shell plus composer plus one mocked send/reply round-trip (per D-01, D-02, D-03). This is the vertical slice all expansion builds on.

Purpose: Prove the architecture end-to-end (route renders, client posts correct shape, reply renders) before badges, history, starters, and panels land.
Output: Working /chat page, fetch client, and contract test with tsc plus build green.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-ui-wiring/06-CONTEXT.md
@.planning/phases/06-chat-ui-wiring/06-UI-SPEC.md
@.planning/phases/05-frontend-shell-landing/05-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end /chat shell plus composer plus one mocked round-trip</name>
  <files>frontend/lib/chat-client.ts, frontend/app/chat/page.tsx</files>
  <read_first>frontend/.env.local.example (NEXT_PUBLIC_WEATHERGPT_API value); schemas/chat.py (ChatRequest message/location, ChatResponse reply/alert_level); api/routes.py (POST /chat, 422/502/500 with detail); frontend/app/page.tsx (Header plus main id main-content pattern); frontend/app/globals.css (glass-card, glass-header tokens)</read_first>
  <action>Build the tracer per D-01, D-02, D-03. Create frontend/lib/chat-client.ts as a client module exporting getApiBaseUrl reading process.env.NEXT_PUBLIC_WEATHERGPT_API trimmed with empty-string fallback, CHAT_TIMEOUT_MS constant 10000, AlertLevel union Green Yellow Orange Red, sendChatMessage using direct client fetch POST to base plus /api/chat with Content-Type application/json and body message plus location where location defaults to empty string never null or undefined, AbortController timeout aborting at 10000ms, single JSON response parsing returning reply plus alert_level with no EventSource or streaming reader anywhere. Create frontend/app/chat/page.tsx as a use client page reusing Header, rendering main with aria-labelledby on an H1 Chat with tabIndex minus 1, a Separator, a location bar with visible label Location htmlFor plus amber MapPin icon plus native input placeholder Location (optional) — e.g. Pune plus hint Used for every question you send, a message list region with role log aria-live polite aria-label Conversation, and a composer with textarea aria-label Type your weather question plus Send Button teal fill with aria-label Send message that disables while in-flight. Wire send flow so the user bubble renders immediately, sendChatMessage is called with current message and current location bar value, assistant reply renders as text node with whitespace-pre-wrap and never dangerouslySetInnerHTML, and fetch errors surface as a minimal inline error Card with Retry re-sending identical payload (full unreachable panel arrives in Plan 03). Keep bubble styling minimal but already distinct: user solid teal right, assistant glass-card left. Do not add starters, badges, history cap, unreachable panel, or mobile dock in this task — those are expansion.</action>
  <acceptance_criteria>frontend/lib/chat-client.ts exists exporting getApiBaseUrl, CHAT_TIMEOUT_MS equal to 10000, and sendChatMessage with no EventSource reference; frontend/app/chat/page.tsx exists with H1 Chat, location input placeholder matching spec, role log message list, composer textarea plus Send button; npx tsc --noEmit passes in place; mocked node --test contract file passes (Task 2)</acceptance_criteria>
  <verify>
    <automated>npx --prefix frontend tsc --noEmit</automated>
    <fails_when>tsc prints any error lines or exits nonzero — tracer types are broken</fails_when>
  </verify>
  <done>The single happy path (type, Send, mocked reply renders) works and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Contract test locking frontend shapes to backend plus build gate</name>
  <files>frontend/lib/__tests__/chat-contract.test.mjs</files>
  <action>Create a dependency-free Node test using node:test plus node:assert at frontend/lib/__tests__/chat-contract.test.mjs that reads schemas/chat.py, api/routes.py, frontend/lib/chat-client.ts, and frontend/app/chat/page.tsx as text and asserts the contract per D-01 and D-02: request body keys message and location present in client, location defaults to empty string, endpoint path /api/chat present, response keys reply and alert_level present, the four level names Green Yellow Orange Red all present in client, CHAT_TIMEOUT_MS 10000 present, no EventSource or getReader or text/event-stream strings in client, no dangerouslySetInnerHTML in page, NEXT_PUBLIC_WEATHERGPT_API referenced in client and in frontend/.env.local.example. Keep the test runnable with plain node --test and zero new npm dependencies. Then run the frontend production build via robocopy to a temp dir (H drive denies in-place deletion) and confirm zero errors.</action>
  <acceptance_criteria>Contract test file exists with at least 8 assertions covering request keys, response keys, four alert names, timeout value, no-streaming, no-HTML-injection, env reference; node --test run shows all tests passing; temp-dir production build completes with zero type or build errors</acceptance_criteria>
  <verify>
    <automated>node --test frontend/lib/__tests__/chat-contract.test.mjs</automated>
    <fails_when>node --test reports 0 passing tests, any failing assertion, or file-not-found — contract is unproven</fails_when>
  </verify>
  <done>Request/response shapes are locked to the frozen backend and the build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted user message and location cross to FastAPI over direct fetch (D-01) |
| env→client | Build-time env NEXT_PUBLIC_WEATHERGPT_API is public and visible in bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | chat-client request body | medium | mitigate | Send only message plus location as JSON with Content-Type application/json; no extra fields; render replies as text nodes only, never dangerouslySetInnerHTML |
| T-06-02 | Information Disclosure | error detail rendering | medium | mitigate | Quote backend detail verbatim as text; never render as HTML; full error taxonomy lands in Plan 03 |
| T-06-SC | Tampering | npm installs | high | mitigate | No new packages in this plan; no install step runs |
</threat_model>

<verification>
Contract test passes via node --test; npx tsc --noEmit passes in place; production build passes via temp-dir robocopy copy with zero errors; backend untouched (read-only reference only).
</verification>

<success_criteria>
/chat renders header plus location bar plus message list plus composer; one mocked send/reply round-trip works; request/response shapes match api/routes.py plus schemas/chat.py; tsc and build green.
</success_criteria>

## Artifacts this phase produces

- frontend/lib/chat-client.ts — direct-fetch client (D-01) with 10s AbortController timeout, single-JSON response (D-02), env base URL
- frontend/app/chat/page.tsx — /chat route shell: header, location bar (D-03), message log, composer (expanded by Plans 02-03)
- frontend/lib/__tests__/chat-contract.test.mjs — mocked-fetch plus shape contract tests (no live server)

<output>
Create `.planning/phases/06-chat-ui-wiring/06-01-SUMMARY.md` when done
</output>
