---
phase: 06-chat-ui-wiring
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - frontend/components/chat/unreachable-panel.tsx
  - frontend/app/chat/page.tsx
  - frontend/components/chat/composer.tsx
  - frontend/.env.local.example
autonomous: true
requirements:
  - FRNT-03
  - FRNT-04
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - Dead backend or missing env shows a friendly panel with heading, Retry, and the exact configured URL — never a blank screen
    - Base URL comes from NEXT_PUBLIC_WEATHERGPT_API with a not-configured state that attempts no fetch
    - Full keyboard plus screen-reader flow works: live regions, labels, focus management, Enter/Shift+Enter/Escape
    - 390px docked composer never overlaps the last message; slop gates and backend pytest guard pass
  artifacts:
    - frontend/components/chat/unreachable-panel.tsx
    - frontend/components/chat/composer.tsx
  key_links:
    - env value -> unreachable panel URL display -> Retry re-sends identical payload
    - composer keyboard -> send flow -> live-region announcement without focus theft
    - pytest backend suite -> proves frozen backend untouched by frontend work
---

<objective>
Hardening: unreachable panel plus env handling (D-08), full a11y (D-07), 390px docked composer (D-06), anti-slop gates, and backend regression guard.

Purpose: Make the chat demo-proof — dead backends explain themselves, every control is operable and announced, mobile never overlaps, and the frozen backend still passes.
Output: Unreachable panel plus extracted composer wired into page.tsx with a11y, mobile, slop, and pytest gates green.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-ui-wiring/06-CONTEXT.md
@.planning/phases/06-chat-ui-wiring/06-UI-SPEC.md
@.planning/phases/06-chat-ui-wiring/06-01-SUMMARY.md
@.planning/phases/06-chat-ui-wiring/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unreachable panel plus env handling plus composer extraction</name>
  <files>frontend/components/chat/unreachable-panel.tsx, frontend/components/chat/composer.tsx, frontend/app/chat/page.tsx</files>
  <read_first>frontend/lib/chat-client.ts (getApiBaseUrl and sendChatMessage error shapes); frontend/app/chat/page.tsx (Plan 02 state to extend); .planning/phases/06-chat-ui-wiring/06-UI-SPEC.md (Copywriting Contract unreachable heading/body, Fetch Contract error-to-state mapping and missing-env rule, Mobile 390px docked composer spec)</read_first>
  <action>Build unreachable-panel exporting UnreachablePanel with role alert plus heading Can't reach the weather backend plus body naming the configured URL plus guidance Start the backend (uvicorn) or check NEXT_PUBLIC_WEATHERGPT_API, then retry. plus the exact URL rendered as code text and a Retry Button re-sending the identical message plus location once per click with no automatic retries. Handle missing or empty env by displaying not configured (NEXT_PUBLIC_WEATHERGPT_API is empty) as the URL text and attempting no fetch on first load. Route errors per D-08: zero messages yet plus network failure or timeout or missing env renders the full panel; with existing messages render inline ErrorCard and keep the composer usable. Extract composer exporting Composer with textarea 1 to 4 rows auto-grow where Enter sends and Shift+Enter inserts newline and Escape blurs, plus Send Button teal fill with SendHorizontal icon showing Send message text on desktop and icon-only 44px with aria-label Send message on mobile, disabled with aria-disabled plus spinner while in-flight. Dock the composer sticky bottom-0 with glass fill plus blur plus top hairline border plus 12px padding plus env safe-area-inset-bottom, and give the message list 16px bottom padding so the last message never hides under the dock. Keep focus in composer on send and never steal focus on reply arrival.</action>
  <acceptance_criteria>UnreachablePanel heading, body, code-text URL, and Retry exist with role alert; missing env shows the not-configured text with zero fetch attempted; dead backend with zero messages shows panel while dead backend with messages keeps composer usable; Composer handles Enter, Shift+Enter, and Escape with docked sticky bottom-0 bar and safe-area padding; npx tsc --noEmit passes</acceptance_criteria>
  <verify>
    <automated>npx --prefix frontend tsc --noEmit</automated>
    <fails_when>tsc prints any error lines or exits nonzero — panel or composer types are broken</fails_when>
  </verify>
  <done>Dead backend and bad env always explain themselves with URL plus Retry, never blank.</done>
</task>

<task type="auto">
  <name>Task 2: A11y pass plus mobile plus slop gates plus backend regression guard</name>
  <files>frontend/app/chat/page.tsx, frontend/components/chat/composer.tsx, frontend/.env.local.example</files>
  <read_first>.planning/phases/06-chat-ui-wiring/06-UI-SPEC.md (Accessibility Contract table, Responsive plus Theme Notes, Anti-Slop Checklist); frontend/components/header.tsx (Header reuse pattern); frontend/.env.local.example (env line to preserve)</read_first>
  <action>Apply the full a11y contract per D-07 without touching backend code: main with aria-labelledby on chat H1, message list role log plus aria-live polite plus aria-label Conversation plus aria-atomic false, loading role status, errors and unreachable role alert, badge aria-label mirroring visible Alert level text, location label htmlFor plus composer aria-label Type your weather question plus Clear with text or aria-label, focus flow with starter click moving focus to composer and Clear moving focus to composer with Conversation cleared. status announcement and route entry focusing H1 tabIndex minus 1 and skip-to-content link targeting main-content, Enter send with Shift+Enter newline with Escape blur, 2px teal focus-visible ring inherited, reduced-motion instant scroll plus static skeleton. Verify mobile per D-06: header compresses with H1 20px and Clear as 44px icon-only with aria-label Clear conversation, location bar stacks full-width above the list, bubbles stack single column user 85 percent right and assistant 100 percent left, docked composer verified at 390 by 844 with no overlap and no horizontal overflow, all touch targets at least 44px. Run slop gates: grep for lorem is clean, no purple-blue gradient strings in chat files, no dangerouslySetInnerHTML in chat files, no committed env files with only the example file carrying NEXT_PUBLIC_WEATHERGPT_API. Run the backend regression guard python -m pytest tests/ -q proving the frozen backend is untouched, and run the production build via robocopy temp-dir copy with zero errors. Add the Clear conversation control with the confirm copy Clear conversation? This removes all messages on this screen. This cannot be undone. with Clear and Cancel plus Back to home link, hiding the landing CTA on chat.</action>
  <acceptance_criteria>All a11y contract rows present and asserted by grep on role log, aria-live polite, role status, role alert, aria-labels, and skip link; 390px layout has sticky docked composer with safe-area padding and no overlap; slop greps clean with badge classes still byte-matching showcase; python -m pytest tests/ -q passes; temp-dir production build passes with zero errors</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>pytest reports any failed or errored test — frontend work broke the frozen backend</fails_when>
  </verify>
  <done>Chat is operable, announced, overlap-free at 390px, slop-free, with backend still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env→UI | Configured backend URL is echoed back as code text in the unreachable panel |
| keyboard→send | Enter/Shift+Enter/Escape drive the send flow; double-submit must be impossible |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | unreachable URL echo | low | accept | URL is public build-time config, not a secret; echo exact value as code text to aid demos and never echo keys |
| T-06-06 | Denial of Service | double-submit / retry storm | low | mitigate | Disable Send while in-flight, abort previous request before new send, manual Retry only with no auto-backoff |
| T-06-SC | Tampering | npm installs | high | mitigate | No new packages in this plan; no install step runs |
</threat_model>

<verification>
npx tsc --noEmit passes in place; production build passes via temp-dir robocopy copy with zero errors; python -m pytest tests/ -q passes; grep gates confirm live regions, labels, skip link, no lorem, no gradient, no dangerouslySetInnerHTML, no committed env files.
</verification>

<success_criteria>
Unreachable states show heading plus Retry plus exact URL and never blank; env comes from NEXT_PUBLIC_WEATHERGPT_API with a no-fetch not-configured state; keyboard plus screen-reader flow matches the contract; 390px docked input never overlaps; slop gates and backend pytest pass.
</success_criteria>

## Artifacts this phase produces

- frontend/components/chat/unreachable-panel.tsx — friendly dead-backend and bad-env panel with Retry plus exact URL (D-08)
- frontend/components/chat/composer.tsx — docked accessible composer with Enter/Shift+Enter/Escape and 44px targets (D-06, D-07)
- Hardened frontend/app/chat/page.tsx — focus flows, Clear conversation confirm, Back to home, skip link, reduced-motion support
- Gate evidence — tsc plus temp-dir build plus pytest plus slop-grep results recorded in the SUMMARY

<output>
Create `.planning/phases/06-chat-ui-wiring/06-03-SUMMARY.md` when done
</output>
