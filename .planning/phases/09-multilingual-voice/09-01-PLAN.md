---
phase: 09-multilingual-voice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/sarvam_client.py
  - backend/services/multilingual.py
  - backend/schemas/chat.py
  - backend/api/routes.py
  - backend/tests/test_multilingual_chat.py
autonomous: true
requirements:
  - MULT-01
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - A Hindi (Devanagari) chat query returns a Hindi grounded reply produced by translating hi-IN to English, running the frozen English agent core, and translating the reply back to hi-IN
    - Numbers and the alert level in the English agent reply survive the round-trip byte-identical (alert_level is derived pre-translation and never translated)
    - Existing English clients are unaffected (no language field means en-IN passthrough with identical behavior)
  artifacts:
    - backend/services/sarvam_client.py (Sarvam translate thin client, model pin mayura:v1)
    - backend/services/multilingual.py (round-trip orchestration around frozen process_chat)
    - backend/schemas/chat.py (optional language field plus language echo)
    - backend/api/routes.py (language wiring reusing existing error mapping)
    - backend/tests/test_multilingual_chat.py (mocked-Sarvam plus mocked-LLM Hindi tests)
  key_links:
    - Sarvam translate responses flow into the agent input and the agent reply flows back through translate before reaching the client
    - alert_level is computed from the English reply before back-translation so translation can never corrupt it
---

<objective>
Prove the production-quality Hindi round-trip tracer end-to-end per D-01, D-03, D-04: Devanagari query to English via Sarvam translate, through the frozen English agent core, back to Hindi, with numbers and alert level preserved, all behind mocked Sarvam HTTP plus a mocked LLM.

Purpose: De-risk the Sarvam translate seam and the wrap-around orchestration pattern before the remaining five languages and voice endpoints land in Plan 02.
Output: Working Hindi chat path with green mocked end-to-end tests and zero changes to the agent core.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-multilingual-voice/09-CONTEXT.md
@backend/services/agent.py
@backend/api/routes.py
@backend/schemas/chat.py
@backend/core/config.py
@backend/tests/test_chat_api.py
</context>

<tasks>

<task type="tracer">
  <name>End-to-end Hindi round-trip — translate client plus orchestration plus test</name>
  <files>backend/services/sarvam_client.py, backend/services/multilingual.py, backend/tests/test_multilingual_chat.py</files>
  <read_first>backend/core/config.py (SARVAM_API_KEY field plus get_settings caching pattern); backend/tests/test_agent_grounding.py (FakeExecutor seam plus cache-isolation fixture style); backend/services/agent.py (process_chat signature plus _derive_alert_level import location — read only, never edit)</read_first>
  <acceptance_criteria>New file backend/services/sarvam_client.py exposes translate_text(text, source_language_code, target_language_code, transport) performing a synchronous POST to https://api.sarvam.ai/translate with header api-subscription-key carrying the configured SARVAM_API_KEY and JSON body carrying input, source_language_code, target_language_code, model mayura:v1, numerals_format international, and returning the translated_text string from the JSON response, raising a sanitized RuntimeError mentioning Sarvam translate failure when the key is missing, the status is not 200, or translated_text is absent. Inputs longer than 1000 characters are split at sentence boundaries into ordered sub-1000-character chunks translated in sequence and joined with a single space. A module-level transport override seam mirroring the imd_client set_transport pattern lets tests inject httpx.MockTransport with zero live network. New file backend/services/multilingual.py exposes process_multilingual_chat(message, location, language) which for hi-IN translates the query to en-IN, calls services.agent.process_chat with the English text, derives alert_level from the English reply via the existing _derive_alert_level helper, then translates the full English reply back to hi-IN and returns reply plus alert_level plus language hi-IN. backend/tests/test_multilingual_chat.py proves the tracer with Sarvam HTTP mocked and the agent mocked: the Devanagari query for Mumbai weather with 29.5 C returns a Hindi reply containing the exact substring 29.5 and alert_level Green. No test performs live network access.</acceptance_criteria>
  <action>Per D-01 Hindi first-class and D-03 full round-trip around the frozen core: build services/sarvam_client.py as a thin synchronous httpx translate client pinned to model mayura:v1 per the Sarvam Mayura docs page (11-language set hi-IN mr-IN ta-IN te-IN bn-IN gu-IN kn-IN ml-IN mr-IN pa-IN od-IN en-IN, 1000 characters per request, header api-subscription-key, JSON keys input source_language_code target_language_code, response key translated_text), with numerals_format pinned to international so digits survive translation, a 15-second timeout, chunking for over-1000-character inputs at sentence boundaries, server-side-only logging of failures via logger.exception, and sanitized RuntimeError messages that never include the key. Build services/multilingual.py orchestration that imports process_chat and _derive_alert_level from services.agent without modifying that module, implements the hi-IN round-trip with alert derivation on the English text before back-translation, and passes location through untouched. Write backend/tests/test_multilingual_chat.py using httpx.MockTransport for the two translate calls plus a patched agent executor returning a canned English reply carrying 29.5 C and an Alert Green line, asserting the Hindi reply keeps 29.5 byte-identical and alert_level equals Green. Note model pin per Claude's Discretion in the commit message.</action>
  <verify>
    <automated>python -m pytest tests/test_multilingual_chat.py -q</automated>
    <fails_when>pytest exits non-zero or reports no tests collected, or any test opens a live connection to api.sarvam.ai</fails_when>
  </verify>
  <done>The single Hindi happy path works end-to-end through mocked Sarvam plus a mocked agent with numbers preserved, committed.</done>
</task>

<task type="auto">
  <name>Chat schema language field plus route wiring reusing error mapping</name>
  <files>backend/schemas/chat.py, backend/api/routes.py</files>
  <read_first>backend/schemas/chat.py (ChatRequest and ChatResponse field style plus max_length conventions); backend/api/routes.py (chat endpoint with throttle plus run_in_threadpool plus ValueError 422 RuntimeError 502 generic 500 mapping plus sanitize_detail helper)</read_first>
  <acceptance_criteria>First action clears the Windows read-only flag on backend/schemas/chat.py and backend/api/routes.py via attrib -R before any edit. ChatRequest gains an optional language field defaulting to en-IN with max_length 10. ChatResponse gains a language field defaulting to en-IN echoed from the request. The chat route normalizes the language value by stripping surrounding whitespace and lowercasing for comparison, calls process_multilingual_chat with message location and language when the normalized value is anything other than en-IN, and keeps the exact existing process_chat call path when the value is en-IN or absent so existing clients see byte-identical behavior. Unsupported language codes do not crash: the route returns HTTP 200 with an English reply naming the six supported codes hi-IN mr-IN ta-IN te-IN bn-IN as-IN and language echoed as en-IN. sanitize_detail redacts the configured SARVAM_API_KEY value alongside the two existing keys. Throttle, run_in_threadpool, and the 422 502 500 mapping are unchanged.</acceptance_criteria>
  <action>Per D-04 per-request language default English with existing clients unaffected: add the optional language field to ChatRequest and the echo field to ChatResponse following the existing Field description style, wire the chat route to branch between the frozen process_chat path for en-IN and the new process_multilingual_chat path otherwise, handle unsupported codes with the honest English supported-list reply per D-02, and extend sanitize_detail to redact the SARVAM_API_KEY per D-06. Do not touch services/agent.py prompts, _TOOLS, or process_chat internals. Keep the response_model ChatResponse contract intact.</action>
  <verify>
    <automated>python -m pytest tests/test_multilingual_chat.py tests/test_chat_api.py tests/test_error_contract.py -q</automated>
    <fails_when>pytest exits non-zero, or any pre-existing chat or error-contract test changes outcome versus the pre-plan baseline</fails_when>
  </verify>
  <done>Language field flows from request through route to orchestration with English behavior unchanged and secrets redacted, committed.</done>
</task>

<task type="auto">
  <name>Route-level Hindi end-to-end test plus English no-regression test</name>
  <files>backend/tests/test_multilingual_chat.py</files>
  <read_first>backend/tests/test_chat_api.py (httpx ASGITransport plus patch of api.routes.process_chat namespace pattern); backend/tests/test_multilingual_chat.py (translate mock helpers built in the tracer task)</read_first>
  <acceptance_criteria>backend/tests/test_multilingual_chat.py contains an ASGI route test posting message in Devanagari with language hi-IN and location Mumbai while patching the Sarvam translate seam and the multilingual orchestration boundary, asserting HTTP 200 with language echoed as hi-IN, alert_level equal to Green, and the exact digit string from the canned English agent reply present byte-identical in the Hindi reply body. A second test posts the same payload without any language field and asserts HTTP 200 with language en-IN and the English reply untouched. A third test posts language xx-YY and asserts HTTP 200 with language en-IN and a reply naming hi-IN. python -m pytest tests/ -q passes with the whole suite green and no live network in any test.</acceptance_criteria>
  <action>Per D-03 numbers and alerts survive translation and D-04 English default: extend the tracer test file with the three route-level tests described above, patching at the api.routes namespace for the orchestration call and at the sarvam_client transport seam for translate HTTP, using the same httpx ASGITransport base_url http://test pattern as test_chat_api.py. Assert digit preservation with an exact-substring check against the canned English reply digits and assert the alert_level field rather than parsing the translated text. Run the full backend suite from the backend directory and fix only failures attributable to this plan.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>pytest exits non-zero, or the full suite count drops below the pre-plan passing count, or any new test requires live network</fails_when>
  </verify>
  <done>Hindi route round-trip plus English default plus unsupported-code honesty proven at HTTP level with the full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/chat language field | Untrusted BCP-47 code crosses into translate calls and prompt context |
| Sarvam translate → orchestration | Untrusted provider text flows into the user-facing reply |
| error path → client | Upstream failures must never carry key material or tracebacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | language field normalization | medium | mitigate | Strip plus lowercase plus allowlist compare; unknown codes get the honest English supported-list reply, never passed to the provider |
| T-09-02 | Tampering | translated reply integrity | high | mitigate | numerals_format pinned to international; alert_level derived from pre-translation English via _derive_alert_level and returned as a separate field |
| T-09-03 | Information disclosure | Sarvam failures | high | mitigate | Log-full server-side via logger.exception; send-safe sanitized RuntimeError mapped to 502 by the existing route mapping; SARVAM_API_KEY added to sanitize_detail redaction |
| T-09-04 | Denial of service | overlong translate input | low | mitigate | 1000-character chunking bounds any single provider request; chat-level 2000-character cap already enforced by schema |
| T-09-SC | Tampering | pip installs | high | mitigate | No new packages in this plan (httpx plus pytest already vendored); no install step so no legitimacy gate needed |
</threat_model>

<verification>
Plan 01 proves the tracer: python -m pytest tests/test_multilingual_chat.py -q green, then python -m pytest tests/ -q green with the pre-plan passing count intact. Spot-check: POST /api/chat with a Devanagari message and language hi-IN under mocks returns Hindi text with English digits intact and alert_level Green.
</verification>

<success_criteria>
Hindi query flows through mocked Sarvam translate plus the frozen agent core back to Hindi with digits byte-identical and alert_level preserved, English default behavior unchanged, agent core untouched, full pytest suite green.
</success_criteria>

<output>
Create `.planning/phases/09-multilingual-voice/09-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Sarvam translate thin client in backend/services/sarvam_client.py (this plan: translate only, mayura:v1 pin)
- Round-trip orchestration in backend/services/multilingual.py (this plan: Hindi path)
- Chat schema language field plus echo and route wiring (this plan)
- Hindi tracer tests with mocked Sarvam plus mocked LLM (this plan)
- Five-language capability map plus STT and TTS clients plus voice endpoints plus caps (Plan 02)
- Per-language and voice tests plus secret-safety tests (Plans 02 and 03)
- Coverage matrix with model pins plus OPT-OUT rows plus opt-in live test plus full gates (Plan 03)
