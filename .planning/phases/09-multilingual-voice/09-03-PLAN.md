---
phase: 09-multilingual-voice
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - backend/tests/test_multilingual_secrets.py
  - backend/tests/test_live_sarvam.py
  - backend/.env.example
  - .planning/phases/09-multilingual-voice/09-COVERAGE.md
autonomous: true
requirements:
  - MULT-01
  - VOIC-01
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - The Sarvam key never appears in any response body, log line, or committed file, proven by sentinel tests mirroring the existing secret-safety suite
    - The coverage matrix records INTEGRATE versus OPT-OUT for translate, STT, and TTS per language with model pins and the as-IN TTS reason, so the seal gate sees every decision
    - Exactly one tiny opt-in live test exists for translate, skipped by default, with everything else mocked in CI
  artifacts:
    - backend/tests/test_multilingual_secrets.py (Sarvam sentinel redaction tests)
    - backend/tests/test_live_sarvam.py (single opt-in skipped-by-default live test)
    - backend/.env.example (SARVAM_API_KEY line)
    - .planning/phases/09-multilingual-voice/09-COVERAGE.md (api-coverage decision matrix)
  key_links:
    - sanitize_detail redaction covers the Sarvam key on every route including both voice endpoints
    - Coverage rows pin the exact models so future phases reuse them without re-verifying docs
---

<objective>
Seal Phase 9 per D-06 key-never-logged plus the api-coverage contribution: Sarvam secret-safety tests, the Phase 9 coverage matrix with per-language INTEGRATE rows and OPT-OUT reasons, one tiny opt-in live translate test, and the full gate battery green for MULT-01 and VOIC-01.

Purpose: Make the Sarvam integration auditable and leak-proof with its decisions recorded for the seal gate and Phase 10 reuse.
Output: Secrets tests, coverage matrix, live opt-in test, documented env key, green gates.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-multilingual-voice/09-CONTEXT.md
@.planning/phases/09-multilingual-voice/09-01-SUMMARY.md
@.planning/phases/09-multilingual-voice/09-02-SUMMARY.md
@.planning/phases/02-imd-live-data/COVERAGE.md
@backend/tests/test_secrets.py
@backend/scripts/live_vendor_probe.py
</context>

<tasks>

<task type="auto">
  <name>Sarvam secret-safety tests plus env example</name>
  <files>backend/tests/test_multilingual_secrets.py, backend/.env.example</files>
  <read_first>backend/tests/test_secrets.py (sentinel forcing plus _assert_no_sentinels plus route-failure plus caplog pattern); backend/.env.example (key placeholder plus comment style); backend/api/routes.py (sanitize_detail secret tuple after Plans 01 and 02)</read_first>
  <acceptance_criteria>First action clears the Windows read-only flag on backend/.env.example via attrib -R before any edit. New file backend/tests/test_multilingual_secrets.py sets OPENROUTER_API_KEY WEATHER_API_KEY and SARVAM_API_KEY to unique sentinel strings, forces a Sarvam translate outage plus an LLM outage through the real chat and transcribe paths, and asserts no sentinel appears in the raised errors, the HTTP response bodies, or the captured weathergpt log output, mirroring test_secrets.py structure. It also asserts sanitize_detail replaces the Sarvam sentinel with the redaction marker. backend/.env.example documents SARVAM_API_KEY with a never-commit comment plus the Sarvam dashboard location where the key is issued, carrying no real key material. python -m pytest tests/test_multilingual_secrets.py -q passes.</acceptance_criteria>
  <action>Per D-06 key never logged and the carry-over sanitize and redact rules: build the sentinel suite covering the multilingual chat path with a forced translate failure, the transcribe path with a forced STT failure, and the direct sanitize_detail unit check, using the same monkeypatch plus cache-clear plus caplog discipline as test_secrets.py. Extend .env.example with the SARVAM_API_KEY placeholder line only. Verify no committed file contains live key material by searching the backend tree for the sentinel values outside the test file and for any 40-plus-character api-subscription-key-like literal.</action>
  <verify>
    <automated>python -m pytest tests/test_multilingual_secrets.py -q</automated>
    <fails_when>pytest exits non-zero, or any sentinel string appears in a response body or captured log, or a live-looking Sarvam key literal is found in the backend tree</fails_when>
  </verify>
  <done>Sarvam key proven absent from errors, bodies, logs, and committed files with the env key documented, committed.</done>
</task>

<task type="auto">
  <name>Coverage matrix with pins plus OPT-OUT rows</name>
  <files>.planning/phases/09-multilingual-voice/09-COVERAGE.md</files>
  <read_first>.planning/phases/02-imd-live-data/COVERAGE.md (INTEGRATE versus OPT-OUT table style with scope note and reason columns); backend/services/sarvam_client.py (final model pins plus endpoint URLs plus capability table from Plans 01 and 02)</read_first>
  <acceptance_criteria>New file .planning/phases/09-multilingual-voice/09-COVERAGE.md carries an INTEGRATE section with one row per capability: translate for hi-IN mr-IN ta-IN te-IN bn-IN via mayura:v1, translate for as-IN via sarvam-translate:v1, STT for all six plus en-IN via saaras:v3 mode transcribe on POST https://api.sarvam.ai/speech-to-text, TTS for hi-IN mr-IN ta-IN te-IN bn-IN en-IN via bulbul:v3 speaker shubh on POST https://api.sarvam.ai/text-to-speech, each row citing its endpoint plus header api-subscription-key plus MockTransport-only CI posture. An OPT-OUT section holds exactly the TTS as-IN row with the reason that Bulbul v3 supports 11 languages with no as-IN entry so speak returns an honest 422. A notes section records the 1000 versus 2000 translate chunk limits, the 2500 TTS character cap, the 2MB audio cap, numerals_format international for digit survival, and frozen-agent-core wrapping. Every row names its requirement MULT-01 or VOIC-01.</acceptance_criteria>
  <action>Record the api-coverage contribution for the new external Sarvam API: write the matrix so the seal gate sees translate STT and TTS each decided with INTEGRATE scope notes and the single OPT-OUT reason, mirroring the Phase 2 coverage file table style, stating pins and fallback rows explicitly without inventing model names beyond mayura:v1 sarvam-translate:v1 saaras:v3 and bulbul:v3 as verified from the Sarvam docs during planning. Reference the frozen English agent core wrap and the MockTransport plus mocked-LLM CI posture with one tiny opt-in live test maximum.</action>
  <verify>
    <automated>python -m pytest tests/test_multilingual_chat.py tests/test_multilingual_voice.py -q</automated>
    <fails_when>pytest exits non-zero, or the coverage file lacks any of the four model pins mayura:v1 sarvam-translate:v1 saaras:v3 bulbul:v3</fails_when>
  </verify>
  <done>Every Sarvam capability decided in the matrix with pins and the as-IN TTS reason visible to the seal gate.</done>
</task>

<task type="auto">
  <name>Opt-in live translate test plus full gate battery</name>
  <files>backend/tests/test_live_sarvam.py</files>
  <read_first>backend/scripts/live_vendor_probe.py (tiny en-IN to hi-IN translate proof plus key-missing early return style); backend/tests/test_live_openrouter.py (pre-existing live-test skip marker pattern)</read_first>
  <acceptance_criteria>New file backend/tests/test_live_sarvam.py holds exactly one test translating the short English string What is the weather today from en-IN to hi-IN through the real translate_text path, skipped unless the RUN_LIVE_SARVAM environment variable equals 1 and a SARVAM_API_KEY is configured, asserting a non-empty translated string on the live path and performing zero network otherwise. The default suite stays fully mocked: python -m pytest tests/ -q passes with the new test reported as skipped and the total passing count at or above the Plan 02 baseline. No other live tests are added in this phase keeping the one-tiny-test maximum. A final scan confirms no new pip or npm packages were introduced by the phase.</acceptance_criteria>
  <action>Per the carry-over MockTransport plus mocked-LLM-only-in-CI rule with one tiny opt-in live test maximum: build the live test with a module-level skipif on the RUN_LIVE_SARVAM gate following the pre-existing live-test marker pattern, reuse the probe script tiny-text choice so the live call stays minimal, never log or print the key, and then run the full ordered battery reusing the proven command verbatim python -m pytest tests/ -q from the backend directory. Fix only gate failures attributable to this phase and leave agent, forecast, trend, and frontend behavior otherwise untouched.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>pytest exits non-zero, or the live test runs without RUN_LIVE_SARVAM set to 1, or the live test count in the phase exceeds one</fails_when>
  </verify>
  <done>Single opt-in live proof exists with CI fully mocked and the full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed files → repo | Env examples, tests, and docs must never carry live key material |
| live opt-in test → provider | A single gated test carries the real key on an explicit operator opt-in |
| coverage docs → future phases | Pins recorded here will be trusted by Phase 10 without re-verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Information disclosure | Sarvam key in outputs | critical | mitigate | Sentinel suite proves absence from errors, bodies, and logs on chat plus transcribe paths; sanitize_detail unit check pins redaction |
| T-09-11 | Information disclosure | key in committed files | high | mitigate | .env.example carries a placeholder only; scan asserts no live-looking key literal in the backend tree; real .env stays gitignored |
| T-09-12 | Information disclosure | live test key handling | medium | mitigate | Live test gated on RUN_LIVE_SARVAM equals 1, tiny fixed text, key never printed or logged, skipped by default in CI |
| T-09-SC | Tampering | pip and npm installs | high | mitigate | No new packages in this plan or phase; battery runs on the vendored toolchain only |
</threat_model>

<verification>
Ordered battery: python -m pytest tests/test_multilingual_secrets.py -q, then coverage pin grep over 09-COVERAGE.md for all four model pins, then python -m pytest tests/ -q with the live test skipped by default, then a scan for live-looking key literals in the backend tree.
</verification>

<success_criteria>
Sarvam sentinel tests pass on every route, coverage matrix records every capability with pins and the as-IN TTS OPT-OUT reason, exactly one skipped-by-default live test exists, env key documented without material, full pytest suite green with no new packages.
</success_criteria>

<output>
Create `.planning/phases/09-multilingual-voice/09-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Sarvam secret-safety sentinel tests plus documented SARVAM_API_KEY placeholder (this plan)
- Phase 9 coverage matrix with model pins plus OPT-OUT rows (this plan)
- Single opt-in live translate test skipped by default (this plan)
- Hindi tracer plus schema plus route wiring (Plan 01)
- Six-language capability map plus voice endpoints plus tests (Plan 02)
