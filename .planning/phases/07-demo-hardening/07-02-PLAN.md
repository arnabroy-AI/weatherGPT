---
phase: 07-demo-hardening
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [Dockerfile, frontend/Dockerfile, frontend/next.config.mjs, compose.yaml, README.md, scripts/demo_smoke.py]
autonomous: true
requirements: [DEMO-01, DEMO-02]
estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - docker compose up starts backend (uvicorn) plus frontend (standalone) from host .env keys never baked into images
    - README takes a fresh clone from clone to working chat in 10 minutes or less
    - Three seeded demo queries pass and byte-match the chat starter strings
    - Compose files validate without a daemon and logs stay key-free
    - Backend pytest plus frontend tsc gates stay green after Docker edits
  artifacts:
    - Dockerfile
    - frontend/Dockerfile
    - compose.yaml
    - README.md
    - scripts/demo_smoke.py
  key_links:
    - compose.yaml frontend build arg NEXT_PUBLIC_WEATHERGPT_API matches chat-client env var
    - README seeded strings byte-match STARTER_QUERIES in starters.tsx
    - scripts/demo_smoke.py asserts the same byte-match plus compose config validity
---

<objective>
Author the one-command SIH demo per D-02 through D-05: backend + frontend Dockerfiles, root compose.yaml fed by host .env at runtime, README 10-minute flow, and an offline-capable seeded-query smoke script. Builds on Plan 01 hardening (CORS/throttle/ignores). Per D-07 v1 locked, English text chat only, no new features.

Purpose: A judge with Docker and no repo knowledge reaches working chat in one command plus three green seeded queries.
Output: Dockerfile, frontend/Dockerfile, standalone next.config.mjs, compose.yaml, README.md, scripts/demo_smoke.py.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-demo-hardening/07-CONTEXT.md
@.env.example
@frontend/.env.local.example
@frontend/lib/chat-client.ts
@frontend/components/chat/starters.tsx
@frontend/next.config.mjs
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Dockerfiles plus standalone frontend output</name>
  <files>Dockerfile, frontend/Dockerfile, frontend/next.config.mjs</files>
  <read_first>frontend/next.config.mjs (currently empty config), frontend/package.json (build/start scripts, next 14.2.18), requirements.txt (pinned backend deps), .dockerignore (Plan 01 exclusions)</read_first>
  <action>Per D-02 author root backend Dockerfile FROM python:3.12-slim-bookworm, workdir /app, copy requirements.txt first for layer cache, pip install without cache, copy the backend sources only (main.py api core schemas services tools), EXPOSE 8000, CMD uvicorn main:app host 0.0.0.0 port 8000; set no ENV secret lines per D-03. Author frontend/Dockerfile multi-stage FROM node:20-alpine: deps stage npm ci, builder stage with ARG NEXT_PUBLIC_WEATHERGPT_API default http://localhost:8000 promoted to ENV for the Next public-inline bake, build, runner stage copying standalone server plus static assets, EXPOSE 3000, CMD node server.js. Set frontend/next.config.mjs output standalone, preserving all existing behavior otherwise. Respect the H:/ no-delete ACL by editing in place and never deleting .next output.</action>
  <acceptance_criteria>Dockerfile contains FROM python:3.12-slim-bookworm and uvicorn main:app and zero lines starting with ENV OPENROUTER or ENV WEATHER; frontend/Dockerfile contains ARG NEXT_PUBLIC_WEATHERGPT_API and node:20-alpine and server.js; frontend/next.config.mjs contains output and standalone; neither Dockerfile contains a literal sk-or or key value.</acceptance_criteria>
  <verify><automated>npx --prefix frontend tsc --noEmit</automated><fails_when>exit code is non-zero or output contains error TS</fails_when></verify>
  <done>Both images defined with pinned bases, keys unbaked, standalone config type-clean.</done>
  <reversibility rating="reversible">New files plus one config key; removal restores prior local-dev-only flow.</reversibility>
</task>

<task type="auto">
  <name>Compose pair with runtime-only keys plus config validation</name>
  <files>compose.yaml</files>
  <read_first>Dockerfile and frontend/Dockerfile from task 1, .env.example (runtime key names), frontend/lib/chat-client.ts (NEXT_PUBLIC_WEATHERGPT_API contract)</read_first>
  <action>Per D-02 and D-03 author root compose.yaml with exactly two services: backend building root Dockerfile with env_file .env and ports 8000:8000, frontend building frontend/Dockerfile with build arg NEXT_PUBLIC_WEATHERGPT_API default http://localhost:8000 and ports 3000:3000 and depends_on backend; no environment secret values inline, only the public base-URL var. First probe docker compose version; when the CLI exists run docker compose config and require success; when no daemon or CLI exists still pass on content gates and record the live-run as human-follow-up in the plan SUMMARY. Keep compose logs key-free by inheriting the backend sanitize/redact pattern (no key echo in compose file or command lines).</action>
  <acceptance_criteria>compose.yaml contains services backend and frontend, env_file .env under backend, NEXT_PUBLIC_WEATHERGPT_API under frontend build args, ports 8000:8000 and 3000:3000; compose.yaml contains zero secret values (no sk-or, no your-key-here); docker compose config succeeds when the CLI is present, else SUMMARY records human-follow-up.</acceptance_criteria>
  <verify><automated>docker compose config --quiet</automated><fails_when>exit code is non-zero or output contains ERROR or validation failed</fails_when></verify>
  <done>Compose pair validates from disk and keys only ever arrive via host .env at runtime.</done>
</task>

<task type="auto">
  <name>README 10-minute flow plus offline-capable seeded smoke script</name>
  <files>README.md, scripts/demo_smoke.py</files>
  <read_first>frontend/components/chat/starters.tsx (STARTER_QUERIES exact strings), frontend/components/live-demo-teaser.tsx (seeded query byte-match reference), .planning/phases/07-demo-hardening/07-CONTEXT.md (D-04 D-05 D-09 disclosure voice)</read_first>
  <action>Per D-04 write root README with sections Prerequisites (Docker, 10 minutes), Quick start compose primary (clone, copy .env.example to .env and fill OPENROUTER_API_KEY, docker compose up, open localhost:3000), Local-dev secondary (uvicorn main:app plus npm dev with NEXT_PUBLIC_WEATHERGPT_API), the three seeded demo queries byte-matching STARTER_QUERIES exactly (Current weather in Pune, Mumbai this weekend, Paddy sowing advice for Nashik) per D-05 with expected Green-to-Red badge note, live-key versus offline note (live LLM needs a real key, otherwise health plus fixtures path), and troubleshooting (daemon down, port busy, ACL index.lock pointer to ENV-REPAIR-STATUS.md). Voice per carry-over anti-slop: plain imperative SIH-judge diction, concrete ports and timings, zero purple-gradient or robot-clipart language. Per D-05 and carry-over write stdlib-only scripts/demo_smoke.py with default offline mode asserting the three README strings byte-match starters.tsx, compose.yaml parses with two services, and GET localhost:8000/health is attempted-but-optional; add opt-in --live flag posting the trio to a running backend and reporting pass/fail per query without ever printing key values. Per D-07 document English text chat only.</action>
  <acceptance_criteria>README.md contains docker compose up and localhost:3000 and localhost:8000 and 10 min and all three exact seeded strings and OPENROUTER_API_KEY and an offline-or-live key note; scripts/demo_smoke.py contains the three exact seeded strings and --live and zero prints of key values; python scripts/demo_smoke.py --offline exits 0.</acceptance_criteria>
  <verify><automated>python scripts/demo_smoke.py --offline</automated><fails_when>exit code is non-zero or output contains MISMATCH or FAIL</fails_when></verify>
  <verify><automated>python -m pytest tests/ -q</automated><fails_when>exit code is non-zero or output contains FAILED or ERROR</fails_when></verify>
  <done>README carries a judge from clone to chat inside 10 minutes and the smoke script proves seeded parity offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host .env→containers | Operator keys enter at runtime only, never at build |
| browser→compose frontend→backend | Judge traffic over localhost ports 3000/8000 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information disclosure | Dockerfiles/compose.yaml | critical | mitigate | No ENV secret lines, env_file runtime-only, .dockerignore excludes .env; acceptance grep asserts zero key material |
| T-07-05 | Information disclosure | compose logs + smoke output | high | mitigate | Smoke script never prints env values; README instructs key-free log sharing; backend sanitize_detail already redacts |
| T-07-06 | Denial of service | compose demo without throttle | low | mitigate | Plan 01 per-IP throttle ships first (depends_on 07-01); compose inherits it |
| T-07-SC | Tampering | base images | medium | mitigate | Pinned tags python:3.12-slim-bookworm and node:20-alpine; human verifies tags on Docker Hub at review |
</threat_model>

<verification>
docker compose config succeeds when CLI present; python scripts/demo_smoke.py --offline exits 0; python -m pytest tests/ -q all passed; npx --prefix frontend tsc --noEmit clean; README seeded strings byte-match starters.tsx; no key material in any new file.
</verification>

<success_criteria>
Fresh-clone judge flow reads compose-primary and reaches chat in 10 minutes or less; three seeded queries documented and smoke-verified; images build from pinned bases with runtime-only keys.
</success_criteria>

## Artifacts this phase produces

This plan produces: Dockerfile (backend uvicorn), frontend/Dockerfile (standalone runner), frontend/next.config.mjs standalone output, compose.yaml (backend plus frontend pair), README.md (10-minute SIH flow), scripts/demo_smoke.py (offline plus opt-in live seeded checks). (Plan 01 produced ENV-REPAIR-STATUS.md, api/throttle.py, CORS wiring, ignore files, hardening tests.)

<output>
Create `.planning/phases/07-demo-hardening/07-02-SUMMARY.md` when done
</output>
