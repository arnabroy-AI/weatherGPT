---
phase: 07-demo-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [main.py, core/config.py, api/throttle.py, api/routes.py, tests/test_demo_hardening.py, tests/conftest.py, .gitignore, .dockerignore, .env.example]
autonomous: true
requirements: [DEMO-01]
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Env-repair state (ACL/index.lock/.env-ignore/docker daemon) is recorded, never blindly retried
    - CORS defaults open for local dev but locks to an env allowlist when set
    - Rapid per-IP POSTs to /api/chat get 429 with Retry-After, normal use unaffected
    - Root .env is gitignored and no secret can leak via ignore/docker gaps
    - Backend pytest plus frontend tsc and chat-contract gates all stay green
  artifacts:
    - .planning/phases/07-demo-hardening/ENV-REPAIR-STATUS.md
    - api/throttle.py
    - tests/test_demo_hardening.py
    - .gitignore
    - .dockerignore
  key_links:
    - main.py CORS middleware reads core/config.py allowlist helper
    - POST /api/chat passes through api/throttle.py before the agent
    - tests/conftest.py resets throttle store so the suite stays isolated
---

<objective>
Tracer-first hardening slice for the SIH demo: verify the D-01 human-prerequisite repair state (probe, record, degrade gracefully), then land the deferred D-06 hardening (CORS env allowlist defaulting open for local dev, stdlib-only per-IP throttle on /chat) plus .gitignore/.dockerignore hygiene, all guarded by pytest. Per D-07 no new features are added.

Purpose: Unblock demo readiness on a verified environment and close the two deferred hardening items without regressing local-dev ergonomics.
Output: ENV-REPAIR-STATUS.md, api/throttle.py, CORS allowlist wiring, ignore files, hardening tests, green gates.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-demo-hardening/07-CONTEXT.md
@main.py
@core/config.py
@api/routes.py
@tests/conftest.py
</context>

<tasks>

<task type="tracer">
  <name>Tracer: env-repair probe plus CORS allowlist plus per-IP throttle end to end</name>
  <files>.planning/phases/07-demo-hardening/ENV-REPAIR-STATUS.md, core/config.py, main.py, api/throttle.py, api/routes.py, tests/test_demo_hardening.py, tests/conftest.py</files>
  <read_first>main.py (CORS block lines 47-54, request-id middleware pattern), core/config.py (Settings + get_settings cache pattern), api/routes.py (chat route + sanitize_detail pattern), tests/conftest.py (dummy-key + cache-reset fixture)</read_first>
  <action>First probe and record, per D-01, and never blindly retry a blocked mutation: run docker info and docker compose version, Test-Path .git/index.lock, git check-ignore -q .env, and one harmless new-file write probe; write all four outcomes to ENV-REPAIR-STATUS.md with a human-follow-up line for anything blocked. Attempt Remove-Item .git/index.lock at most once and only when the probe shows it is deletable; on failure record it as a human prerequisite and continue with disk-verifiable work. Then wire ONE path end to end per D-06: add a CORS_ALLOW_ORIGINS string setting (default empty) plus a helper that returns star-open when unset and an exact-match origin list parsed from comma-separated env when set; switch main.py CORSMiddleware to that helper with allow_credentials true only for the explicit list. Add stdlib-only api/throttle.py (time.monotonic plus threading.Lock sliding window, 60 requests per 60 seconds per client IP, keyed on request.client.host only, never on X-Forwarded-For) exposing check function and reset_rate_limiter; enforce it on POST /api/chat only, returning 429 JSON detail plus a Retry-After header, sanitized through the existing sanitize_detail. Wire reset_rate_limiter into the conftest autouse fixture so existing tests stay isolated. Add CHAT_THROTTLE_PER_MIN int setting default 60 (agent discretion per CONTEXT, env-overridable). Per D-07 add no features beyond this. Using stdlib only per D-06, no new dependency.</action>
  <acceptance_criteria>ENV-REPAIR-STATUS.md exists and names docker daemon reachable yes/no, index.lock present/absent plus removed-or-human-follow-up, .env ignored yes/no; core/config.py contains CORS_ALLOW_ORIGINS and CHAT_THROTTLE_PER_MIN; main.py contains CORS_ALLOW_ORIGINS with no remaining hardcoded allow_origins star-only line; api/throttle.py contains Retry-After and reset_rate_limiter; one over-limit POST /api/chat returns 429 with a Retry-After header while a single POST still returns 200 or 502-per-key-status.</acceptance_criteria>
  <verify><automated>python -m pytest tests/test_demo_hardening.py -q</automated><fails_when>exit code is non-zero or the output contains FAILED or ERROR</fails_when></verify>
  <done>Single happy path plus single throttled path both behave end to end and repair state is on record.</done>
  <reversibility rating="reversible">CORS/throttle are env-gated settings edits revertible by unsetting two vars.</reversibility>
</task>

<task type="auto">
  <name>Ignore hygiene: root .gitignore plus .dockerignore plus env examples</name>
  <files>.gitignore, .dockerignore, .env.example</files>
  <read_first>.env.example (current two-key shape), frontend/.env.local.example (base-URL var, read-only reference)</read_first>
  <action>Create root .gitignore per D-01 listing .env exactly plus __pycache__/ and *.pyc and .pytest_cache/ and .coverage* and frontend/node_modules/ and frontend/.next/ and frontend/out/; create root .dockerignore per D-03 and carry-over listing .env and .env.local and node_modules and __pycache__ and .planning and .git and .pytest_cache/ and *.pyc; append commented CORS_ALLOW_ORIGINS (empty means local-dev open) and CHAT_THROTTLE_PER_MIN=60 lines to .env.example without touching the two existing key lines. Do not edit frontend/.env.local.example. New files are fine on this ACL; edit .env.example in place.</action>
  <acceptance_criteria>git check-ignore -q .env exits 0; .gitignore contains a line exactly .env; .dockerignore contains lines .env and node_modules and __pycache__ and .planning; .env.example contains CORS_ALLOW_ORIGINS and CHAT_THROTTLE_PER_MIN; git status no longer proposes .env as untracked-committable (check-ignore proof suffices).</acceptance_criteria>
  <verify><automated>git check-ignore -q .env</automated><fails_when>exit code is non-zero (file not ignored)</fails_when></verify>
  <done>Root .env is ignored, Docker builds cannot bake keys via ignored paths, examples document the two new vars.</done>
</task>

<task type="auto">
  <name>Hardening regression gate: CORS plus throttle plus secret-safety, full suite green</name>
  <files>tests/test_demo_hardening.py</files>
  <read_first>tests/test_demo_hardening.py (tracer tests from task 1), tests/test_secrets.py (secret-safety pattern), tests/test_error_contract.py (error-shape pattern)</read_first>
  <action>Extend tests/test_demo_hardening.py to lock: CORS reflects an explicit Origin when CORS_ALLOW_ORIGINS is set and star-open when unset; throttle returns 429 with Retry-After after the configured limit with fresh reset between tests; 429 and CORS-rejected bodies never contain the dummy key value; normal single POST still succeeds. Then run the three proven gates and report each result; fix regressions before finishing. Per D-07 no product behavior beyond the D-06 pair.</action>
  <acceptance_criteria>tests/test_demo_hardening.py contains Origin allowlist assertion and 429 assertion and dummy-key-absent assertion; python -m pytest tests/ -q reports all passed; npx --prefix frontend tsc --noEmit exits 0; node --test frontend/lib/__tests__/chat-contract.test.mjs reports all passed.</acceptance_criteria>
  <verify><automated>python -m pytest tests/ -q</automated><fails_when>exit code is non-zero or output contains FAILED or ERROR</fails_when></verify>
  <verify><automated>npx --prefix frontend tsc --noEmit</automated><fails_when>exit code is non-zero or output contains error TS</fails_when></verify>
  <verify><automated>node --test frontend/lib/__tests__/chat-contract.test.mjs</automated><fails_when>exit code is non-zero or output contains not ok or failing</fails_when></verify>
  <done>Full backend suite plus both frontend gates green with hardening locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Untrusted Origin header and request volume cross here |
| env→app | Operator-supplied CORS_ALLOW_ORIGINS and throttle limit cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | api/throttle.py IP key | medium | accept | Key on request.client.host only, never X-Forwarded-For; single-node SIH demo, documented limitation |
| T-07-02 | Tampering | CORS allowlist parse | medium | mitigate | Exact-match origin list from env, no regex or wildcard-passthrough when set; credentials enabled only for explicit list |
| T-07-03 | Information disclosure | 429/log bodies | high | mitigate | All throttle error detail passes through existing sanitize_detail; test asserts dummy key absent |
| T-07-SC | Tampering | package installs | low | accept | Zero new dependencies this plan (stdlib-only throttle per D-06); nothing to audit |
</threat_model>

<verification>
python -m pytest tests/ -q all passed; npx --prefix frontend tsc --noEmit clean; node --test frontend/lib/__tests__/chat-contract.test.mjs all passed; git check-ignore -q .env exits 0; over-limit POST returns 429 with Retry-After.
</verification>

<success_criteria>
ENV-REPAIR-STATUS.md records all four probes with follow-ups; CORS allowlist + throttle live behind env vars; ignore files land; all three gates green and reported.
</success_criteria>

## Artifacts this phase produces

This plan produces: ENV-REPAIR-STATUS.md (probe record), api/throttle.py (stdlib throttle), CORS allowlist wiring in core/config.py + main.py, tests/test_demo_hardening.py, root .gitignore, root .dockerignore, extended .env.example. (Plan 02 adds Dockerfiles, compose.yaml, README.md, scripts/demo_smoke.py.)

<output>
Create `.planning/phases/07-demo-hardening/07-01-SUMMARY.md` when done
</output>
