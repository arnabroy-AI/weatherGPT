---
phase: 02-imd-live-data
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/imd_client.py
  - tools/weather.py
  - tests/fixtures/open_meteo_mumbai.json
  - tests/test_imd_client.py
autonomous: true
requirements:
  - DATA-01
  - DATA-03
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Querying get_current_weather('Pune') returns live Open-Meteo values (e.g. temperature_c 26.1 from the recorded fixture) as a JSON string through the unchanged agent seam"
    - "Tool name stays get_current_weather and return type stays JSON string, so services/agent.py needs no rewrite"
    - "Every consumer-facing payload discloses non-authentic model data (source names open-meteo, never claims IMD issuance)"
  artifacts:
    - tools/imd_client.py
    - tests/fixtures/open_meteo_mumbai.json
    - tests/test_imd_client.py
  key_links:
    - "tools/weather.py delegates provider fetch to tools/imd_client.py and maps all 14 frozen keys"
    - "tests/test_imd_client.py injects httpx.MockTransport so CI never hits the live network"
---

<objective>
Prove end-to-end live current weather for one known city (Pune, fixture-recorded as Mumbai sample per RESEARCH.md) through the frozen tool seam — per D-01 (implement against the researcher's pick), D-09 (Open-Meteo engine behind the IMD-shaped seam, disclosed non-authentic), and DATA-01/DATA-03.

Purpose: Establish the single provider seam and field mapping first, so cache/fallback/city-map work in later plans builds on a verified slice.
Output: tools/imd_client.py, rewired tools/weather.py body, recorded fixture, mapping + contract tests green.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-imd-live-data/02-CONTEXT.md
@.planning/phases/02-imd-live-data/02-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Open-Meteo live path end-to-end for one known city</name>
  <files>tools/imd_client.py, tools/weather.py, tests/fixtures/open_meteo_mumbai.json</files>
  <read_first>
    - File modified: tools/weather.py (replace mock body, keep tool name plus signature plus JSON-string return).
    - Source of truth: .planning/phases/02-imd-live-data/02-RESEARCH.md sections Winning Source Exact Contract, Field-to-Our-JSON Mapping Table, Code Examples (Mumbai live sample plus fetch skeleton).
    - Source of truth: .planning/phases/02-imd-live-data/02-CONTEXT.md decisions D-01, D-02, D-09 and Canonical References (frozen contracts).
    - Source of truth: services/agent.py lines 75-99 (alert_level fallback parse the tool JSON must satisfy).
  </read_first>
  <action>Per D-01 and D-09, implement the Phase 2 runtime against Open-Meteo as the live engine behind the IMD-shaped seam. Create tools/imd_client.py owning all provider knowledge: a sync fetch function using httpx.Client with 6.0s timeout that GETs https://api.open-meteo.com/v1/forecast with params current set to temperature_2m,relative_humidity_2m,apparent_temperature,precipitation,weather_code,pressure_msl,wind_speed_10m,wind_direction_10m,visibility plus hourly set to precipitation plus past_days set to 1 plus wind_speed_unit set to kmh plus timezone set to UTC, accepting an injectable httpx transport for tests; a frozen WMO weather-code to condition-text dict where code 3 maps to Overcast and any unmapped code yields Unknown (code N) with no crash; a 16-point compass conversion via index int((deg+11.25)/22.5)%16 over N,NNE,NE,ENE,E,ESE,SE,SSE,S,SSW,SW,WSW,W,WNW,NW,NNW; a derive_alert_level function with conservative thresholds mapping severe WMO codes 95,96,99 plus heavy-rain codes 65 plus 80-82 upward and defaulting to Green; and a mapper emitting exactly the 14 frozen keys location, observed_at_utc, temperature_c, feels_like_c, humidity_pct, condition, rainfall_mm_last_24h, wind_kph, wind_direction, pressure_hpa, visibility_km, source, alert_level, advisory. Rewire the tools/weather.py tool body only: normalise location via strip with lower for lookup but echo title-cased user input in the location key, resolve Pune and Mumbai to frozen lat-lon pairs (Mumbai 19.07283,72.88261 per RESEARCH.md verified geocode; Pune 18.5204,73.8567), call the imd_client fetch plus mapper, set source to the literal open-meteo-live (non-IMD model data; IMD-direct pending), never use the words IMD-issued for derived levels, keep WEATHER_API_KEY read-and-ignored so the keyless path works with the existing .env per D-02, do not touch services/agent.py, api/routes.py, schemas/chat.py, main.py, core/config.py per D-06 and Phase 1 carryover. Record tests/fixtures/open_meteo_mumbai.json verbatim from the RESEARCH.md Mumbai live sample (latitude 19.086115, current temperature_2m 26.1, apparent_temperature 31.8, relative_humidity_2m 90, weather_code 3, pressure_msl 1010.4, wind_speed_10m 4.0, wind_direction_10m 342, time 2026-09-10T19:30) extended with an hourly precipitation array of 24 zeros ending 0.0 so the rainfall sum path is deterministic. Rainfall mapping sums the last 24 hourly precipitation steps, never current.precipitation alone. Missing visibility maps to None, never 0.0. Do not add caching, fallback, city map, or coverage doc in this plan — later plans own those. No new pip packages; httpx plus pytest only.</action>
  <acceptance_criteria>
    - tools/imd_client.py exists and imports without error.
    - tools/weather.py still exposes tool name get_current_weather and returns a JSON string.
    - tests/fixtures/open_meteo_mumbai.json parses as JSON and contains current.temperature_2m equal to 26.1.
    - get_current_weather.invoke with location Pune under MockTransport returns JSON whose temperature_c equals 26.1, wind_direction equals NNW, source contains open-meteo, and alert_level equals Green.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Exit code nonzero, or any test reaches api.open-meteo.com over the live network, or temperature_c differs from 26.1 for the fixture path.</fails_when>
  </verify>
  <done>One known city flows from tool invoke through Open-Meteo mapping to 14-key JSON end-to-end with mocked HTTP, committed.</done>
</task>

<task type="auto">
  <name>Mapping plus contract tests on the recorded fixture</name>
  <files>tests/test_imd_client.py</files>
  <read_first>
    - File modified: tests/test_imd_client.py (new file).
    - Source of truth: tests/test_weather_tool.py (frozen contract assertions that must keep passing: location echo, temperature_c, alert_level, source keys, tool name).
    - Source of truth: .planning/phases/02-imd-live-data/02-RESEARCH.md section Test Strategy (MockTransport pattern, six listed cases — this task covers mapping plus contract only).
  </read_first>
  <action>Create tests/test_imd_client.py with two tests using httpx.MockTransport serving tests/fixtures/open_meteo_mumbai.json: test_mapping asserts all 14 frozen keys present and Mumbai-sample values temperature_c 26.1, feels_like_c 31.8, humidity_pct 90, wind_kph 4.0, wind_direction NNW, pressure_hpa 1010.4, observed_at_utc 2026-09-10T19:30, alert_level Green, source containing open-meteo; test_contract_unchanged invokes get_current_weather for Mumbai and asserts the parsed location equals Mumbai and the tool name equals get_current_weather. Point the tool at the mock transport via dependency injection on the imd_client fetch function (module-level transport override or client factory parameter), never via live network. Leave cache, TTL, outage, key-missing tests to later plans. Reuse the proven Phase 1 verify command verbatim.</action>
  <acceptance_criteria>
    - tests/test_imd_client.py contains test_mapping and test_contract_unchanged and both pass.
    - Existing tests/test_weather_tool.py tests still pass unchanged.
    - No test performs a live HTTP call (suite passes with network disabled).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Exit code nonzero, or test_weather_tool.py fails, or suite duration suggests live network waits (any single test over 10s).</fails_when>
  </verify>
  <done>Mapping and frozen-contract tests green on the recorded fixture with mocked HTTP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Open-Meteo HTTPS JSON → imd_client mapper | Untrusted provider strings cross into tool JSON consumed by the agent prompt |
| User location string → upstream query params | Untrusted input reaches outbound HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | imd_client provider JSON parse | medium | mitigate | Defensive .get plus type checks on every provider field; unmapped WMO code yields Unknown string, never crash or eval |
| T-02-02 | Information disclosure | tool error paths | medium | mitigate | User-facing errors carry no WEATHER_API_KEY, full URLs, or tracebacks; reuse sanitize_detail pattern from api/routes.py |
| T-02-03 | Spoofing | alert_level derivation | high | mitigate | Conservative thresholds, default Green, source discloses non-IMD model data; never emit the phrase IMD-issued for derived levels |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan; httpx plus pytest already vendored so no legitimacy gate triggered |
</threat_model>

<verification>
Tracer slice verified by python -m pytest tests/ -q covering the new mapping tests plus the frozen contract tests. Full 3-criteria phase gate (real values, degraded under 5s, cache hits) completes in Plans 02-03.
</verification>

<success_criteria>
get_current_weather for a known city returns fixture-grounded live values (temperature_c 26.1) with open-meteo source disclosure, tool signature frozen, suite green.
</success_criteria>

<output>
Create `.planning/phases/02-imd-live-data/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- tools/imd_client.py (provider fetch plus mapping tables plus alert derivation)
- tools/weather.py (rewired body on the frozen seam)
- tests/fixtures/open_meteo_mumbai.json (recorded live sample)
- tests/test_imd_client.py (mapping plus contract tests)
