---
phase: 02-imd-live-data
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - tools/weather.py
  - tests/test_imd_client.py
  - .planning/phases/02-imd-live-data/COVERAGE.md
autonomous: true
requirements:
  - DATA-01
  - DATA-03
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "At least 15 major Indian cities resolve via the curated in-code map with normalised lookup (case and whitespace insensitive)"
    - "An unknown location returns a best-guess result that discloses the substitution instead of refusing or silently swapping"
    - "Empty WEATHER_API_KEY still returns the live keyless path, and no key material appears in responses or logs"
  artifacts:
    - tools/weather.py
    - tests/test_imd_client.py
    - .planning/phases/02-imd-live-data/COVERAGE.md
  key_links:
    - "Curated city map feeds the imd_client fetch lat-lon with normalisation"
    - "Best-guess disclosure flows through JSON fields into the agent reply note"
---

<objective>
Finish the phase: curated city map with normalised lookup, best-guess plus disclosure for unknowns, secret-safety for the key path — per D-02 (keyless-first, WEATHER_API_KEY required-semantics reserved), D-07 (curated in-code map), D-08 (best-guess plus disclose), plus the Phase 1 secret-safety carryover (sanitize and redact keys) and the api-coverage COVERAGE.md matrix. Covers DATA-01 city breadth and DATA-03 contract stability.

Purpose: Give the tool real geographic breadth without runtime geocoding, keep unknowns honest, prove keys never leak, and record API coverage for the phase gate.
Output: City map plus best-guess in tools/weather.py, key-missing plus secret-safety tests, COVERAGE.md, full suite green with coverage report.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-imd-live-data/02-CONTEXT.md
@.planning/phases/02-imd-live-data/02-RESEARCH.md
@.planning/phases/02-imd-live-data/02-01-SUMMARY.md
@.planning/phases/02-imd-live-data/02-02-SUMMARY.md
@tools/weather.py
@api/routes.py
</context>

<tasks>

<task type="auto">
  <name>Curated city map plus best-guess disclosure</name>
  <files>tools/weather.py, tests/test_imd_client.py</files>
  <read_first>
    - File modified: tools/weather.py (add curated map dict plus guess path; keep Plan 01 fetch/mapping and Plan 02 cache/fallback).
    - Source of truth: .planning/phases/02-imd-live-data/02-CONTEXT.md decisions D-07, D-08.
    - Source of truth: .planning/phases/02-imd-live-data/02-RESEARCH.md Open Questions item 2 (15-20 majors: metros plus state capitals plus Nashik and Pune) and geocode pattern.
  </read_first>
  <action>Per D-07, add a frozen curated map in tools/weather.py of exactly 18 Indian cities to lat-lon pairs: Mumbai 19.07283,72.88261; Delhi 28.6139,77.2090; Pune 18.5204,73.8567; Nashik 19.9975,73.7898; Chennai 13.0827,80.2707; Kolkata 22.5726,88.3639; Bengaluru 12.9716,77.5946; Hyderabad 17.3850,78.4867; Ahmedabad 23.0225,72.5714; Jaipur 26.9124,75.7873; Lucknow 26.8467,80.9462; Bhopal 23.2599,77.4126; Patna 25.5941,85.1376; Thiruvananthapuram 8.5241,76.9366; Kochi 9.9312,76.2673; Guwahati 26.1445,91.7362; Chandigarh 30.7333,76.7794; Srinagar 34.0837,74.7973. Lookup key is location.strip().lower() so Mumbai, mumbai with trailing space, and MUMBAI all resolve identically. Per D-08, unmappable input (empty after strip, unknown name, or raw lat-lon string that fails to parse as two floats in range) resolves to the nearest map entry by exact-match failure fallback to Mumbai 19.07283,72.88261 and sets three disclosure fields on the payload: requested_location echoing raw input, resolved_location set to Mumbai (best guess), and advisory prefixed with the literal Showing best-guess data for Mumbai instead of X. where X is the raw input. Never refuse and never substitute silently. Also cap location input at 120 chars before use per the Phase 1 schema carryover. Append two tests to tests/test_imd_client.py under MockTransport: test_city_normalisation asserting Mumbai with mixed case and surrounding whitespace yields location Mumbai with one transport hit pattern; test_best_guess asserting location Atlantis XYZ returns resolved_location Mumbai (best guess) plus advisory containing best-guess. Keep tool name, signature, 14 keys, source strings, and cache/fallback behavior untouched.</action>
  <acceptance_criteria>
    - tools/weather.py map contains exactly 18 entries including Nashik and Pune.
    - Invoke with MUMBAI with trailing space returns location Mumbai from the map path.
    - Invoke with Atlantis XYZ returns resolved_location Mumbai (best guess) and advisory containing the literal best-guess.
    - Unknown input never raises and never returns silently substituted data without the disclosure fields.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Exit code nonzero, or map has fewer than 18 entries, or unknown-location payload lacks resolved_location and the best-guess advisory.</fails_when>
  </verify>
  <done>Known cities resolve by normalised lookup and unknowns return disclosed best-guess data, tests green.</done>
</task>

<task type="auto">
  <name>Secret-safety plus key-missing path and COVERAGE.md gate</name>
  <files>tests/test_imd_client.py, .planning/phases/02-imd-live-data/COVERAGE.md</files>
  <read_first>
    - File modified: tests/test_imd_client.py (append key plus secrecy tests).
    - New file: .planning/phases/02-imd-live-data/COVERAGE.md (API capability matrix).
    - Source of truth: .planning/phases/02-imd-live-data/02-CONTEXT.md decisions D-02, D-06 and Phase 1 carryovers (sanitize/redact keys, pass-plus-report gate, threadpool/30s-retry/logging/request-id untouched).
    - Source of truth: api/routes.py function sanitize_detail (redaction pattern to mirror in assertions) and core/config.py Settings WEATHER_API_KEY default empty string.
  </read_first>
  <action>Add two tests to tests/test_imd_client.py. First test_key_missing_live_path: monkeypatch Settings WEATHER_API_KEY to empty string and clear the cache, then invoke for Mumbai under MockTransport and assert the live path still returns source containing open-meteo per D-02 (keyless-first; required-semantics reserved for the future IMD-direct cutover only). Second test_no_key_leak: set WEATHER_API_KEY to the literal SECRET_PROBE_42 cape, force a transport failure, then assert the fallback JSON string plus captured log output contain neither SECRET_PROBE_42 nor the WEATHER_API_KEY name-adjacent value, mirroring the sanitize_detail redaction pattern. Then create .planning/phases/02-imd-live-data/COVERAGE.md as a capability matrix with exactly two sections: INTEGRATE listing current weather via Open-Meteo forecast current endpoint behind the IMD-shaped seam with one-line scope note live values mapped to the 14-key contract; OPT-OUT listing day-wise forecast with reason deferred to Phase 3, alert color issuance with reason derived heuristic only and real IMD colors on gateway cutover, geocoding at runtime with reason frozen curated map per D-07, persistent or disk cache with reason forbidden by D-03, and agent prompt or route or schema changes with reason frozen by DATA-03 and D-06. Confirm Phase 1 carryovers untouched: threadpool offload, 30s retry, logging, request-id, error mapping. Finish with the proven Phase 1 full verify commands verbatim, first the plain suite then the coverage-report run as the pass-plus-report gate.</action>
  <acceptance_criteria>
    - test_key_missing_live_path passes with empty WEATHER_API_KEY returning open-meteo source.
    - test_no_key_leak passes with SECRET_PROBE_42 absent from JSON and logs.
    - .planning/phases/02-imd-live-data/COVERAGE.md exists with INTEGRATE and OPT-OUT sections, each OPT-OUT row carrying a one-line reason.
    - python -m pytest tests/ -q passes and python -m pytest tests/ -q --cov=. --cov-report=term-missing reports coverage without error.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Exit code nonzero, or key-missing invoke leaves the live path, or SECRET_PROBE_42 appears in any response or log line.</fails_when>
  </verify>
  <done>Key path proven keyless-safe, COVERAGE.md matrix written, full suite plus coverage report green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw location string → map lookup plus best-guess | Untrusted input selects coordinates and disclosure text |
| WEATHER_API_KEY value → logs plus error JSON | Secret adjacent to data-path error handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | city map lookup | low | mitigate | Normalised key lookup only; lat-lon parse validated as two in-range floats; unknown input falls to disclosed Mumbai guess, never attacker-chosen coordinates |
| T-02-08 | Information disclosure | WEATHER_API_KEY in data path | high | mitigate | Key read-and-ignored on the keyless path; fallback JSON and logs asserted free of key material by test_no_key_leak mirroring sanitize_detail |
| T-02-09 | Spoofing | best-guess substitution | medium | mitigate | requested_location plus resolved_location plus best-guess advisory make substitution visible end-to-end per D-08 |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan; no legitimacy gate triggered |
</threat_model>

<verification>
DATA-01 breadth via 18-city map plus normalisation and best-guess tests; DATA-03 via frozen tool name/signature/keys plus untouched agent/routes/schemas; secret-safety via key-missing and no-leak tests. Phase gate: python -m pytest tests/ -q followed by python -m pytest tests/ -q --cov=. --cov-report=term-missing, both green.
</verification>

<success_criteria>
18 curated cities resolve case-insensitively, unknowns disclose best-guess Mumbai, empty key still serves live data with no leak, COVERAGE.md matrix complete, full suite plus coverage report green.
</success_criteria>

<output>
Create `.planning/phases/02-imd-live-data/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- tools/weather.py (curated 18-city map plus best-guess disclosure)
- tests/test_imd_client.py (normalisation, best-guess, key-missing, secret-safety tests)
- .planning/phases/02-imd-live-data/COVERAGE.md (INTEGRATE versus OPT-OUT matrix with reasons)
