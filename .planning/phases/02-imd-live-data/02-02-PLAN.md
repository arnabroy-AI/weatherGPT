---
phase: 02-imd-live-data
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tools/weather.py
  - tools/imd_client.py
  - tests/test_imd_client.py
autonomous: true
requirements:
  - DATA-02
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Two identical get_current_weather queries within 10 minutes trigger exactly one upstream HTTP call (second is a cache hit)"
    - "An expired cache entry triggers a refetch on next query"
    - "When upstream fails, the tool still returns valid mock-shaped JSON stamped as fallback within 5 seconds, disclosing degraded data"
  artifacts:
    - tools/weather.py
    - tests/test_imd_client.py
  key_links:
    - "Cache dict keyed by normalised location guards the imd_client fetch call"
    - "Outage branch returns fallback JSON that still satisfies the 14-key contract"
---

<objective>
Add the 10-minute in-memory TTL cache and the outage fallback with staleness disclosure — per D-03 (in-memory 10-min TTL keyed by normalised location), D-04 (outage returns mock-shaped payload stamped as fallback), D-05 (degraded data always disclosed), and DATA-02. Builds on the Plan 01 tracer slice; stale strategy is stale-only-on-error per RESEARCH.md recommendation under the agent's discretion.

Purpose: Make the demo resilient — repeated queries avoid duplicate upstream calls and a dead provider still yields a fast degraded answer.
Output: TTL cache plus fallback stamping in tools/weather.py, cache/TTL/outage tests green.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-imd-live-data/02-CONTEXT.md
@.planning/phases/02-imd-live-data/02-RESEARCH.md
@.planning/phases/02-imd-live-data/02-01-SUMMARY.md
@tools/weather.py
@tools/imd_client.py
</context>

<tasks>

<task type="auto">
  <name>10-minute TTL cache keyed by normalised location</name>
  <files>tools/weather.py, tests/test_imd_client.py</files>
  <read_first>
    - File modified: tools/weather.py (add module-level cache dict plus TTL constant; keep Plan 01 fetch plus mapping flow).
    - Source of truth: .planning/phases/02-imd-live-data/02-RESEARCH.md section Caching Guidance (600s TTL, key equals location.strip().lower(), threading.Lock, never cache error payloads as fresh).
    - Source of truth: .planning/phases/02-imd-live-data/02-CONTEXT.md decisions D-03, D-05.
  </read_first>
  <action>Per D-03, add a process-local cache in tools/weather.py: module-level dict mapping normalised location key (location.strip().lower()) to a tuple of expiry monotonic timestamp plus payload dict, TTL constant 600 seconds, guarded by a threading.Lock for the Phase 1 threadpool offload. On a cache hit with unexpired entry, return the cached payload serialised to JSON with two disclosure fields added: cached boolean true and cache_age_s integer seconds since store. On miss or expiry, fetch via imd_client, store the fresh payload with stored_at monotonic timestamp, and return it with cached false and cache_age_s 0. Never store fallback or error payloads as fresh entries. Stale strategy is stale-only-on-error (kept simple per discretion; the error branch in the next task may serve an expired entry stamped with age). Append test_cache_hit to tests/test_imd_client.py: two identical invokes for Pune under a counting MockTransport assert exactly one transport hit and second payload cached true; plus test_ttl_expiry: pre-seed an expired entry for Pune then invoke and assert a refetch occurs (transport hit count one) and returned cached false. Do not change the 14 frozen keys, tool name, or source strings from Plan 01.</action>
  <acceptance_criteria>
    - tools/weather.py defines a TTL constant equal to 600 and a threading.Lock guarding the cache dict.
    - test_cache_hit passes with transport hit count exactly 1 for two identical invokes.
    - test_ttl_expiry passes with a refetch on expired entry.
    - Fresh payload carries cached false and cache_age_s 0; hit payload carries cached true and cache_age_s as a non-negative integer.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Exit code nonzero, or transport hit count is 2 for identical back-to-back invokes, or expired entry is served without refetch.</fails_when>
  </verify>
  <done>Repeat queries within TTL hit the cache and expired entries refetch, all tests green.</done>
</task>

<task type="auto">
  <name>Outage fallback with stamped source and disclosure</name>
  <files>tools/weather.py, tests/test_imd_client.py</files>
  <read_first>
    - File modified: tools/weather.py (add try/except around the imd_client fetch; keep Plan 01 mapping plus Plan 02 task-1 cache).
    - Source of truth: .planning/phases/02-imd-live-data/02-CONTEXT.md decisions D-04, D-05, D-06 (fallback is data-path only; LLM errors stay fail-loud, routes unchanged).
    - Source of truth: .planning/phases/02-imd-live-data/02-RESEARCH.md sections Outage behavior and Test Strategy case 4 (transport raises httpx.ConnectError, assert fallback source stamp plus valid JSON).
  </read_first>
  <action>Per D-04 and D-05, wrap the upstream fetch in tools/weather.py with exception handling for httpx.HTTPError, TimeoutException, and ValueError/KeyError/TypeError from schema mismatch: on failure, if an expired cache entry exists for the normalised key, serve it stamped with stale true plus cache_age_s plus source suffixed with the literal +stale-fallback; otherwise return the mock-shaped 14-key payload with deterministic placeholder values (temperature_c 29.5, condition Light rain unavailable, alert_level Yellow) and source set to the literal mock-imd-fallback plus stale true plus cached false. In both branches the advisory field appends the literal sentence Note: live data unavailable; showing fallback values. which flows into the agent reply per D-05. Log the full provider exception server-side via logging with no key material; user-facing JSON never contains WEATHER_API_KEY, full URLs, or tracebacks. Do not touch services/agent.py, api/routes.py, or the LLM 30s-retry path per D-06. Append test_outage_fallback to tests/test_imd_client.py: MockTransport raising httpx.ConnectError, assert invoke returns parseable JSON with source mock-imd-fallback, all 14 keys present, advisory containing live data unavailable, and wall time under 5 seconds; plus test_stale_on_error: seed an expired Pune entry, fail the transport, assert the served payload carries stale true. Reuse the proven Phase 1 verify command verbatim.</action>
  <acceptance_criteria>
    - Outage invoke returns valid JSON with source exactly mock-imd-fallback and advisory containing the literal live data unavailable.
    - Outage invoke completes in under 5 seconds wall time.
    - Stale-entry outage serves expired data stamped stale true instead of the generic mock.
    - No fallback payload is stored as a fresh cache entry (a later healthy invoke still refetches).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Exit code nonzero, or fallback source differs from mock-imd-fallback, or advisory lacks the disclosure sentence, or outage path exceeds 5 seconds.</fails_when>
  </verify>
  <done>Provider outage yields a fast stamped disclosed fallback that satisfies the contract, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Expired cache entry → user response | Stale data crosses into agent grounding under failure |
| Provider exception → fallback JSON | Error path output reaches the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | TTL cache dict | medium | mitigate | Key strictly by normalised location; lock-guarded; error payloads never stored as fresh so failures cannot pin the cache |
| T-02-05 | Information disclosure | outage log plus fallback JSON | medium | mitigate | Log full traceback server-side only; fallback JSON carries source plus age, never keys, URLs, or tracebacks |
| T-02-06 | Denial of service | upstream timeout budget | low | mitigate | 6.0s httpx timeout keeps the degraded answer under the 5s success criterion including the cache path |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan; no legitimacy gate triggered |
</threat_model>

<verification>
DATA-02 verified by test_cache_hit (one transport call for two invokes), test_ttl_expiry (refetch on expiry), test_outage_fallback plus test_stale_on_error (stamped disclosed fallback under 5s). Full suite via python -m pytest tests/ -q.
</verification>

<success_criteria>
Repeated identical queries hit cache within TTL, expired entries refetch, and a killed provider still returns disclosed fallback JSON in under 5 seconds with the suite green.
</success_criteria>

<output>
Create `.planning/phases/02-imd-live-data/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- tools/weather.py (TTL cache plus stale-only-on-error plus fallback stamping)
- tests/test_imd_client.py (cache, TTL, outage, stale tests)
