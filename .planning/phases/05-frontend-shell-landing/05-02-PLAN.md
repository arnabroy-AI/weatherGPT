---
phase: 05-frontend-shell-landing
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - frontend/components/features.tsx
  - frontend/components/live-demo-teaser.tsx
  - frontend/components/alerts-showcase.tsx
  - frontend/components/how-it-works.tsx
  - frontend/components/ui/card.tsx
  - frontend/components/ui/separator.tsx
requirements:
  - FRNT-01
  - FRNT-02
autonomous: true
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Features, live-demo teaser, alerts showcase, and how-it-works sections render real WeatherGPT copy with zero lorem"
    - "Alert chips show Green Yellow Orange Red wording matching the backend alert_level contract"
    - "Sections use glass cards and shadcn primitives consistently with the plan-01 token system"
    - "Teaser chat CTA is disabled with a Phase 6 caption, never a dead link"
  artifacts:
    - "frontend/components/features.tsx"
    - "frontend/components/live-demo-teaser.tsx"
    - "frontend/components/alerts-showcase.tsx"
    - "frontend/components/how-it-works.tsx"
  key_links:
    - "section components -> glass token classes from globals.css -> Card/Badge primitives"
    - "alerts-showcase badges -> backend Alert: Green/Yellow/Orange/Red wording contract"
    - "teaser disabled CTA -> /chat reservation (no route created)"
---

<objective>
Expansion slice 1 per D-03/D-04/D-07: build the four middle landing sections (features, live-demo teaser, alerts showcase, how-it-works) with exact UI-SPEC copy and glass Card plus Badge treatment, verified by build plus copy grep gates.

Purpose: Deliver half the 8-section landing with honest data-source wording the chat badge will mirror in Phase 6.
Output: Four section components with real copy, ready for plan-03 page assembly.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-frontend-shell-landing/05-CONTEXT.md
@.planning/phases/05-frontend-shell-landing/05-UI-SPEC.md
@.planning/phases/05-frontend-shell-landing/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Features grid plus how-it-works steps with real copy</name>
  <files>frontend/components/features.tsx, frontend/components/how-it-works.tsx, frontend/components/ui/card.tsx, frontend/components/ui/separator.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Copy deck items 2 Features six cards and 5 How-it-works three steps, Layout Per Section rows 2 and 5, Spacing Scale)
    - .planning/phases/05-frontend-shell-landing/05-01-SUMMARY.md (token class names and Card usage established in tracer)
  </read_first>
  <action>Build two sections per D-03/D-04 using stock Card and Separator plus lucide icons only. Features section id features: section head with eyebrow kicker plus heading plus sub, then grid 1col mobile / 2col sm / 3col lg of 6 equal glass Cards carrying the exact six UI-SPEC bodies (ask-like-you-talk, alerts at a glance, 5-day outlooks, farm advice paddy wheat cotton sugarcane maize soybean, asks-when-unsure, honest-on-failure), each with distinct lucide icon, title, and two-line body in Inter 16px. How-it-works section id how-it-works: 3 Cards in 1col / 3col md with numbered steps 01 You ask (Rain in Delhi tomorrow), 02 Tools ground it (values quoted never invented), 03 You get answer plus alert (Alert Green-to-Red line plus 1-2 line advisory), with connecting Separator on desktop. Section vertical padding 96px desktop / 64px mobile inside max-w-6xl container. No lorem, no clipart, no gradient per D-07; every card carries WeatherGPT-specific voice.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>build exits nonzero or features/how-it-works components fail typecheck</fails_when>
  </verify>
  <acceptance_criteria>
    - frontend/components/features.tsx contains all six strings: Ask like you talk, Alerts you can read, 5-day outlooks, Farm advice grounded, asks when unsure variant, Honest when data fails variant
    - frontend/components/how-it-works.tsx contains You ask, Tools ground it, You get answer plus alert variant and ids how-it-works
    - Both files use Card from stock shadcn import path and glass token classes, lucide icons only, zero img tags
    - npm run build passes with zero type errors
  </acceptance_criteria>
  <done>Features and how-it-works render six plus three real-copy cards in responsive glass grids.</done>
</task>

<task type="auto">
  <name>Task 2: Live-demo teaser with seeded queries plus alerts showcase with four levels</name>
  <files>frontend/components/live-demo-teaser.tsx, frontend/components/alerts-showcase.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Copy deck items 3 teaser with three seeded queries and honesty note, 4 alerts showcase four cards plus disclaimer, Env contract on disabled CTA)
    - .planning/phases/05-frontend-shell-landing/05-CONTEXT.md (D-04 real copy, D-07 ban list, /chat reserved deferred idea)
  </read_first>
  <action>Build two sections per D-03/D-04/D-07. Live-demo teaser section id live-demo: larger glass panel blur 16px with heading See what an answer looks like, sub Real reply shapes, three seeded-query Cards stacked mobile / 3col lg: query Current weather in Pune with reply 33.2C Partly cloudy Alert Green, query Mumbai this weekend with day-wise highs plus Alert Orange (Sat) plus 2-line advisory, query Paddy sowing advice for Nashik with seedling plus drainage note; honesty note Sample values shown plus disabled button Open the chat with caption Arrives in Phase 6 rendered as a disabled button, never an anchor to a missing route. Alerts showcase section id alerts: heading One line that tells you how worried to be, sub Four levels, four Cards 1col / 2col sm / 4col lg each headed by its Badge (Green No severe weather expected, Yellow Carry an umbrella, Orange Avoid unnecessary travel, Red Avoid travel and stay indoors) with advisory text, each Badge showing its level-name text (never color alone), plus disclaimer Showcase advisories illustrate severity wording. Live advisories are estimates never IMD-issued warnings.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>build exits nonzero or teaser/showcase components fail typecheck</fails_when>
  </verify>
  <acceptance_criteria>
    - frontend/components/live-demo-teaser.tsx contains See what an answer looks like, Current weather in Pune, Mumbai this weekend, Paddy sowing advice for Nashik, a disabled attribute on the chat button, and the string Arrives in Phase 6
    - frontend/components/alerts-showcase.tsx contains all four level names Green Yellow Orange Red plus Alert: wording and the disclaimer string never described as IMD-issued warnings variant
    - No anchor with href /chat exists in either file (grep href="/chat" returns zero matches)
    - npm run build passes with zero type errors
  </acceptance_criteria>
  <done>Teaser and alerts sections render honest seeded copy with disabled CTA and four data-color badges.</done>
</task>

<task type="auto">
  <name>Task 3: Anti-slop copy gate on the four new sections</name>
  <files>frontend/components/features.tsx, frontend/components/live-demo-teaser.tsx, frontend/components/alerts-showcase.tsx, frontend/components/how-it-works.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Anti-Slop Checklist first four items, Accessibility notes on badges never color alone)
    - .planning/phases/05-frontend-shell-landing/05-CONTEXT.md (D-07 ban list plus visual review checklist)
  </read_first>
  <action>Enforce D-07 on the four plan-02 sections only (plan-03 gates the full page): run case-insensitive greps for lorem, robot/clipart image markers, and purple-blue gradient strings (purple, from-purple, via-blue, linear-gradient with purple/blue) scoped to the four section files plus shared globals.css, and confirm every Card carries WeatherGPT-specific copy by checking for domain strings (IMD, Alert, monsoon or crop or district or forecast) in each file. Fix any hit by replacing with UI-SPEC copy or solid teal/slate treatments before finishing. Do not add images; keep lucide icons and CSS shapes only.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>build exits nonzero after anti-slop fixes</fails_when>
  </verify>
  <acceptance_criteria>
    - Case-insensitive grep for lorem across the four section files returns zero matches
    - Grep for from-purple, via-blue, purple-500, blue-500 gradient markers across frontend/components and frontend/app/globals.css returns zero matches
    - Zero img tags across the four section files
    - Each of the four files contains at least two of the domain strings IMD, Alert, monsoon, forecast, crop, district
    - npm run build passes with zero type errors
  </acceptance_criteria>
  <done>Four sections pass the scoped anti-slop gate and remain build-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->static-sections | Teaser and showcase render fixed copy; no user input or fetch in this plan |
| copy-trust | Advisory wording must not impersonate official IMD warnings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Spoofing | alerts-showcase advisories | high | mitigate | Ship the UI-SPEC disclaimer verbatim; describe live advisories as derived estimates, never IMD-issued |
| T-05-02-02 | Tampering | teaser seeded values | medium | mitigate | Honesty note labels values as samples; live answers quote tool values only (Phase 6 wiring) |
| T-05-02-SC | Tampering | npm installs | high | mitigate | No new third-party packages in this plan; stock shadcn Card/Separator only |
</threat_model>

<verification>
- npm run build in frontend/ passes with zero type errors
- Grep href="/chat" across frontend/ returns zero matches (route still reserved, no dead link)
- Scoped anti-slop greps (lorem, purple/blue gradient markers, img tags) clean on the four sections
- Each section file carries WeatherGPT-specific domain copy
</verification>

<success_criteria>
- Four middle sections render exact UI-SPEC copy in glass Cards with responsive grids
- Alert badges mirror the backend Green/Yellow/Orange/Red contract with text labels
- Teaser CTA disabled with Phase 6 caption; build gate green
</success_criteria>

<output>
Create `.planning/phases/05-frontend-shell-landing/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- frontend/components/features.tsx (6-card glass grid, id features)
- frontend/components/live-demo-teaser.tsx (seeded-query panel, id live-demo, disabled Phase 6 CTA)
- frontend/components/alerts-showcase.tsx (4-level badges, id alerts, disclaimer)
- frontend/components/how-it-works.tsx (3-step cards, id how-it-works)
- frontend/components/ui/card.tsx, frontend/components/ui/separator.tsx (stock shadcn additions)
- .planning/phases/05-frontend-shell-landing/05-02-SUMMARY.md
