---
phase: 05-frontend-shell-landing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.mjs
  - frontend/tailwind.config.ts
  - frontend/tsconfig.json
  - frontend/components.json
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/app/globals.css
  - frontend/lib/utils.ts
  - frontend/components/ui/button.tsx
  - frontend/components/ui/badge.tsx
  - frontend/components/header.tsx
  - frontend/components/hero.tsx
  - frontend/.env.local.example
requirements:
  - FRNT-01
  - FRNT-02
autonomous: true
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Running npm run build inside frontend/ succeeds with zero type errors"
    - "Landing root route renders sticky glass header plus hero section from the token system"
    - "Teal plus amber on slate glass tokens and Space Grotesk plus Inter fonts load in light and dark"
    - "/chat route is reserved and no chat UI exists yet"
  artifacts:
    - "frontend/app/layout.tsx"
    - "frontend/app/page.tsx"
    - "frontend/app/globals.css"
    - "frontend/components/header.tsx"
    - "frontend/components/hero.tsx"
  key_links:
    - "layout.tsx -> globals.css theme variables -> header/hero glass classes"
    - "next/font (Space Grotesk + Inter) -> layout font variables -> hero display type"
    - "NEXT_PUBLIC_WEATHERGPT_API reserved in .env.local.example for Phase 6"
---

<objective>
Tracer slice per D-01/D-02/D-05/D-06: scaffold greenfield frontend/ (Next.js 14 App Router + TS + Tailwind + stock shadcn), wire the glassmorphism token system with light/dark, and render ONE real section (hero + header shell) end-to-end, verified by npm run build with zero type errors.

Purpose: Prove the scaffold, theme, font, and build pipeline on first commit so the 7 expansion sections build on a proven slice.
Output: Working frontend/ shell that boots, renders header + hero with real UI-SPEC copy, and passes the build gate.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-frontend-shell-landing/05-CONTEXT.md
@.planning/phases/05-frontend-shell-landing/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify toolchain then scaffold frontend/ stock Next.js + shadcn shell</name>
  <files>frontend/package.json, frontend/next.config.mjs, frontend/tailwind.config.ts, frontend/tsconfig.json, frontend/components.json, frontend/lib/utils.ts, frontend/.env.local.example</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-CONTEXT.md (D-01 stock stack in frontend/, D-02 stock shadcn layout, NEXT_PUBLIC_WEATHERGPT_API reservation, /chat reserved)
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Init contract section, Component Inventory provenance note, Registry Safety stock-only rule)
  </read_first>
  <action>First record node and npm versions per environment notes, then scaffold per D-01/D-02: create Next.js 14 App Router TypeScript Tailwind app rooted at frontend/ (app/ router, no src/ dir, no custom-minimal variant). Run the stock shadcn init inside frontend/ to produce components/ui, lib/utils with clsx + tailwind-merge + cva, and CSS-variable theme in globals.css with class-based dark mode. Install only the two tracer primitives button and badge from the stock registry plus their Radix peers (Slot, no third-party registries). If npx network init is blocked, hand-scaffold the identical stock layout (components.json with slate base, cssVariables true) and record the fallback in the summary. Create frontend/.env.local.example containing exactly NEXT_PUBLIC_WEATHERGPT_API=http://localhost:8000 with a comment that Phase 6 consumes it. Do not create any /chat route or chat component (reserved per CONTEXT deferred ideas). Do not touch any backend Python file.</action>
  <verify>
    <automated>node --version; npm --version</automated>
    <fails_when>node or npm binary missing or version lines absent from output</fails_when>
  </verify>
  <acceptance_criteria>
    - frontend/package.json exists with next 14, react, typescript, tailwindcss, lucide-react present
    - frontend/components.json exists with stock slate-base CSS-variable config
    - frontend/lib/utils.ts exists exporting cn helper
    - frontend/.env.local.example contains the exact string NEXT_PUBLIC_WEATHERGPT_API
    - No frontend/app/chat directory and no file matching frontend/**/chat* exists
  </acceptance_criteria>
  <done>Toolchain versions recorded; stock scaffold present with shadcn layout and reserved env name; zero chat files.</done>
</task>

<task type="auto">
  <name>Task 2: Token system plus fonts plus theme provider (D-05/D-06 per UI-SPEC)</name>
  <files>frontend/app/globals.css, frontend/app/layout.tsx, frontend/components/theme-toggle.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Color table, Glassmorphism tokens table, Typography section, Accessibility notes on focus ring and reduced motion)
    - .planning/phases/05-frontend-shell-landing/05-CONTEXT.md (D-05 teal plus amber on slate glass light plus dark, D-06 Grotesk plus Inter via next/font)
  </read_first>
  <action>Implement the UI-SPEC token tables exactly per D-05/D-06: page background slate-50 light and slate-950 dark; glass fill white 0.70 light and slate-900 0.60 dark; glass borders, blur scale (cards backdrop-blur-md, header backdrop-blur-sm, hero panel backdrop-blur-lg), radius cards rounded-2xl buttons 10px chips full, light shadow 0 8px 32px rgba(15,23,42,0.08) and dark 0 8px 32px rgba(0,0,0,0.40); hero-only teal radial wash rgba(15,118,103,0.08) light and rgba(45,212,191,0.06) dark with solid slate surfaces elsewhere and no purple-blue gradient. Accent deep teal #0f7667 (dark #2dd4bf) reserved for primary CTA fill, links, focus-visible rings, active nav; amber #d97706 (dark #fbbf24) reserved for eyebrow kickers, rating row, MoES strip top border, SIH badge border. Load Space Grotesk 600 for display and Inter 400+600 for body via next/font with display swap and system fallback stack; hero H1 40px desktop clamped 32px under 640px tracking -0.02em; section titles Space Grotesk 24px tracking -0.01em; kickers Inter 14px 600 uppercase tracking +0.08em. Wire class-based dark mode with a Light/Dark/System switcher defaulting to prefers-color-scheme, 2px teal focus-visible rings, skip-to-content link, and prefers-reduced-motion disabling the wash animation. Component boundaries and exact blur values are the agent's discretion per CONTEXT.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>build exits nonzero or output contains Failed to compile or type error lines</fails_when>
  </verify>
  <acceptance_criteria>
    - frontend/app/globals.css contains #0f7667 and #d97706 and backdrop-blur utilities and no linear-gradient purple-blue string
    - frontend/app/layout.tsx references Space Grotesk and Inter via next/font and renders a theme provider plus skip link
    - frontend/components/theme-toggle.tsx offers Light, Dark, System options
    - npm run build passes with zero type errors
  </acceptance_criteria>
  <done>Token system, fonts, and theme switching live in both modes; build gate green.</done>
</task>

<task type="tracer">
  <name>Task 3: Tracer header plus hero with real copy, one path end-to-end</name>
  <files>frontend/components/header.tsx, frontend/components/hero.tsx, frontend/app/page.tsx, frontend/components/ui/button.tsx, frontend/components/ui/badge.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Copywriting Contract header item and hero item, Layout Per Section rows 0-1, Spacing Scale container max-w-6xl rule)
    - .planning/phases/05-frontend-shell-landing/05-CONTEXT.md (D-03 section order, D-04 real copy zero lorem, D-07 ban list)
  </read_first>
  <action>Wire ONE entry point through every layer per D-03/D-04: sticky glass header (wordmark WeatherGPT, links Features Live demo Alerts How it works FAQ, theme switcher, CTA Try a sample question, hamburger to Sheet drawer under 768px — Sheet may be a minimal local drawer in this tracer since only button+badge shadcn primitives are installed) plus hero section id top with the exact UI-SPEC copy per D-04: eyebrow BUILT FOR SIH26068, H1 Ask the weather in plain words. Get answers India can act on. as the single page H1, sub, CTAs Try a sample question scrolling to #live-demo and See how it works scrolling to #how-it-works, microcopy No sign-up, metric row 18 cities mapped, trust line Current engine: keyless model data. Page container max-w-6xl with 16px mobile and 24px desktop gutters and 96px desktop / 64px mobile section rhythm; CTA row stacks full-width on 390px mobile. Use only lucide icons and CSS shapes, no images, no lorem, no purple-blue gradient, per D-07. page.tsx in this tracer renders header plus hero only; the remaining 7 sections arrive in plans 02-03.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>build exits nonzero or compiled page count is zero</fails_when>
  </verify>
  <acceptance_criteria>
    - frontend/app/page.tsx contains id top hero H1 string Ask the weather in plain words and anchor href #live-demo
    - frontend/components/header.tsx contains wordmark WeatherGPT and a theme control and a mobile drawer trigger
    - frontend/components/hero.tsx contains SIH26068 eyebrow and 18 cities mapped metric text
    - Case-insensitive grep for lorem across frontend/app and frontend/components returns zero matches
    - npm run build passes with zero type errors
  </acceptance_criteria>
  <done>Single happy path (root route renders header plus hero from tokens) works end-to-end and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->static-landing | Untrusted visitor input limited to nav/theme controls; no forms or data intake in this plan |
| build-supply-chain | npm/npx fetched packages cross into the repo during scaffold |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering | npm/shadcn installs | high | mitigate | Stock registry only, no third-party registries; pin versions, review package.json diff before commit |
| T-05-01-02 | Spoofing | MoES/IMD strip copy (future) | medium | mitigate | This plan ships no MoES strip; hero trust line states model-data engine honestly, no endorsement claims or logos |
| T-05-01-SC | Tampering | npm installs | high | mitigate | Package-legitimacy gate: verify next, react, tailwindcss, lucide-react on npmjs.com before install completes |
</threat_model>

<verification>
- npm run build in frontend/ passes with zero type errors (the Phase 5 gate)
- node --version and npm --version recorded in plan 01 summary
- Case-insensitive grep for lorem across frontend/app and frontend/components is clean
- No frontend/app/chat route and no chat component exists (reserved for Phase 6)
- Backend Python tree untouched (git status shows no .py modifications)
</verification>

<success_criteria>
- Root route renders sticky glass header plus hero with exact UI-SPEC copy in light and dark scaffolding
- Token system (teal plus amber on slate glass) and Grotesk plus Inter fonts wired
- Build gate green; env name NEXT_PUBLIC_WEATHERGPT_API reserved; /chat still free
</success_criteria>

<output>
Create `.planning/phases/05-frontend-shell-landing/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- frontend/ scaffold: package.json, next.config.mjs, tailwind.config.ts, tsconfig.json, components.json, lib/utils.ts, .env.local.example (NEXT_PUBLIC_WEATHERGPT_API placeholder)
- frontend/app/layout.tsx (fonts, theme provider, skip link), frontend/app/globals.css (glassmorphism + color + type tokens), frontend/app/page.tsx (tracer: header + hero only)
- frontend/components/ui/button.tsx, frontend/components/ui/badge.tsx (stock shadcn primitives)
- frontend/components/header.tsx, frontend/components/hero.tsx, frontend/components/theme-toggle.tsx
- .planning/phases/05-frontend-shell-landing/05-01-SUMMARY.md (toolchain versions, scaffold method, build result)
