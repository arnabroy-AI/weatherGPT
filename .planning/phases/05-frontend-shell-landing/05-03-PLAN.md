---
phase: 05-frontend-shell-landing
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - frontend/components/moes-strip.tsx
  - frontend/components/faq.tsx
  - frontend/components/footer.tsx
  - frontend/app/page.tsx
  - frontend/components/ui/accordion.tsx
  - frontend/components/ui/sheet.tsx
  - frontend/components/ui/dropdown-menu.tsx
requirements:
  - FRNT-01
  - FRNT-02
autonomous: true
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "All 8 landing sections render in UI-SPEC order with real copy and zero lorem"
    - "Light and dark modes render consistently with responsive 390px mobile layout and working drawer nav"
    - "Full-page anti-slop checklist passes including no purple-blue gradient and no clipart"
    - "npm run build passes with zero type errors and backend pytest stays green"
  artifacts:
    - "frontend/components/moes-strip.tsx"
    - "frontend/components/faq.tsx"
    - "frontend/components/footer.tsx"
    - "frontend/app/page.tsx"
  key_links:
    - "page.tsx assembles header + hero + 4 plan-02 sections + moes-strip + faq + footer in D-03 order"
    - "Accordion FAQ + Sheet drawer + DropdownMenu theme switch wired to Radix stock primitives"
    - "frontend build gate + backend pytest regression guard both green"
---

<objective>
Final assembly per D-03/D-04/D-05/D-06/D-07: build MoES strip, FAQ, and footer, assemble all 8 sections in order on page.tsx, and run the full verification gate (anti-slop checklist, light/dark plus 390px responsive checks, frontend build, backend pytest guard).

Purpose: Close FRNT-01/FRNT-02 with a shippable landing that meets all 4 ROADMAP success criteria.
Output: Complete 8-section landing, verified end-to-end with both suites green.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-frontend-shell-landing/05-CONTEXT.md
@.planning/phases/05-frontend-shell-landing/05-UI-SPEC.md
@.planning/phases/05-frontend-shell-landing/05-01-SUMMARY.md
@.planning/phases/05-frontend-shell-landing/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MoES strip plus FAQ plus footer with real copy</name>
  <files>frontend/components/moes-strip.tsx, frontend/components/faq.tsx, frontend/components/footer.tsx, frontend/components/ui/accordion.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Copy deck items 6 MoES strip, 7 FAQ six questions, 8 footer columns plus bottom bar; Layout Per Section rows 6-8)
    - .planning/phases/05-frontend-shell-landing/05-CONTEXT.md (D-03 order, D-04 zero lorem, D-07 ban list; agent discretion on component boundaries)
  </read_first>
  <action>Build the final three sections per D-03/D-04/D-07 with stock Accordion, Separator, and Badge. MoES strip: full-bleed slate band with 2px amber top border, centered two-line statement Built for the Ministry of Earth Sciences (SIH26068) plus data-roadmap sentence keyless model engine today with honest disclosure then IMD-direct next, no logos and no fake endorsement claims. FAQ section id faq: narrow max-w-3xl single-open collapsible Accordion with exactly the six UI-SPEC questions (data source, outage fallback under 5 seconds, 18 mapped cities plus best-guess disclosure, six crops farm use, English-first v1 Hindi v2, POST /api/chat plus GET /health API), keyboard-native via Radix. Footer: three link columns Product (Features, Live demo, Alerts, How it works), Project (SIH26068, MoES track, API docs anchor, Status v1 demo), Fine print (Demo build model estimates not official IMD warnings), Separator above bottom bar with 2026 WeatherGPT SIH26068 plus English-first No accounts in v1. All body copy Inter 16px, no lorem, no clipart, amber only for kickers and borders per token rules.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>build exits nonzero or accordion/faq/footer components fail typecheck</fails_when>
  </verify>
  <acceptance_criteria>
    - frontend/components/moes-strip.tsx contains Ministry of Earth Sciences and SIH26068 and amber top-border class, zero img or logo tags
    - frontend/components/faq.tsx contains six accordion items including Where does the weather data come from and Hindi or other languages variant plus single-open collapsible props
    - frontend/components/footer.tsx contains Product, Project, Fine print columns plus 2026 WeatherGPT and not official IMD warnings text
    - npm run build passes with zero type errors
  </acceptance_criteria>
  <done>MoES strip, FAQ, and footer render exact UI-SPEC copy with stock primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Full page assembly in D-03 order plus nav primitives</name>
  <files>frontend/app/page.tsx, frontend/components/ui/sheet.tsx, frontend/components/ui/dropdown-menu.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Layout Per Section order fixed hero-features-teaser-alerts-how-MOES-FAQ-footer, Responsive Breakpoints md 768 nav switch, Accessibility Notes landmarks plus single H1)
    - .planning/phases/05-frontend-shell-landing/05-01-SUMMARY.md (header/hero exports to compose)
    - .planning/phases/05-frontend-shell-landing/05-02-SUMMARY.md (four middle-section exports to compose)
  </read_first>
  <action>Assemble frontend/app/page.tsx importing header, hero, features, live-demo-teaser, alerts-showcase, how-it-works, moes-strip, faq, footer in exactly that D-03 order inside header/main/footer landmarks with aria-labelledby on sections and a single page H1 from hero. Install stock Sheet and DropdownMenu to back the header mobile drawer (hamburger under 768px, links at 768px and up) and the Light/Dark/System theme switcher with aria-pressed state; grids follow UI-SPEC (features 1-2-3, teaser 1-3, alerts 1-2-4, steps 1-3), hero display 32px under 640px else 40px, CTA rows stack full-width at 390px with no horizontal overflow, all icon-only controls minimum 44px touch targets. Keep /chat uncreated and teaser CTA disabled; Sheet and DropdownMenu come from the stock registry only.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>build exits nonzero or page fails to compile all eight sections</fails_when>
  </verify>
  <acceptance_criteria>
    - frontend/app/page.tsx imports all eight section components in D-03 order and renders ids top, features, live-demo, alerts, how-it-works, faq in that document order
    - Exactly one h1 tag exists across frontend/app/page.tsx plus hero component
    - Sheet and DropdownMenu stock components exist under frontend/components/ui
    - Grep href="/chat" across frontend/ returns zero matches and no frontend/app/chat directory exists
    - npm run build passes with zero type errors
  </acceptance_criteria>
  <done>Complete landing assembles all 8 sections in order with working responsive nav and theme controls.</done>
</task>

<task type="auto">
  <name>Task 3: Full verification gate — anti-slop, responsive, build, backend guard</name>
  <files>frontend/app/page.tsx, frontend/app/globals.css</files>
  <read_first>
    - .planning/phases/05-frontend-shell-landing/05-UI-SPEC.md (Anti-Slop Checklist seven items, Responsive Breakpoints 390px rules, Accessibility Notes contrast and reduced motion)
    - .planning/phases/05-frontend-shell-landing/05-CONTEXT.md (D-07 ban list plus visual checklist, backend untouched constraint, npm run build gate)
  </read_first>
  <action>Run the D-07 verification gate over the whole frontend plus the backend guard without modifying Python code: case-insensitive lorem grep clean across frontend/, gradient-marker grep (from-purple, via-blue, purple-500/blue-500 combos) clean with only the teal radial wash allowed, zero img/robot/clipart tags with lucide plus CSS shapes only, every card carrying domain copy (IMD, Alert, monsoon, forecast, crop, or district), single-H1 plus focus-visible plus reduced-motion assertions in code, 390px rules (stacked CTAs, 1col grids, drawer nav) confirmed by class assertions (flex-col/grid-cols-1 under breakpoints, Sheet trigger present), light/dark token pairs present in globals.css, then npm run build zero type errors and finally the backend regression python -m pytest tests/ -q from repo root to prove Python stayed untouched and green. Record any check that needs human eyes (light/dark render, 390px overlap) as a checklist result in the summary with file and content evidence.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>build exits nonzero or any anti-slop grep returns a match</fails_when>
  </verify>
  <acceptance_criteria>
    - Case-insensitive grep for lorem across frontend/ returns zero matches
    - Grep for from-purple, via-blue, purple- gradient markers across frontend/ returns zero matches
    - Zero img tags across frontend/components and frontend/app
    - npm run build passes with zero type errors
  </acceptance_criteria>
  <done>Full anti-slop checklist evidenced, build green, backend pytest green, landing shippable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->landing | Static content only; FAQ Accordion and Sheet drawer are client UI with no data submission |
| footer-links | Anchor-only in-page links; no external fetches in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Spoofing | MoES/IMD strip + footer fine print | high | mitigate | No logos, no endorsement claims; fine print states model estimates are not official IMD warnings |
| T-05-03-02 | Information Disclosure | .env.local.example | low | accept | Example file carries only the placeholder URL http://localhost:8000, no secrets; rationale documented in summary |
| T-05-03-SC | Tampering | npm installs | high | mitigate | Stock shadcn Accordion/Sheet/DropdownMenu only; legitimacy re-checked via npmjs.com if versions change |
</threat_model>

<verification>
- All 8 sections render in D-03 order with real copy; lorem grep clean across frontend/
- No purple-blue gradients, no img/clipart tags, every card domain-specific
- Light/dark token pairs plus 390px responsive classes plus drawer plus theme switcher evidenced in summary
- npm run build in frontend/ passes with zero type errors (ROADMAP criteria 2 and 4)
- python -m pytest tests/ -q from repo root passes (backend regression guard, Phase 04 proven command)
- No frontend/app/chat route exists; backend .py files unmodified
</verification>

<success_criteria>
- ROADMAP Phase 5 criteria met: 8 sections with zero lorem, consistent tokens light/dark, no AI-slop markers, build with no type errors
- FAQ, MoES strip, footer complete with honest sourcing and fine print
- Both suites green; /chat reserved for Phase 6
</success_criteria>

<output>
Create `.planning/phases/05-frontend-shell-landing/05-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- frontend/components/moes-strip.tsx (amber-bordered MoES statement band)
- frontend/components/faq.tsx (6-item stock Accordion, id faq)
- frontend/components/footer.tsx (3 columns plus bottom bar)
- frontend/components/ui/accordion.tsx, frontend/components/ui/sheet.tsx, frontend/components/ui/dropdown-menu.tsx (stock shadcn additions)
- frontend/app/page.tsx (final 8-section assembly in D-03 order)
- .planning/phases/05-frontend-shell-landing/05-03-SUMMARY.md (anti-slop evidence, responsive evidence, build plus pytest results)
