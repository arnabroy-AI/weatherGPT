---
phase: 08-climate-trends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/imd_client.py
  - tools/weather.py
  - services/agent.py
  - tests/fixtures/open_meteo_archive_pune.json
  - tests/test_climate_trends.py
autonomous: true
requirements:
  - CLIM-01
  - CLIM-02
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Asking a past-30-day trend question for Pune returns rain sum, temp mean/min/max, and wettest/driest days computed from archive daily aggregates, never invented
    - Trend replies disclose the 30-day window plus non-IMD model-data sourcing in both JSON source and reply text
    - The agent routes trend questions to get_climate_trends and quotes its numbers exactly under a mocked LLM
  artifacts:
    - tools/imd_client.py (archive fetch plus aggregate mapping)
    - tools/weather.py (get_climate_trends tool)
    - services/agent.py (tool registration plus one trend rule)
    - tests/fixtures/open_meteo_archive_pune.json
    - tests/test_climate_trends.py
  key_links:
    - Archive fetch response flows into aggregate mapping, then into tool JSON, then into the agent reply numbers
---

<objective>
Prove the end-to-end 30-day climate-trend slice for one known city (Pune): fixture-backed archive fetch plus aggregate mapping plus get_climate_trends tool plus one agent prompt rule plus a mocked-LLM test proving reply numbers match fixture aggregates, per D-01, D-02, D-04.

Purpose: De-risk the new provider capability (archive daily aggregates) and the agent wiring before hardening and UI land in Plan 02.
Output: Working trend path behind the IMD-shaped seam with a green mocked-LLM number-match test.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-climate-trends/08-CONTEXT.md
@.planning/phases/02-imd-live-data/02-RESEARCH.md
@tools/imd_client.py
@tools/weather.py
@services/agent.py
@tests/test_agent_grounding.py
</context>

<tasks>

<task type="tracer">
  <name>End-to-end 30-day trend for Pune — archive fetch plus aggregates plus tool</name>
  <files>tools/imd_client.py, tools/weather.py, tests/fixtures/open_meteo_archive_pune.json</files>
  <read_first>tools/imd_client.py (fetch_open_meteo param shape, _rainfall_last_24h aggregation style, set_transport seam, LIVE_SOURCE disclosure pattern); tools/weather.py (CITY_COORDS 18-city map, _CACHE TTL pattern, _fallback_payload stamping, @tool JSON-string contract); .planning/phases/08-climate-trends/08-CONTEXT.md (D-01, D-02)</read_first>
  <acceptance_criteria>First action clears the Windows read-only flag on tools/imd_client.py and tools/weather.py via attrib -R before any edit. Fixture file tests/fixtures/open_meteo_archive_pune.json exists with a daily section holding 30-entry time, temperature_2m_max, temperature_2m_min, precipitation_sum arrays for Pune. Invoking get_climate_trends for Pune under imd_client MockTransport serving that fixture returns a JSON string whose rain_sum_mm equals the rounded sum of the fixture precipitation_sum array and whose temp_mean_c equals the rounded mean of daily means. JSON carries window_days 30 plus window_start and window_end dates plus a source string containing the substring non-IMD. python -m pytest tests/test_climate_trends.py -q passes for the mapping and tool tests present at this point.</acceptance_criteria>
  <action>Per D-01: add OPEN_METEO_ARCHIVE_BASE_URL https://archive-api.open-meteo.com/v1/archive plus ARCHIVE_DAILY_FIELDS temperature_2m_max,temperature_2m_min,precipitation_sum plus fetch_archive_open_meteo(latitude, longitude, transport) mirroring fetch_open_meteo (sync httpx.Client, 6s timeout, params latitude, longitude, past_days 30, daily ARCHIVE_DAILY_FIELDS, timezone UTC, wind_speed_unit kmh omitted as daily-only; transport falls back to the module-level _TRANSPORT_OVERRIDE; sanitized RuntimeError on failure with logger.exception server-side only). Per D-02: add map_archive_to_payload(data, location_display) computing exactly rain_sum_mm (rounded 2dp sum of precipitation_sum), temp_mean_c (rounded 2dp mean of per-day (max+min)/2), temp_min_c (rounded min of temperature_2m_min), temp_max_c (rounded max of temperature_2m_max), wettest_day (date string of max precipitation_sum day), driest_day (date string of min precipitation_sum day), deviation_note (one sentence comparing wettest-day rain against the window daily mean, e.g. Wettest day YYYY-MM-DD at N.N mm vs window daily mean M.M mm), window_days 30, window_start first date, window_end last date, source set to open-meteo-archive (non-IMD model data; IMD-direct pending), location echo capped at 80 chars; defensive .get parsing with safe defaults, never a crash on short arrays (short arrays map over available days; wholly missing daily raises sanitized RuntimeError). In tools/weather.py add get_climate_trends(location: str) as a langchain @tool returning a JSON string: reuse CITY_COORDS lookup, 120-char cap, latlon parse, and Mumbai best-guess plus disclosure exactly like get_current_weather; call imd_client.fetch_archive_open_meteo then map_archive_to_payload; return JSON string with the mapping keys plus cached false, cache_age_s 0, stale false on the fresh path (cache hardening itself is Plan 02, this task returns fresh only plus sanitized fallback on exception). Record the Pune fixture by hand-authoring 30-day daily arrays consistent with the mapping (no live network in CI); reuse Pune coords 18.5204, 73.8567 from CITY_COORDS. Do not touch get_current_weather, get_weather_forecast, routes, or schemas.</action>
  <verify>
    <automated>python -m pytest tests/test_climate_trends.py -q</automated>
    <fails_when>pytest exits non-zero or reports no tests collected, or the tool JSON rain_sum_mm differs from the fixture sum</fails_when>
  </verify>
  <done>One happy path works end-to-end: Pune fixture through archive fetch and mapping through get_climate_trends JSON with correct aggregates and non-IMD source, committed.</done>
</task>

<task type="auto">
  <name>Agent registration plus one trend prompt rule</name>
  <files>services/agent.py</files>
  <read_first>services/agent.py (SYSTEM_PROMPT rules 1-10 style, _TOOLS list, _derive_alert_level fallback); tools/weather.py (get_climate_trends signature from prior task in this plan)</read_first>
  <acceptance_criteria>First action clears the Windows read-only flag on services/agent.py via attrib -R before editing. services/agent.py imports get_climate_trends and lists it in _TOOLS alongside the two existing tools. SYSTEM_PROMPT contains exactly one new numbered rule (rule 11) naming get_climate_trends, stating trend questions about past-30-day rain/temperature call that tool and quote rain_sum_mm, temp_mean_c, wettest_day exactly, disclose the 30-day window plus non-IMD sourcing, and never invent observations. No other prompt rules edited. Current and forecast behavior unchanged.</acceptance_criteria>
  <action>Per D-04 minimal wiring: extend the tools.weather import to include get_climate_trends, append it to _TOOLS without reordering the existing two entries, and append a single rule 11 to SYSTEM_PROMPT following the terse numbered style of rules 1-10 (trend routing, exact-value quoting of rain_sum_mm/temp_mean_c/wettest_day/driest_day, window plus non-IMD disclosure, never invent observations). Keep the Phase 3/4 precedent of one rule only; do not alter _derive_alert_level, process_chat retry logic, or route/schema files. Note the decision as per D-04 in the commit message.</action>
  <verify>
    <automated>python -m pytest tests/test_climate_trends.py -q</automated>
    <fails_when>pytest exits non-zero, or grep for get_climate_trends in services/agent.py returns zero matches</fails_when>
  </verify>
  <done>Agent exposes the trends tool with one new prompt rule; existing rules and tools untouched.</done>
</task>

<task type="auto">
  <name>Mocked-LLM number-match test proving grounded trend replies</name>
  <files>tests/test_climate_trends.py</files>
  <read_first>tests/test_agent_grounding.py (FakeExecutor pattern, mock_open_meteo fixture, _CACHE isolation fixture, ALERT regex usage); tests/fixtures/open_meteo_archive_pune.json (exact aggregate values produced by task 1)</read_first>
  <acceptance_criteria>tests/test_climate_trends.py contains a FakeExecutor-based test mirroring test_agent_grounding.py that patches services.agent._get_executor, feeds REAL get_climate_trends JSON from the Pune fixture via MockTransport, and asserts the canned reply string contains the exact rain_sum_mm value, the exact temp_mean_c value, the wettest_day date string, the substring non-IMD, and the substring 30-day. Cache isolation fixture clears tools.weather _CACHE around every test. python -m pytest tests/test_climate_trends.py -q passes with at least 4 tests: mapping aggregates, tool JSON contract, cache-free fresh stamps, mocked-LLM number match. No test performs live network access.</acceptance_criteria>
  <action>Per D-04: build tests/test_climate_trends.py on the test_agent_grounding.py FakeExecutor seam (patch services.agent._get_executor, invoke returns canned reply plus intermediate_steps carrying real get_climate_trends JSON obtained through imd_client MockTransport serving open_meteo_archive_pune.json; always reset transport after). Cases: mapping test asserting rain_sum_mm/temp_mean_c/temp_min_c/temp_max_c/wettest_day/driest_day/window_days/source against hand-computed fixture values; tool contract test asserting JSON-string return with location Pune and cached/stale stamps; mocked-LLM test asserting the canned trend reply embeds the exact numeric strings from the tool JSON plus non-IMD plus 30-day; outage test asserting MockTransport raising httpx.ConnectError yields stamped mock fallback JSON (source contains mock and stale markers) rather than an exception. Pin transport via imd_client.set_transport and reset in teardown; isolate tools.weather _CACHE per test. Never call the live archive host in any test.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>pytest exits non-zero or any test attempts a live connection to archive-api.open-meteo.com</fails_when>
  </verify>
  <done>Mocked-LLM test proves trend reply numbers match fixture aggregates; full pytest suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive-API → mapping | Untrusted provider daily JSON crosses into tool output and LLM context |
| tool JSON → agent reply | Tool values flow into user-facing grounded text |
| test seam → CI | MockTransport must fully stand in for network in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | map_archive_to_payload daily parsing | high | mitigate | Defensive .get plus type checks on daily arrays; short arrays map available days only; unusable schema raises sanitized RuntimeError; NaN/inf guarded via _as_float |
| T-08-02 | Information disclosure | fetch_archive_open_meteo errors | medium | mitigate | Log-full server-side via logger.exception, send-safe sanitized RuntimeError to caller; no URLs, keys, or tracebacks in user payloads |
| T-08-03 | Information disclosure | trend reply authenticity | high | mitigate | source string carries non-IMD model-data marker end-to-end; agent rule 11 repeats window plus non-IMD disclosure; never described as IMD-issued |
| T-08-04 | Spoofing | wettest/driest day claims | medium | mitigate | Dates derived strictly from fixture/provider arrays (argmax/argmin), never LLM-generated; number-match test pins reply values to tool JSON |
| T-08-SC | Tampering | pip installs | high | mitigate | No new packages in this plan (httpx plus pytest already vendored); no install step so no legitimacy gate needed |
</threat_model>

<verification>
Plan 01 proves the tracer: python -m pytest tests/test_climate_trends.py -q green, then python -m pytest tests/ -q green. Spot-check: parse get_climate_trends Pune JSON and compare rain_sum_mm against the hand-summed fixture precipitation_sum array.
</verification>

<success_criteria>
Pune 30-day trend flows fixture to tool JSON to mocked-LLM reply with exact numbers, window plus non-IMD disclosure present, full pytest green, current/forecast/agent behavior otherwise untouched.
</success_criteria>

<output>
Create `.planning/phases/08-climate-trends/08-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Archive fetch plus aggregate mapping in tools/imd_client.py (this plan)
- get_climate_trends tool in tools/weather.py (this plan)
- Agent tool registration plus one trend rule in services/agent.py (this plan)
- Pune archive fixture plus mocked-LLM number-match tests (this plan)
- Cache/fallback/disclosure hardening, climate strip component, page assembly, coverage update, full gates (Plan 02)
