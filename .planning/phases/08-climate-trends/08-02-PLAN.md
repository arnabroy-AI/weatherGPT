---
phase: 08-climate-trends
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tools/weather.py
  - frontend/components/climate-strip.tsx
  - frontend/app/page.tsx
  - frontend/lib/__tests__/chat-contract.test.mjs
  - .planning/phases/02-imd-live-data/02-COVERAGE.md
  - .planning/phases/03-forecast-alerts/03-COVERAGE.md
autonomous: true
requirements:
  - CLIM-02
  - CLIM-03
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - Repeated trend queries hit a 10-minute cache and archive outages degrade exactly like the current path with stamped mock plus disclosed fallback note
    - Trend replies always disclose the 30-day window plus non-IMD sourcing, including cached, stale, and fallback cases
    - The landing page shows a compact climate strip with real copy and zero lorem reusing the alerts-showcase pattern, and all gates stay green
  artifacts:
    - tools/weather.py (trend cache plus fallback hardening)
    - frontend/components/climate-strip.tsx
    - frontend/app/page.tsx (strip assembly)
    - Coverage matrix update integrating the daily-archive capability
  key_links:
    - Trend cache key namespaces entries so they never collide with current or forecast keys
    - Climate strip renders inside the landing assembly without breaking build or contract tests
---

<objective>
Harden the Plan 01 trend slice (trend cache, outage fallback, disclosure parity per D-03) plus ship the landing climate strip reusing the alerts-showcase pattern per D-05, then run the full gate battery (pytest, tsc, temp-dir build, slop, contract), per CLIM-02 and CLIM-03.

Purpose: Turn the proven tracer into demo-grade behavior with degraded-mode parity and a visible landing surface for the MoES climate-information gap.
Output: Hardened trends tool, climate strip on landing, coverage integration note, all gates green.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-climate-trends/08-CONTEXT.md
@.planning/phases/08-climate-trends/08-01-SUMMARY.md
@tools/weather.py
@frontend/components/alerts-showcase.tsx
@frontend/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Trend cache plus fallback and disclosure hardening</name>
  <files>tools/weather.py, tests/test_climate_trends.py</files>
  <read_first>tools/weather.py (CACHE_TTL_S 600, _CACHE_LOCK, _decorate_hit/_decorate_stale/_decorate_fresh, forecast tuple-key namespacing, _FALLBACK_NOTE disclosure sentence, _fallback_payload shape); .planning/phases/08-climate-trends/08-CONTEXT.md (D-03); .planning/phases/08-climate-trends/08-01-SUMMARY.md (tool behavior built in Plan 01)</read_first>
  <acceptance_criteria>First action clears the Windows read-only flag on tools/weather.py via attrib -R before editing. get_climate_trends uses cache key tuple (climate, lookup_key) namespaced away from current plain-string keys and forecast tuple keys so no collision is possible. Two identical trend invocations under MockTransport produce exactly one transport hit with the second payload stamped cached true. Archive outage with a live cache entry returns the stale-stamped entry with source suffixed plus-stale-fallback and advisory containing the literal fallback note Note: live data unavailable; showing fallback values. Archive outage with an empty cache returns a mock-shaped trend payload carrying window_days 30, source containing mock plus non-IMD, and the same literal fallback note. Fallback and stale payloads are never stored as fresh cache entries. New tests cover cache hit, stale-serve, and empty-cache fallback. python -m pytest tests/ -q passes.</acceptance_criteria>
  <action>Per D-03 exact parity with the current path: wrap the Plan 01 get_climate_trends fresh path with the established cache discipline — tuple key (climate, lookup_key) checked under _CACHE_LOCK with 600s TTL returning _decorate_hit stamps; store only base success payloads; on exception log server-side and serve _decorate_stale when an expired entry exists else a mock-shaped trend fallback built from _fallback_payload field values extended with window_days 30, window_start and window_end set to empty strings, rain_sum_mm 0.0, deviation_note set to Trend data unavailable, and advisory prefixed with the literal _FALLBACK_NOTE sentence; reuse _apply_guess_note best-guess disclosure unchanged; keep JSON-string return and the 120-char cap; never cache error payloads as fresh. Extend tests/test_climate_trends.py with cache-hit single-transport-hit counting, TTL-expiry refetch, stale-serve stamping, and empty-cache fallback disclosure tests using httpx.MockTransport only. Do not alter get_current_weather or get_weather_forecast cache entries or keys.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>pytest exits non-zero, or two identical trend calls produce two transport hits, or any fallback payload lacks the literal fallback note</fails_when>
  </verify>
  <done>Trend cache hits within TTL, outages degrade stamped plus disclosed exactly like the current path, suite green.</done>
</task>

<task type="auto">
  <name>Climate strip component plus landing assembly</name>
  <files>frontend/components/climate-strip.tsx, frontend/app/page.tsx</files>
  <read_first>frontend/components/alerts-showcase.tsx (section wrapper, kicker plus section-title classes, glass-card plus CardContent plus Badge pattern, muted footnote disclosure style); frontend/app/page.tsx (Header, Hero, Features, LiveDemoTeaser, AlertsShowcase, HowItWorks, MoesStrip, Faq, Footer assembly order)</read_first>
  <acceptance_criteria>First action clears the Windows read-only flag on frontend/app/page.tsx via attrib -R before editing. New file frontend/components/climate-strip.tsx exports a ClimateStrip function component reusing the alerts-showcase section wrapper classes (mx-auto w-full max-w-6xl px-4 py-16 md:px-6 md:py-24), kicker paragraph, section-title heading, and glass-card Card pattern with zero new design tokens and zero purple-blue gradient classes. Copy is real only: heading names 30-day trends, body names rain sum plus temperature means plus wettest/driest days, footnote carries non-IMD model-data disclosure; the file contains no occurrence of the string lorem. page.tsx imports ClimateStrip and renders it immediately after AlertsShowcase and before HowItWorks with all existing sections otherwise unmoved. npx tsc --noEmit passes inside frontend.</acceptance_criteria>
  <action>Per D-05: create frontend/components/climate-strip.tsx as a compact strip mirroring alerts-showcase.tsx structure (section with aria-labelledby, centered kicker plus h2 plus intro paragraph, three glass-card facts for 30-day rain sum, temperature mean/min/max, wettest/driest days with concrete example-ish real copy such as Past-30-day rain sum and Mean, min, max temperature plus Wettest and driest days, closing footnote disclosing observed model aggregates over the past 30 days from non-IMD model data). Use only existing tokens and components (Badge, Card, CardContent); add no new colors, fonts, or gradient utilities to respect the anti-slop bans. Assemble in frontend/app/page.tsx directly after the AlertsShowcase element. Keep chat, current, forecast, and agent-orchestration code untouched. Note the choice as per D-05 in the commit message.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>tsc or the Next build exits non-zero, or grep -ri lorem on the two touched frontend files returns any match</fails_when>
  </verify>
  <done>Climate strip renders on landing in the showcase pattern with real copy and zero lorem; frontend build passes.</done>
</task>

<task type="auto">
  <name>Coverage integration plus full gate battery</name>
  <files>.planning/phases/02-imd-live-data/02-COVERAGE.md, .planning/phases/03-forecast-alerts/03-COVERAGE.md, frontend/lib/__tests__/chat-contract.test.mjs</files>
  <read_first>.planning/phases/02-imd-live-data/02-COVERAGE.md (archive opt-out or omission wording to amend); .planning/phases/03-forecast-alerts/03-COVERAGE.md (same); frontend/lib/__tests__/chat-contract.test.mjs (existing contract assertions to keep passing)</read_first>
  <acceptance_criteria>Both COVERAGE files state the Phase 8 decision: the daily-archive trend capability INTEGRATEs with reason (past-30-day observed aggregates via archive-api.open-meteo.com daily fields behind the same seam, disclosed non-IMD) rather than opting out, each naming get_climate_trends and the archive host exactly once or more. Full battery passes in order: python -m pytest tests/ -q, npx tsc --noEmit in frontend, temp-dir production build copy (never delete plus recreate in H colon workspace), slop grep for lorem plus purple gradient utilities returns zero matches on the strip and page files, node --test frontend/lib/__tests__/chat-contract.test.mjs passes. No source behavior changes in this task beyond the coverage files.</acceptance_criteria>
  <action>Record the api-coverage decision: edit the Phase 2 and Phase 3 COVERAGE files to change the archive posture from OPT-OUT or omitted to INTEGRATE with reason, citing the Phase 8 daily-archive trend path (archive-api.open-meteo.com with past_days 30 and daily temperature_2m_max,temperature_2m_min,precipitation_sum behind the imd_client seam, MockTransport plus fixtures only in CI, disclosed non-IMD), so the seal-time gate sees it decided. Then run the gates in order reusing the proven commands verbatim: python -m pytest tests/ -q; npx tsc --noEmit in frontend; temp-copy production build (copy frontend to the approved temp dir and build there since H colon denies deletion, never delete plus recreate in place); slop scan grepping the strip and page files for lorem and gradient clichés; node --test frontend/lib/__tests__/chat-contract.test.mjs. Fix only gate failures attributable to this phase; leave current, forecast, and agent-orchestration behavior otherwise untouched. Sanitize and redact any secrets in output per carry-over rules.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>pytest exits non-zero, or either COVERAGE file lacks the string get_climate_trends</fails_when>
  </verify>
  <done>Archive capability integrated in the coverage matrix with reason and the full gate battery green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trend cache → callers | Cached aggregates must carry the same disclosure as fresh ones |
| fallback path → user | Outage payloads must never read as live observations |
| landing strip → visitor | Static copy must not overclaim IMD issuance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | trend cache keys | high | mitigate | Tuple key (climate, lookup_key) namespaced from current and forecast keys; error payloads never stored as fresh; lock-guarded dict access |
| T-08-06 | Information disclosure | stale and fallback trend payloads | high | mitigate | Stale entries suffixed plus-stale-fallback with age; empty-cache fallback stamped mock plus non-IMD with the literal fallback note; advisory always surfaces the note |
| T-08-07 | Spoofing | climate strip copy | medium | mitigate | Footnote discloses non-IMD model aggregates; no IMD-issued wording anywhere in the strip; zero-lorem grep gate |
| T-08-SC | Tampering | npm installs | high | mitigate | No new npm or pip packages in this plan; build and tsc run on the vendored toolchain only |
</threat_model>

<verification>
Ordered battery: python -m pytest tests/ -q, then npx tsc --noEmit in frontend, then temp-dir production build, then slop grep over frontend/components/climate-strip.tsx and frontend/app/page.tsx, then node --test frontend/lib/__tests__/chat-contract.test.mjs. Coverage grep: both COVERAGE files mention get_climate_trends.
</verification>

<success_criteria>
Trend cache and fallback match the current path with disclosure in all states, climate strip live on landing with real copy and zero lorem, coverage matrix integrates the archive capability with reason, every gate green.
</success_criteria>

<output>
Create `.planning/phases/08-climate-trends/08-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Trend cache plus fallback hardening in tools/weather.py (this plan)
- frontend/components/climate-strip.tsx plus page.tsx assembly (this plan)
- Coverage matrix integration for the daily-archive capability (this plan)
- Archive fetch, tool, agent rule, fixture, number-match tests (Plan 01)
