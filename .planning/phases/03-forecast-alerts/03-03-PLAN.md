---
phase: 03-forecast-alerts
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_forecast_cache.py
  - .planning/phases/03-forecast-alerts/COVERAGE.md
  - .planning/phases/02-imd-live-data/COVERAGE.md
autonomous: true
requirements:
  - ALRT-01
  - ALRT-02
  - ALRT-03
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - Repeated identical forecast queries hit the location-plus-days cache with no duplicate upstream calls inside TTL
    - Forecast outage degrades to partial current-only output with an honest unavailable note and caches nothing bad
    - Secret material never appears in forecast JSON, advisories, rollup, or logs, and the coverage matrix marks daily forecast INTEGRATE
  artifacts:
    - tests/test_forecast_cache.py cache plus fallback plus secret-safety suite
    - .planning/phases/03-forecast-alerts/COVERAGE.md decided daily-forecast matrix
    - .planning/phases/02-imd-live-data/COVERAGE.md forecast rows flipped to INTEGRATE
  key_links:
    - Cache tests exercise the Plan 01 location-plus-days key through MockTransport hit counting
    - Secret probe test mirrors the Phase 2 no-key-leak pattern on the forecast path
    - Coverage update supersedes the Phase 2 OPT-OUT forecast row so the seal-time gate sees a decided matrix
---

<objective>
Harden the forecast path per carryover rules: same 10-min cache keyed location-plus-days per D-06, partial current-only fallback with honest note per D-07, MockTransport plus fixtures only in CI, sanitize and redact, and the pass-plus-report gate. Close the api-coverage loop by flipping the Phase 2 OPT-OUT forecast rows to INTEGRATE.

Purpose: Prove cache correctness, fallback honesty, and secret-safety on the forecast path, and leave a decided coverage matrix for the daily capability.
Output: Cache plus fallback plus secret-safety tests, a Phase 3 COVERAGE.md matrix, and the Phase 2 matrix update.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forecast-alerts/03-CONTEXT.md
@.planning/phases/02-imd-live-data/COVERAGE.md
@tools/weather.py
@tools/imd_client.py
@tests/test_imd_client.py
@tests/test_forecast.py
</context>

<tasks>

<task type="auto">
  <name>Forecast cache, TTL, partial-fallback, and secret-safety tests</name>
  <read_first>tools/weather.py cache plus _decorate plus _fallback plus _FALLBACK_NOTE; tests/test_imd_client.py cache-hit plus TTL-expiry plus outage plus no-key-leak tests; tests/test_forecast.py fixture plus MockTransport pattern from Plan 01</read_first>
  <files>tests/test_forecast_cache.py</files>
  <action>Create tests/test_forecast_cache.py mirroring the Phase 2 cache test idioms against get_weather_forecast under httpx.MockTransport serving the Plan 01 daily fixture, with cache isolation clearing the weather _CACHE around each test. Cover two identical Mumbai 5-day invokes causing exactly 1 transport hit with first cached false age 0 and second cached true; Mumbai 5-day versus Mumbai 3-day causing 2 hits proving the days component of the key; Mumbai versus mumbai-with-spaces sharing one entry with 1 hit proving normalisation; a pre-seeded expired Mumbai 5-day entry forcing a refetch with 1 hit; a daily ConnectError outage returning partial output with empty forecast_days, forecast_available false, a note containing forecast unavailable, live current temperature fields present, stale true, and elapsed under 5 seconds, followed by a healthy invoke causing exactly 1 hit proving no fallback pinning; and a secret probe setting WEATHER_API_KEY to SECRET_PROBE_42 then killing daily transport, asserting the probe string appears in neither the returned JSON nor caplog error output. No test touches the live network and no source file changes are needed; if the Plan 01 key or partial shape differs, assert the actual Plan 01 contract and record the variance in the SUMMARY.</action>
  <verify>
    <automated>python -m pytest tests/test_forecast_cache.py -q</automated>
    <fails_when>pytest reports a failure in hit-count assertions, TTL expiry, partial-fallback shape, the 5-second outage budget, or secret-probe absence</fails_when>
  </verify>
  <acceptance_criteria>Cache-hit test shows exactly 1 upstream call for two identical invokes; days-variant test shows 2 hits; expired entry forces refetch; outage returns empty forecast_days plus forecast_available false plus forecast unavailable note in under 5 seconds with no cache pinning; SECRET_PROBE_42 appears in neither payload nor logs</acceptance_criteria>
  <done>Forecast cache plus fallback plus secret-safety suite green, zero live network, zero source changes</done>
</task>

<task type="auto">
  <name>Coverage matrix plus Phase 2 forecast row flip and full-suite gate</name>
  <read_first>.planning/phases/02-imd-live-data/COVERAGE.md full matrix; tests/test_forecast.py plus tests/test_forecast_alerts.py plus tests/test_forecast_cache.py pass state</read_first>
  <files>.planning/phases/03-forecast-alerts/COVERAGE.md, .planning/phases/02-imd-live-data/COVERAGE.md</files>
  <action>Create .planning/phases/03-forecast-alerts/COVERAGE.md as the decided daily-forecast matrix with an INTEGRATE table rowing the Open-Meteo daily fetch behind the IMD-shaped forecast seam, the 5-day day-wise mapping with per-day alerts, the worst-day Alert line, per-day plus rollup advisories, the location-plus-days 10-min cache, the partial current-only fallback, and the minimal agent wiring, plus an OPT-OUT table rowing persistent or disk cache as forbidden per D-06, full mock forecast bundles as forbidden per D-07, separate stricter alert thresholds as rejected per D-03, and forecast orchestration deferred to Phase 4 per D-08. Edit .planning/phases/02-imd-live-data/COVERAGE.md only to flip its Day-wise forecast OPT-OUT row to an INTEGRATE row noting superseded by Phase 3 COVERAGE.md with the new matrix location, leaving all other rows byte-identical. Then run the full suite as the pass-plus-report gate.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Full suite shows any failure or error, or either COVERAGE.md lacks a decided daily-forecast matrix entry</fails_when>
  </verify>
  <acceptance_criteria>Phase 3 COVERAGE.md exists with INTEGRATE rows covering daily fetch, day-wise mapping, worst-day line, advisories, cache, fallback, and agent wiring plus OPT-OUT rows for disk cache, mock forecast bundles, stricter thresholds, and orchestration; Phase 2 COVERAGE.md forecast row reads INTEGRATE with a supersede note; python -m pytest tests/ -q passes fully</acceptance_criteria>
  <done>Decided matrix visible to the seal-time gate in both locations, full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→source | Tests inject transport failures and secret probes into the forecast path |
| docs→gate | Coverage matrices feed the seal-time decided-matrix gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | forecast error payload caching | medium | mitigate | Fallback and partial payloads never stored as fresh, proven by the no-pinning test |
| T-03-07 | Information disclosure | forecast logs and notes | medium | mitigate | Secret probe test on the forecast path mirrors Phase 2 redaction; sanitized errors only |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan |
</threat_model>

<verification>
Cache proof: hit-count assertions green under MockTransport. Fallback proof: partial shape plus honest note plus under-5s budget. Safety proof: secret probe absent from JSON and logs. Docs proof: both COVERAGE.md files carry decided daily-forecast entries. Gate: python -m pytest tests/ -q fully green.
</verification>

<success_criteria>
Identical forecast queries hit cache within TTL with day-count key isolation; outage yields partial current-only output with honest note and sub-5s timing; secrets never leak; coverage matrices decide daily forecast as INTEGRATE; full suite passes.
</success_criteria>

<output>
Create `.planning/phases/03-forecast-alerts/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- tools/imd_client.py: fetch_forecast_open_meteo plus map_forecast_to_payload with worst-day Alert line (Plan 01)
- tools/weather.py: get_weather_forecast tool with location-plus-days cache plus partial fallback (Plan 01)
- tests/fixtures/open_meteo_forecast_mumbai.json plus tests/test_forecast.py tracer suite (Plan 01)
- Per-day Orange/Red advisories plus overall rollup plus agent wiring (Plan 02)
- Cache, fallback, secret-safety tests plus coverage matrix update (this plan)
</plan>
