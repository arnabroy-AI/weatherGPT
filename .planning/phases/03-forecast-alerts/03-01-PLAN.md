---
phase: 03-forecast-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/imd_client.py
  - tools/weather.py
  - tests/fixtures/open_meteo_forecast_mumbai.json
  - tests/test_forecast.py
autonomous: true
requirements:
  - ALRT-01
  - ALRT-02
estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Invoking get_weather_forecast for Mumbai returns 5 day entries with min/max temp, rain chance, condition, and per-day alert
    - Forecast reply payload carries exactly one worst-day Alert line matching the Phase 1 Alert reply regex
    - Forecast outage still returns current-weather values plus an honest forecast-unavailable note
  artifacts:
    - tools/imd_client.py daily fetch plus per-day mapping
    - tools/weather.py get_weather_forecast tool with location-plus-days cache
    - tests/fixtures/open_meteo_forecast_mumbai.json recorded daily fixture
    - tests/test_forecast.py tracer tests
  key_links:
    - get_weather_forecast invokes imd_client daily fetch through the shared MockTransport seam
    - Per-day alert reuses derive_alert_level and condition text reuses wmo_to_text with no duplicate tables
    - Worst-day Alert line matches the Phase 1 Alert colon level reply regex with the day tag in trailing parens
---

<objective>
Prove the end-to-end 5-day forecast slice for one known city through the new tool, fixture-backed, including the worst-day Alert line. Per D-01 this adds a new get_weather_forecast tool and leaves get_current_weather untouched. Per D-02 each day carries date, min/max C, rain chance pct, condition text, and per-day alert with the same disclosure strings as the current path.

Purpose: Establish the daily-fetch seam and tool contract that Plans 02 and 03 expand with advisories, cache hardening, and agent wiring.
Output: Daily fetch plus mapping in imd_client.py, the new tool in weather.py, a recorded daily fixture, and green tracer tests.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forecast-alerts/03-CONTEXT.md
@.planning/phases/02-imd-live-data/02-RESEARCH.md
@tools/imd_client.py
@tools/weather.py
@tests/test_imd_client.py
</context>

<tasks>

<task type="tracer">
  <name>Tracer: daily fetch plus per-day mapping in imd_client</name>
  <read_first>tools/imd_client.py fetch_open_meteo plus map_open_meteo_to_payload plus derive_alert_level plus wmo_to_text plus _build_advisory; tests/test_imd_client.py MockTransport plus fixture pattern; 02-RESEARCH.md Open-Meteo field reference</read_first>
  <files>tools/imd_client.py</files>
  <action>Add fetch_forecast_open_meteo(latitude, longitude, days, transport) to tools/imd_client.py per D-01, requesting Open-Meteo daily fields temperature_2m_max, temperature_2m_min, precipitation_probability_max, precipitation_sum, weathercode, and wind_speed_10m_max with wind_speed_unit kmh and timezone UTC, reusing the module transport override plus REQUEST_TIMEOUT_SECONDS and the sanitized RuntimeError pattern. Add map_forecast_to_payload(provider_dict, location_display, days) returning location, source with the same LIVE_SOURCE disclosure string, a days list of exactly the requested day count where each entry holds date, temp_min_c, temp_max_c, rain_chance_pct, condition via wmo_to_text, and alert_level via derive_alert_level called with the day weathercode, day wind max, and day precipitation_sum. Derive worst_alert as the most severe per-day level under ordering Green below Yellow below Orange below Red with worst_day as the first day reaching it, and alert_line as the string Alert colon space worst_alert space open-paren three-letter weekday of worst_day close-paren per D-04, e.g. Alert colon Orange open-paren Sat close-paren, which still matches the Phase 1 Alert colon level reply regex. Cap location_display at 80 chars, parse daily arrays defensively with .get plus type checks defaulting to safe values, never crash on short or missing arrays, and never describe derived levels as IMD-issued. Do not touch fetch_open_meteo, map_open_meteo_to_payload, WMO_CONDITION, derive_alert_level, or _build_advisory bodies.</action>
  <verify>
    <automated>python -m pytest tests/test_forecast.py -q</automated>
    <fails_when>pytest reports collection error or any test failure naming map_forecast_to_payload, fetch_forecast_open_meteo, or the Alert line assertion</fails_when>
  </verify>
  <acceptance_criteria>map_forecast_to_payload on a 5-day daily dict returns 5 day entries each with date, temp_min_c, temp_max_c, rain_chance_pct, condition, alert_level; worst_alert equals the severest day level; alert_line starts with Alert colon space plus the worst level and carries the worst weekday in trailing parens; unknown weathercode maps to Unknown open-paren code N close-paren without raising</acceptance_criteria>
  <done>Daily fetch plus mapping exists, reuses wmo_to_text and derive_alert_level with no duplicated tables, and the tracer test file passes</done>
</task>

<task type="auto">
  <name>New get_weather_forecast tool with location-plus-days cache and partial fallback</name>
  <read_first>tools/weather.py full file including _CACHE plus _decorate_fresh plus _fallback_payload plus get_current_weather; tools/imd_client.py fetch_forecast_open_meteo plus map_forecast_to_payload from the tracer task</read_first>
  <files>tools/weather.py</files>
  <action>Add a new langchain tool named get_weather_forecast in tools/weather.py per D-01 with signature location string and days defaulting to 5 and clamped to the 3 to 5 range, leaving get_current_weather byte-identical. Reuse CITY_COORDS resolution, the lat-lon parse, the Mumbai best-guess plus disclosure, the 120-char cap, and normalisation from the current tool. Key the existing _CACHE dict per D-06 as normalised location plus pipe plus day count so forecast and current entries never collide, with the same CACHE_TTL_S 600 expiry, hit stamps cached true with age, and stale-only-on-error serving stamped stale with plus-stale-fallback source suffix. On forecast fetch or schema failure per D-07 return a partial payload containing the current-weather live values for the resolved location fetched via the existing current path plus forecast_days set to an empty list, forecast_available false, and an honest note string containing forecast unavailable naming the location, and never synthesize mock forecast days. Store only successful forecast payloads, never fallback or partial payloads.</action>
  <verify>
    <automated>python -m pytest tests/test_forecast.py -q</automated>
    <fails_when>pytest reports a failure in cache-key isolation between forecast and current entries, in the days clamp, or in the partial-fallback shape assertions</fails_when>
  </verify>
  <acceptance_criteria>Invoking get_weather_forecast with Mumbai returns JSON with forecast_days of length 5, source containing open-meteo, and an Alert line; invoking with days 2 clamps to 3 and days 9 clamps to 5; killing the daily transport returns current-weather temperature fields plus empty forecast_days plus forecast_available false plus a note containing forecast unavailable; get_current_weather tests still pass unchanged</acceptance_criteria>
  <done>New tool works end-to-end under MockTransport, caches per location-plus-days, degrades to partial current-only on outage, and the current tool is untouched</done>
</task>

<task type="auto">
  <name>Recorded daily fixture plus tracer tests</name>
  <read_first>tests/test_imd_client.py fixture plus MockTransport plus cache-isolation patterns; tests/fixtures/open_meteo_mumbai.json current sample shape</read_first>
  <files>tests/fixtures/open_meteo_forecast_mumbai.json, tests/test_forecast.py</files>
  <action>Create tests/fixtures/open_meteo_forecast_mumbai.json as a recorded-shape Open-Meteo daily response for Mumbai covering 5 consecutive dates with daily arrays temperature_2m_max, temperature_2m_min, precipitation_probability_max, precipitation_sum, weathercode, and wind_speed_10m_max, where at least one day uses a severe code such as 95 with high precipitation_sum so the worst-day level is Orange or Red, plus latitude, longitude, and utc_offset_seconds fields. Create tests/test_forecast.py serving that fixture through httpx.MockTransport via imd_client.set_transport with reset in teardown and cache isolation mirroring test_imd_client.py, asserting end-to-end get_weather_forecast.invoke for Mumbai returns 5 days with min below or equal max, rain chance 0 to 100, condition text, per-day alert, an alert_line matching regex Alert colon space Green Yellow Orange Red with trailing parens day tag, worst_alert equal to the severest day, source disclosing non-IMD model data, transport hit count exactly 1 across two identical invokes proving the cache, and the partial current-only shape when the daily handler raises httpx.ConnectError. Use only MockTransport; no live network.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <fails_when>Full suite shows any failure or error, or any test performs live network access outside MockTransport</fails_when>
  </verify>
  <acceptance_criteria>python -m pytest tests/test_forecast.py -q passes with at least 5 tests covering mapping, worst-day line, cache hit, outage partial, and clamp; python -m pytest tests/ -q passes with zero regressions to Phase 1 and Phase 2 suites</acceptance_criteria>
  <done>Fixture plus tracer tests committed, full suite green, no live calls in CI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider→tool | Untrusted Open-Meteo daily JSON crosses into tool output and agent replies |
| user→tool | Untrusted location and days inputs reach URL params and cache keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | map_forecast_to_payload daily parsing | high | mitigate | Defensive .get plus type checks on every daily array, length-mismatch tolerance, safe defaults, unknown-code text fallback, no eval |
| T-03-02 | Information disclosure | fetch_forecast_open_meteo error path | medium | mitigate | Sanitized RuntimeError with no URLs or keys, log-full server-side only, reuse Phase 1 sanitize pattern |
| T-03-03 | Tampering | forecast cache key | medium | mitigate | Key strictly normalised location plus day count, never cache fallback or partial payloads as fresh |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan; httpx plus pytest already vendored |
</threat_model>

<verification>
Tracer proof: python -m pytest tests/test_forecast.py -q green, then python -m pytest tests/ -q green with no regressions. Spot-check: worst-day Alert line matches regex Alert colon space level and carries the day tag in parens.
</verification>

<success_criteria>
Mumbai 5-day forecast returns day-wise min/max plus rain chance for 5 days; heavy-rain fixture day surfaces Orange or Red in both the Alert line and worst_alert; outage returns partial current-only with honest note; get_current_weather untouched.
</success_criteria>

<output>
Create `.planning/phases/03-forecast-alerts/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- tools/imd_client.py: fetch_forecast_open_meteo plus map_forecast_to_payload with worst-day Alert line (this plan)
- tools/weather.py: get_weather_forecast tool with location-plus-days cache plus partial fallback (this plan)
- tests/fixtures/open_meteo_forecast_mumbai.json plus tests/test_forecast.py tracer suite (this plan)
- Per-day Orange/Red advisories plus overall rollup plus agent wiring (Plan 02)
- Cache, fallback, secret-safety tests plus coverage matrix update (Plan 03)
</plan>
