---
phase: 03-forecast-alerts
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tools/imd_client.py
  - services/agent.py
  - tests/test_forecast_alerts.py
autonomous: true
requirements:
  - ALRT-02
  - ALRT-03
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 2
  confidence: med
must_haves:
  truths:
    - Every Orange or Red forecast day carries a 1 to 2 line safety advisory and the reply carries one overall trip-level rollup line
    - The agent exposes the forecast tool and its prompt states when to call it for multi-day questions
    - The worst-day Alert line still parses through the Phase 1 Alert reply regex
  artifacts:
    - tools/imd_client.py per-day advisories plus rollup builder
    - services/agent.py forecast tool registration plus one prompt rule
    - tests/test_forecast_alerts.py advisory plus rollup plus wiring tests
  key_links:
    - Per-day advisory reuses _build_advisory texts with no forked severity wording
    - Agent _TOOLS list includes get_weather_forecast alongside get_current_weather
    - Worst-day line keeps Alert colon level prefix so _derive_alert_level needs no change
---

<objective>
Add per-day Orange/Red advisories plus the overall trip-level rollup per D-05, reusing derive_alert_level per D-03 and _build_advisory texts, and apply the minimal agent wiring per D-08: register the new tool plus one prompt rule. Full forecast orchestration stays out of scope for Phase 4.

Purpose: Satisfy ALRT-03 safety guidance and ALRT-02 single Alert line while keeping the agent change to registration plus one line.
Output: Advisory plus rollup fields in the forecast payload, agent tool registration plus prompt rule, and wiring tests.
</objective>

<execution_context>
@C:/Users/USER/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forecast-alerts/03-CONTEXT.md
@tools/imd_client.py
@services/agent.py
@tests/test_forecast.py
</context>

<tasks>

<task type="auto">
  <name>Per-day advisories plus overall rollup in forecast mapping</name>
  <read_first>tools/imd_client.py _build_advisory plus map_forecast_to_payload plus derive_alert_level from Plan 01; tests/test_forecast.py worst-day assertions</read_first>
  <files>tools/imd_client.py</files>
  <action>Extend the Plan 01 map_forecast_to_payload in tools/imd_client.py per D-05 so each day entry gains an advisory string built by calling the existing _build_advisory with the day alert_level, day condition, and a location-plus-date label, with zero changes to _build_advisory bodies and no forked severity wording. Add a top-level rollup_advisory string: when no Orange or Red day exists it states no severe weather expected naming the location and day span; when severe days exist it states in one to two lines the worst level, the worst weekday in parens, which dates carry Orange or Red, and one trip-level guidance sentence such as reconsider outdoor plans on those days. Keep alert_line exactly as Plan 01 defined it per D-04. Preserve all defensive parsing, the 80-char caps, and the never-IMD-issued wording.</action>
  <verify>
    <automated>python -m pytest tests/test_forecast_alerts.py -q</automated>
    <fails_when>pytest reports a failure in per-day advisory presence on Orange or Red days, in rollup content for severe versus calm forecasts, or in alert_line format regression</fails_when>
  </verify>
  <acceptance_criteria>A 5-day payload with an Orange day shows that day advisory containing avoid unnecessary travel, a Red day advisory containing avoid travel and stay indoors, Green days carry no-severe wording, and rollup_advisory names the worst level plus worst weekday plus the severe dates; a calm all-Green payload rollup states no severe weather expected; alert_line still matches Alert colon space level with trailing parens day</acceptance_criteria>
  <done>Advisories plus rollup live in the forecast payload reusing existing advisory texts, covered by tests, alert_line unchanged</done>
</task>

<task type="auto">
  <name>Minimal agent wiring plus regex compatibility proof</name>
  <read_first>services/agent.py SYSTEM_PROMPT plus _TOOLS plus _derive_alert_level plus _VALID_ALERTS; tools/weather.py get_weather_forecast tool name from Plan 01</read_first>
  <files>services/agent.py, tests/test_forecast_alerts.py</files>
  <action>In services/agent.py per D-08 import get_weather_forecast alongside get_current_weather and append it to _TOOLS so the list holds exactly both tools, and add exactly one rule line to SYSTEM_PROMPT stating for multi-day or weekend or trip forecast questions call get_weather_forecast with the best location and requested day count. Make no other agent changes: no orchestration, no location resolution, no retry or timeout or parsing edits. In tests/test_forecast_alerts.py add wiring tests asserting get_weather_forecast is present in the agent _TOOLS collection by name, the SYSTEM_PROMPT mentions get_weather_forecast exactly once, and the Plan 01 alert_line string such as Alert colon Orange open-paren Sat close-paren fed to the agent _derive_alert_level returns Orange, plus a Red variant, proving the Phase 1 reply regex still matches the worst-day format with no parser change.</action>
  <verify>
    <automated>python -m pytest tests/test_forecast_alerts.py tests/test_forecast.py -q</automated>
    <fails_when>pytest reports a missing tool registration, a prompt rule count mismatch, a regex parse failure on the worst-day line, or any Plan 01 tracer regression</fails_when>
  </verify>
  <acceptance_criteria>Agent _TOOLS contains both get_current_weather and get_weather_forecast; SYSTEM_PROMPT contains one get_weather_forecast rule line; _derive_alert_level on Alert colon Orange open-paren Sat close-paren returns Orange; full forecast plus alerts test files pass together</acceptance_criteria>
  <done>Agent exposes the forecast tool with a one-line prompt rule, regex compatibility proven, no orchestration added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool→agent | Forecast advisory strings flow into LLM-grounded replies |
| user→agent | Multi-day questions route to the new tool with user-supplied location |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Spoofing | advisory text authority | medium | mitigate | Advisories reuse vetted _build_advisory wording, capped at 80-char inputs, never claim IMD issuance |
| T-03-05 | Information disclosure | agent tool surface | low | mitigate | Registration only, no new secrets or params; location handling reuses the 120-char cap |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan |
</threat_model>

<verification>
Advisory proof: python -m pytest tests/test_forecast_alerts.py -q green with Orange and Red advisory text assertions. Wiring proof: tool registered, single prompt rule, worst-day line parses to its level. Regression: Plan 01 tracer file still green.
</verification>

<success_criteria>
Orange/Red days each carry 1 to 2 line safety advisories plus one overall rollup line; agent lists the forecast tool with one prompt rule; worst-day Alert line parses through the unchanged Phase 1 regex.
</success_criteria>

<output>
Create `.planning/phases/03-forecast-alerts/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- tools/imd_client.py: fetch_forecast_open_meteo plus map_forecast_to_payload with worst-day Alert line (Plan 01)
- tools/weather.py: get_weather_forecast tool with location-plus-days cache plus partial fallback (Plan 01)
- tests/fixtures/open_meteo_forecast_mumbai.json plus tests/test_forecast.py tracer suite (Plan 01)
- Per-day Orange/Red advisories plus overall rollup plus agent wiring (this plan)
- Cache, fallback, secret-safety tests plus coverage matrix update (Plan 03)
</plan>
